# Flows Clone Task List

## Current Status
- [x] AUD-041 independent-ordering correction — present typed remembered-project recovery only after the exact expected blank startup authority is adopted, without treating that legitimate adoption epoch advance as stale; bind delayed display to committed-authority ordering; and clear renderer recovery after every committed canonical Open/New/authority event, including another-window blank replacement, while canceled and rejected choices keep the prompt. Production/tests: `830efd9e`; superseding author evidence is in `docs/notes/943-aud041-startup-recovery-ordering-correction.md`. Fresh independent review remains mandatory; no approval or integration is claimed.
- [x] AUD-041 startup recovery author correction — keep normal launch blank unless last-project reopening is explicitly enabled; preserve an unavailable/corrupt/invalid remembered `.sloom` path plus a typed failure; discover adjacent backups; and offer Retry, Open Another, Recover Backup, or session-only Continue Blank through the same authority-checked Paper/Image replacement transaction as File → Open. Production/tests: `7469e81d` plus authority-supersession follow-up `d5bff0e2`; author evidence is in `docs/notes/942-aud041-startup-project-recovery.md`. Fresh independent review remains mandatory; no integration or audit closure is claimed.
- [x] AUD-029 superseding media-boundary review correction: route `data:`, `blob:`, and remote image/video/audio/document references through the same bounded MIME-family validation; retain sanitized renderer/Electron/Android status detail; and permanently prove wrong-family pre-submit rejection, source-video object-URL cleanup on success/failure/cancel, and remote PDF materialization. Production/tests: `978b6c41`; evidence is in `docs/notes/943-aud029-media-boundary-review-correction.md`. Fresh independent review remains mandatory; no approval, integration, or closure is claimed.
- [x] AUD-029 downstream media materialization author correction: centralize remote image/video/audio/document inputs behind renderer fetch plus the existing Electron/Android fallback; require successful status, strict media MIME, bounded bytes, cancellation, and query-free error identity; and prove representative BFL image, Vertex Veo video, and ElevenLabs audio routes never forward raw provider URLs. Production/tests: `21cb6c58`; evidence is in `docs/notes/942-aud029-downstream-media-materialization.md`. Superseded by the review correction above; no approval, integration, or closure is claimed here.
- [x] AUD-039 local-upscaler truth correction: retain the persisted `local-ai-cpu` compatibility identifier, but remove every user-facing CPU-only promise from the managed `realesrgan-ncnn-vulkan` route; publish an explicit Vulkan/no-CPU-fallback capability contract; turn simulated no-Vulkan process failures into an actionable typed response; and cover Paper, Image, help, runtime-capability, TypeScript, lint, build, and clean-diff author gates. Fresh independent approval remains mandatory; see `docs/notes/941-aud039-local-vulkan-upscaler-truth.md`.
- [x] FBL-031 edit-ownership epoch correction — bind standalone `.slppr` opens to one uninterrupted holder grant plus a local continuity epoch, preserving same-holder heartbeats while rejecting release, transfer, return-to-local reacquisition, and managed/unmanaged round trips; queued requests capture the current epoch only at their own queue admission. Production/tests: `80700a3f`; evidence is in `docs/notes/941-fbl031-edit-ownership-epoch-correction.md`. Fresh independent review remains mandatory; no approval, integration, or audit closure is claimed.
- [x] FBL-031 superseding concurrent-open correction — serialize the complete canonical standalone `.slppr` ownership transaction across menu/picker/external-open/baton-handoff routes so two same-digest opens cannot both claim an absent rollback baseline; retain unrelated application concurrency, submission-owned bytes, queue recovery after failure, and fresh authority at queue admission. Production/tests: `6a727b50`; evidence is in `docs/notes/940-fbl031-concurrent-open-ownership-correction.md`. Fresh independent review remains mandatory; no approval, integration, or audit closure is claimed.
- [x] FBL-031 standalone `.slppr` ownership author correction — converge native menu, browser/Android picker, external-open, and baton-handoff entry points on one exact Paper/edit-baton/desktop-authority transaction; stage managed records off-store, publish them atomically with exact rollback, preserve additive clean-tab identity/backing paths, and tolerate same-holder heartbeats without tolerating ownership transfer. Production/tests: `f8999909`; evidence is in `docs/notes/939-fbl031-standalone-slppr-ownership-transaction.md`. A fresh independent gate remains mandatory; no approval, integration, or audit closure is claimed.
- [x] AUD-030 supported-route policy-matrix correction: prompt pass-through and Android generation remain on-device when provider proxying is configured; intentionally proxied Local/Open execution owns a registered zero-delay policy; every supported direct/proxy route resolves to a registered, independently instantiated limiter; and permanent execution tests prove the real local/proxy destinations. Production/tests: `46559c4a`; evidence is in `docs/notes/941-aud030-supported-route-policy-matrix-correction.md`. Fresh independent review remains mandatory; no approval, integration, or closure is claimed.
- [x] AUD-030 provider start-scheduler correction: provider admission now owns operation starts rather than whole polling lifetimes; Atlas, BytePlus, backend-proxy, local/open, Android, and local transforms use truthful independent policy keys; paid-job retry/cancellation ownership is unchanged; and deterministic tests prove cross-provider concurrency, same-policy spacing, queued cancellation, and failed-start recovery. Production/tests: `54991f64`; evidence is in `docs/notes/934-aud030-provider-start-scheduler.md`. Superseded by the supported-route policy-matrix correction above; no approval or integration is claimed here.
- [x] Refresh the local Sloom Studio Application Menu build and correct the shutdown race exposed by the installed-build check: capture the Flow window's `WebContents` while its `BrowserWindow` is live so renderer-authorization teardown never dereferences a destroyed window. The focused Electron main-process gate passes 49/49, lint/build/package pass, the installed archive exactly matches the fresh package, and an isolated native `Browser.close` exits 0 without the destroyed-object exception. See `docs/notes/933-installed-application-menu-shutdown-correction.md`.
- [x] AUD-019 independent-gate correction — retain the schema-v1 ordered Paper workspace sync while failing closed on unsupported envelope versions, making managed-record repository/store publication atomic with exact baseline rollback, binding image/font/license/ICC records to their authored roles before publication, and staging LAN inventory until the authority accepts the envelope. Production/tests: `c55b94c`; evidence is in `docs/notes/930-aud019-independent-gate-corrections.md`. Fresh independent review remains mandatory; no integration or closure is claimed.
- [x] AUD-019 author correction — replace active-document-only Paper sync with a schema-v1 ordered workspace envelope, exact active-tab identity, complete managed image/font/license/ICC reference inventory, acknowledged bytes-before-envelope publication, transactional SHA-256 receiver staging, and serialized apply. The hybrid snapshot discriminator keeps legacy peers on the safe active-document path; current legacy inbound changes are routed by document/page identity and ambiguous multi-tab replacements fail closed. Production/tests: `d8c763c`; evidence is in `docs/notes/929-aud019-paper-workspace-managed-asset-sync.md`. Superseded by the independent-gate correction above; no integration or closure is claimed here.
- [x] FBL-023 requested-value and exception-safety author correction: retain all source/delimiter/blank/geometry/typography and inline-newline fixes while verifying requested native Canvas typography values, routing percentage stretch and rejected/invalid state through exact bounded CSS measurement, and containing DOM/font-platform exceptions without stale-state reuse or probe leaks. Production/tests: `98d50a5`; evidence is in `docs/notes/927-fbl023-requested-value-exception-safety.md`. A fresh Terra Context Cartographer gate remains mandatory; no approval, integration, or closure is claimed.
- [x] AUD-040 final desktop external-open correction: preserve one stable second-instance delivery identity, snapshot an opened file through one descriptor before queueing, share dirty Paper/Image Save/Discard/Cancel authorization, and serialize normal Open/New/Save with accepted external transitions. Production/tests: `f3a4ad2`; author verification is recorded in `docs/notes/overlap-sol-aud040-desktop-final-correction-2026-07-17.md`; fresh Terra review is pending.
- [x] Exact managed-font shipping-blocker correction: authenticate the digest alias before live rich-editor paint/commit; preserve human family, weight, stretch, variable coordinates, and explicit oblique angle through DOM serialization/reopen; reject TTC/OTC browser/member-zero masquerades instead of relying on Chromium-ignored URL fragments; and make Paper Source Library plus Video storyboard publication await exact payload rasterization with typed zero-side-effect failure. Production/tests commit awaits fresh independent review; no approval is claimed. See `docs/notes/924-exact-managed-font-shipping-blockers-closed.md`.
- [x] FBL-005/AUD-020 Terra reconciliation: port exact managed Paper face identity onto the current portable-asset integration without replacing its canonical Project Library builder; preserve oblique angle/stretch/relative rich-run identity, reject descriptor collisions and hostile portable metadata, require requested-identity browser readiness before print, and retain transactional asset staging. Production/tests commit pending a fresh independent Sol gate; no approval claimed. See `docs/notes/920-fbl-005-aud-020-terra-reconciliation.md`.
- [x] AUD-001 atomic-authority correction: retained `a286a91`, `d8c0baa`, and `247d07e`; `7f24400` plus follow-up `91aa71d` make temporary Source URL ownership transactional across the entire Open/New renderer/native handoff, including unregistered live-import URLs, prepared-B cleanup, shared URL reference counting, exact-once successful release, every renderer-stage rollback, native rejection, concurrent pre-commit rejection, repeated failure, and cleanup exceptions. Exact clean `91aa71d` received fresh independent Terra approval on 2026-07-17 after Terra first rejected `7f24400` and the reproduced blocker was corrected without rewriting history. See `docs/notes/overlap-sol-aud001-source-url-lifetime-evidence-2026-07-17.md`.
- [x] **AUD-002 Terra correction — immutable Flow run ownership:** complete the K3 baseline with immutable dependency-graph ownership, exact graph cancellation/invalidation, live duplicate hydration safety, nonrecyclable identity coverage for every creation/hydration path, atomic Source/result publication rollback, once-only incurred-provider usage attribution, same-workspace replacement invalidation, one-per-root dependency memoization, paste identity scope, and no-op revision stability. Fresh Sol and Kimi checks passed the corrected branch. Integration remains intentionally held for combined reconciliation with AUD-006/AUD-008. See `docs/notes/916-aud-002-terra-run-ownership-correction.md`.
- [x] **AUD-006 final correction — API Requester exactly-once, persistence, and cancellation.** Supersedes the earlier reachability-only repair: runtime callback attachment now preserves live running state while hydration resets stale activity; duplicate root runs coalesce and cancellation remains visible; a run-scoped promise memo executes diamond dependencies once; arbitrary API Requester calls never enter automatic replay; header/body/query credential projection is redacted for export/project/local persistence and reopened redactions fail closed until replaced; custom headers override case-insensitively; and pending stream reads race cancellation, cancel once, and release their lock. Permanent deterministic coverage includes duplicate click/UI, Function diamond, methods/body, status/retry, binary/empty/oversize/UTF-8 streams, pending-read abort, usage/Source Bin, and JSON/form persistence. Awaiting a fresh independent gate; no self-approval is claimed. See `docs/notes/overlap-codex-aud006-final-correction-2026-07-16.md`.
- [x] FBL-011 Terra final-blocker correction: retain the schema-v2 identity/fail-closed persistence work, make native Video frame-export pre-layout await registration and use the same exact-alias typography metrics as card paint, and remove render-time ref mutation from the Video registration gate with a committed-effect/suspended-render regression. Implementation is complete and awaits a fresh Terra read-only gate; no approval is claimed. See `docs/notes/overlap-kimi-fbl-011-terra-final-blocker-repair-2026-07-16.md`.
- [x] Close Terra's callable-metadata, no-OffscreenCanvas fallback, and global symbol-retention Image snapshot findings: reject callable resource metadata before ownership, fail with a typed actionable unsupported-platform error instead of returning a partial Canvas shape, bind symbols per verified cache entry, and prove repeated fresh-symbol invalidation/disposal. See `docs/notes/921-image-snapshot-function-platform-symbol-lifecycle-correction.md` and production/tests commit `1de7a81`.
- [x] Close Terra's direct-data, symbol-key, and hardening-rollback Image snapshot findings with one bounded metadata contract, transactional retained-resource handling, permanent bitmap/mask cache regressions, and full lineage verification. See `docs/notes/920-image-snapshot-direct-data-symbol-transaction-correction.md` and production/tests commit `df4ac2f`.
- [x] Close Terra's final Image snapshot resource-metadata coverage gaps: treat `data` as opaque only at the direct proven pixel-container position, bound and content-bind nested binary metadata, establish non-extensible own-field coverage before verified readiness so arbitrary Proxy `ownKeys` cannot hide expandos, fail closed on revoked/trapping resources, preserve no-readback/no-codec readiness and rollback/disposal behavior, and add bitmap/mask cached/uncached plus getter/prototype/cycle/depth/aggregate regressions. See `docs/notes/919-image-snapshot-resource-metadata-coverage-final-correction.md` and production/tests commit `95dd2c6`.
- [x] Repair Terra's final snapshot-integrity gate: enforce exact one-to-one snapshot layer/proof identity, move deep content verification out of React render into an exact immutable-resource verified-state lifecycle, bound project/`.slimg` snapshot decode work before allocation/hash, correct the changed-file lint regression, and add identity/cache/invalidation/bounds coverage plus corrected evidence. See `docs/notes/915-image-snapshot-verified-state-final-repair.md` and production/tests commit `71d7f11`.
- [x] Repair Terra's final Image snapshot content-integrity BLOCK: bind every bitmap, mask, and selection payload to canonical role/dimension/layer-aware SHA-256 proof; recompute proof at project/`.slimg` save boundaries; reject same-size mutation, swaps, and digest tampering transactionally; preserve legacy snapshots as unavailable; prove fresh-store readiness, rollback, and exact partial-resource disposal. See `docs/notes/overlap-sol-image-snapshot-integrity-2026-07-16.md` and production/tests commit `37cc1fb`.
- [x] Repair Terra's BLOCK on AUD-021/AUD-022 and overlapping AUD-023: make named Image snapshots structurally proven and selection-complete across project/`.slimg` persistence, make live project pixel decode transactional, dispose owned snapshot resources across every lifecycle boundary, clear/restore the selection registry with document lifecycle, add corruption/rollback/disposal/selection coverage, and record the follow-up evidence without rewriting history. See the Terra BLOCK follow-up in `docs/notes/overlap-sol-image-snapshot-integrity-2026-07-16.md` and production/tests commit `2f05169`.
- [x] **AUD-015 final correction — ordered settings/license identity convergence.** Replaced the global late-hydration marker drain with per-key read-epoch + durable-write ownership, serialize stamped encrypted writes across renderer windows, and claim license generations before activation/import awaits. Permanent deterministic coverage proves two-window BroadcastChannel removal, API-key removal with an unrelated provider change, activation→remove/import, overlapping reads, failed-read recovery, delayed encryption, and three-window remove→activate convergence. See `docs/notes/914-aud015-ordered-settings-license-convergence.md`; fresh provider final gate remains required.
- [x] **AUD-015 Sol follow-up — reconcile legacy verifier scheduling and complete the build gate.** The reported same-key rehydrate and activation→backup-import failures were fixture bleed from recursively queued per-record persistence after `vi.resetModules()`, not license-identity ownership regressions; the older hydration fixture also now treats immutable record decrypts separately and sequences simulated remote cache replacement after prior local persistence. All proof assertions remain active. A behavior-equivalent checksum-regex spelling fixes the repository lint blocker. Final evidence is in `docs/notes/917-aud015-legacy-scheduling-build-gate.md`; fresh Terra approval remains mandatory.
- [x] **AUD-015 superseding correction — durable stale-read convergence.** A hydration snapshot now revalidates its stamped generation against storage immediately before merge and re-reads the current encrypted blob when decrypt races a later durable write; a separate committed-generation sidecar plus released failed claims prevents an abandoned reservation from poisoning recovery. Permanent coverage includes the exact version-10 two-window dropped-broadcast resurrection, three-window reload convergence, duplicate notices, removal generations, delayed decrypt/encrypt, storage fallback, and same/different-key races. See `docs/notes/915-aud015-durable-stale-read-convergence.md`; fresh provider final gate remains required.
- [x] Independently recovered and verified the incomplete Fable 5 broken-feature audit without treating its placeholder as authoritative; compared supported findings with the completed Codex correctness audit, recorded new issues, corroborations, corrections, unverified leads, and a consolidated repair order in docs/audits/fable-partial-audit-comparison-2026-07-16.md, with process notes in docs/notes/913-fable-partial-audit-comparison.md.
- [x] Repository-wide static correctness audit: inspected Flow/provider execution, Image/Video media editing and export, Paper/print/project persistence, settings/startup, and Electron/platform integration; recorded 44 evidence-backed findings with severity, exact source paths, failure/reproduction guidance, repair direction, test gaps, green baseline, and clean areas in `docs/audits/codebase-correctness-audit-2026-07-16.md`, with process notes in `docs/notes/911-codebase-correctness-audit.md`.
- [x] Sloom Studio origin-story demo zine, Flow navigation, and Paper document tabs: expand the cleaned Signaloom feature into a complete 16-page typography-led Paper publication with a cover, long-form articles, varied editorial pacing, and multiple fictional advertisements; build the new visual campaigns as reusable-reference composites in clearly named Flow workspaces; add ordinary-wheel vertical pan, Shift+wheel horizontal pan, and Ctrl/Cmd+wheel zoom to Flow; support multiple open Paper documents as tabs within one `.sloom` project and import `.slppr` files as additional project documents; create a reusable Paper-layout skill; install and visually proof the configured desktop build. See `docs/notes/910-sloom-studio-origin-zine.md`.
- [x] Paper unified rich-typesetting completion: make every Inspector typography control update retained rich content correctly, preserve highlighted ranges while using docked/portalled controls, support selected-word character properties and selected-paragraph typesetting, replace fragile colour inputs, give the floating toolbar a compact advanced typography panel, round-trip every rich property/link through editing and print HTML, and verify/install the configured desktop build. See `docs/notes/909-paper-unified-rich-typesetting.md`.
- [x] Integrate the vetted `/home/cabewse/work_SPaC3/fonts` collection as Sloom Studio's shared managed font library: package all 116 families/430 faces with their license evidence, provide a searchable preview-first browser across Paper/Image/Video, pin exact selected faces into Paper documents, improve typography controls (including kerning and face-aware weights/styles), and prove exact output preservation (native embedded Unicode type in PDF/X; deterministic resolved glyphs in flattened KDP output). Installed configured Linux build refreshed and verified. See `docs/notes/908-shared-managed-font-library.md`.
- [x] Restore Paper canvas/export visual parity for parent-page stacking, balanced text wrapping, and translucent frame compositing; the real bilingual Signaloom proofs/PDFs now retain parent bands, authored opacity, matching line breaks, page-one loom photography, and page-two advertising photography in the refreshed installed configured build. See `docs/notes/907-paper-canvas-export-fidelity.md`.
- [x] Repair Paper CMYK production setup and strict export delivery: bundled redistribution-cleared ICC profiles can now be installed as exact managed document assets; Soft Proof offers in-place profile setup, identifies the current page, and materializes every managed image before ICC simulation; KDP uses a native Save dialog and deliberately flattens complete pages to 300-DPI process CMYK PDF/X-1a with 0.125-inch bleed; one-byte ICC TAC quantization is corrected without hiding real ink-limit violations; and both Signaloom magazine demos embed/select FOGRA39. Installed-app smoke tests produced complete English/Japanese soft proofs and two-page KDP PDFs with both photographs intact. See `docs/notes/906-paper-kdp-soft-proof-profile-repair.md`.
- [x] Fix native Paper PDF and page-image export so the destination chooser opens before expensive rasterization, progress/result feedback remains visible without the Inspector, and successful exports expose their exact file/folder path with an open action. Also repaired the shared flattened-page SVG markup that made image-bearing pages fail both formats at decode time. The refreshed installed app preserved the configured profile and produced a valid two-page A4 PDF plus two 1600×2263 PNG pages from the real Signaloom magazine. See `docs/notes/905-paper-native-pdf-png-export-fix.md` and `docs/superpowers/specs/2026-07-15-paper-export-destination-feedback-design.md`.
- [x] Flow typed-port contract/runtime parity audit: independently verify every input handle and dynamic node variant against the data types its runtime actually consumes; restore every supported type currently rejected by exact connection validation (including Image/Video reference descriptions and Source Bin text/container ingest); reject or implement every declared-but-unconsumed type; keep multi-edge media preview, execution dependency discovery, and prompt aggregation stable regardless of edge order; generate a durable 63-node parity matrix and wire it into the production Flow gate. See `docs/notes/903-flow-contract-runtime-parity-audit.md` and `docs/audits/flow-node-audit-2026-07-15.md`.
- [x] Signaloom magazine demo: created two professional two-page Paper spreads (English and Japanese) about developing Signaloom, generated the shared editorial and Sloan Studio T-shirt demo-ad assets in a dedicated Flow project, reserved the lower half of page two for the clearly labeled concept advertisement, then completed a contemporary editorial redesign with clean presentation defaults, independent Japanese vertical lanes, and seven native furigana annotations. Saved both as standalone `.slppr` files in `/home/cabewse/Documents/Loom Workspace` and native-opened both in Paper with correct facing-page order and zero overset text. See `docs/notes/904-signaloom-bilingual-magazine-demo.md`.
- [x] Refresh the local Linux Application Menu install from the integrated `main` build and align all launcher templates to one top-level `AudioVideo` category so desktop validators stay clean and menu implementations do not show duplicate Sloom Studio entries. See `docs/notes/902-linux-main-application-menu-install.md`.
- [x] **Full Flow workspace node/provider/Vertex audit (docs/notes/901).** All 63 node types now have executable purpose/port/example contracts; new and saved edges use exact text/number/boolean/JSON/media/container/control typing; handles and directional edges share data-type colors/patterns; Image reference handles alternate on exterior edges and remain visibly blocked above each model's limit. Shared API contracts cover 182 curated text/image/video/audio models (178 normal options), preserve unconfigured selections, filter vestigial discovery IDs, and gate controls/requests by exact model/API capability. Vertex ADC JSON import/detection/project selection works in-app without terminal commands across Windows/macOS/Linux/Android for supported credential types. Added generated node/model matrices, migration fixtures, `verify:flow-production`, user guide, and verification ledger. Full suite 613 files/4,687 tests, TypeScript, build, Android Gradle, and Flow gate pass; lint 0 errors/84 existing warnings. Paid provider live calls remain key-unavailable; Windows/macOS native launch and Android physical-device generation remain external verification.
- [x] Paper Project 1 managed-print core: content-addressed Paper assets, exact managed fonts and ICC profiles, deterministic editor/export render plans, native CMYK/spot/overprint PDF content, fail-closed licensed PDF/X saving, and truthful Stability PPI handling are complete. The opt-in Fontsource downloader stores pinned vetted open faces without bundling font files; strict output is blocked without an exact authorized face or profile. Final local evidence passed 24 focused files/221 tests, the PDF/X-1a/X-4 production verifier, 589 repository files/4,394 tests, lint, and production build. Task 17 verified the unconfigured-key BYOK block plus live HTTP 200 Fast and Conservative results; frame replacement achieved 300 and 366 PPI respectively. Acrobat/Enfocus, RIP/press, KDP, and InDesign evidence remain external-pending. The commercial entitlement gate, pricing, and verifier were not changed. See `docs/audits/paper-workspace-project1.md`, `docs/audits/paper-stability-live-2026-07-14.md`, and `docs/notes/900-paper-managed-print-core.md`.
- [x] Repair the Windows desktop packaging baseline after the unsupported MSIX target revert: remove invalid/stale MSIX scripts, readiness claims, tests, and release artifact globs while preserving the working x64 NSIS lane.
- [x] Image performance rewrite, pixel-store foundation slice: make `TiledBitmap` configurable, revisioned, transparent-tile-pruning, reference-audited, byte-accounted, and disposable; add opaque snapshots plus `CanvasPixelStore`/`TiledPixelStore` region APIs and parity tests while the live `ImageLayer` backend remains Canvas. See `docs/notes/863-image-performance-pixel-store-foundation.md`.
- [ ] Image performance rewrite, baseline/gate slice: turn the existing brush HUD into structured input/start/release/compositor/worker/effect/overlay/cache/history metrics, add deterministic 4K/8K fixtures, record the Canvas baseline, and wire `gate:image-performance` into the native release gate.
- [ ] Image performance rewrite, compatibility migration slice: move `ImageLayer.bitmap`/`mask` to authoritative `LayerPixelStore` instances, route direct Canvas2D/blob helpers through region APIs, and keep the policy-selected live backend on Canvas until parity gates pass.
- [x] **DOCX import — showcase-fixture overhaul: page breaks, sections, running headers/footers, footnotes, multi-column, faithful pagination (docs/notes/855; `src/lib/paperDocumentFormats.ts`).** Owner's `Slum_Studio_DOCX_Import_Showcase.docx` = "new standard of truth." Now honored: explicit page breaks (`<w:br type=page>`/`pageBreakBefore` → fresh page — fixes "page-two paragraph on page one"); multi-section geometry (`parseDocxSections` landscape/columns/gutter/margins/titlePg + resolved header/footer content w/ inheritance); running headers/footers drawn in the margin band every page with live `{PAGE}`/`{NUMPAGES}` field substitution (simple + complex fldChar-across-runs); footnotes (superscript marker + note at that page's bottom above a rule); multi-column sections as one native `columns:N` frame (CSS columnFill:auto); plus double/dotted underline, `<w:dstrike>`, `<w:caps>`→UPPERCASE, `<w:spacing>`→tracking. **Clipping fix (owner: text "cut off earlier than it should"):** `measureImportedTextBlock` sizes each frame to SHOW all its text (text + 4mm frame padding + border + spacing; DOM-verified zero clip) while advancing the layout cursor tighter so adjacent frames' empty padding overlaps → word-processor density (owner: "some overlap that needs to happen"). Live-verified via Playwright re-import on the dev server: **10 pages matching the source exactly**, page 1 = intro only, headers/footers/footnotes/columns/landscape all render, no clipping. `npm run build` green; +1 importer test (page break + header/footer fields + footnote + 3-section geometry). Still approximated: double/dotted underline & double-strike as distinct styles (model booleans only), endnotes, TOC/content-control contents.
- [x] **Video motion-comics toolset + one universal KDE global menu (see docs/notes/826–833; branch `feature/video-motioncomics-and-globalmenu`, commits `e2a4fd0`→`f368b49`→`1960d32`).** Two bodies of work, both `npm run build` green (tsc -b + vite) and unit-tested (final sweep **315 tests / 20 files**). **Desktop — ONE truly universal global menu (not a Signal-Loom-only applet):** `desktop/kde/signal-loom-globalmenu/` is a C++ fork of stock plasma-workspace v6.6.5 `applets/appmenu` (GPL preserved) with a single ~30-line `appmenumodel.cpp` patch — a focused window that has no native appmenu whose AppId matches signal-loom + the `org.signalloom.PanelMenu` service on the bus imports `(org.signalloom.PanelMenu, /org/signalloom/menus/active)`; everything else (Qt via KWin appmenu protocol, GTK via appmenu-gtk-module, XWayland via registrar) is byte-identical stock behavior. App side: `electron/globalMenu/panelMenu{Model,Service}.cjs` is a native-Wayland D-Bus menu service + a `com.canonical.dbusmenu` twin bound to the active workspace, so per-workspace menus live-switch on window focus; opt-in `SIGNAL_LOOM_ELECTRON_PANEL_MENU=1` deliberately NOT wired to XWayland forcing (GPU stays intact). Live-verified earlier by screenshots (Dolphin menus AND the real app's Flow menus, native Wayland). The superseded no-compiler QML applet stays in-repo as `desktop/kde/signal-loom-panelmenu/`. **Video — motion-comics (plan note 828), delivered in 5 phases:** (1) foundation types in `flow.ts` — bezier tail keyframe fields, 16-mode blend union, `EditorTextTypography`, `EditorVisualTrackKind`; (2A) `videoComicTail.ts` real cubic-bezier speech-bubble tails with independently keyframable anchor/tip/curve, painter + FFmpeg parity; (2B) `videoTextFlow.ts` + `editorClipEffects.ts` Paper-grade wrap/justify/arc text for Video + all 16 Image blend modes for both CSS preview and FFmpeg export; (2C) `gifFrames.ts` pure animated-GIF decode/timing core (GIF disposal compositing + playhead→frame mapping) wired into ingest (`mediaFormatRegistry.ts`/`flowStore.ts`), the Program Monitor preview (playhead-synced frame), and FFmpeg export (`mediaComposition.ts` — animated GIFs emit `-ignore_loop 0` instead of the frame-freezing `-loop 1`, verified empirically against real generated GIFs); (2D) `editorTimelineTrackDrag.ts` cross-track vertical clip dragging (hit-test + lock-respecting trackIndex patch) + overlay track kind so text/comic captions live on their own separate track. Committed on a feature branch off `main` (NOT pushed to the public repo — awaiting owner). Owner live-verify pending for the on-screen behaviors (menu workspace-switch, GIF playback in export, cross-track drag feel).
- [x] **Paper page → Source Library, surfaced on page right-click (see docs/notes/811).** The whole-page flatten-to-library pipeline already existed (`exportPaperPageToSourceLibrary` rasterizes a page through `rasterizeFlattenedPaperPageToPng`, SVG fallback, then `sourceBinStore.addAssetItem` with a deterministic `paper-page:<docId>:<pageId>:<WxH>:<bleed|trim>` sourceKey so re-sends replace in place) and was wired only into the Export Document dropdown ("Source" = this page, "Envelope" = all pages). Owner asked for it as a first-class page action for motion-comic keyframing, so added **"Send This Page to Source Library"** + **"Send All Pages to Source Library"** to the page right-click context menu (`PaperContextMenu`, new `onSendPageToSourceLibrary`/`onSendAllPagesToSourceLibrary` props → the existing `sendPaperPageToSourceLibraryById`/`exportAllPagesToSourceEnvelope`), and clarified the two Export-Document descriptions to name the shared library + Image/Video/Flow. Item shape: `kind:'image'`, PNG data URL, label `<doc title> - Page N`. `npx tsc -b` clean on touched files (only pre-existing VideoWorkspace WIP warnings remain, another agent's file); paper + sourceBin suites green (paperPageFlattenExport, paperVideoAssets, paperWebcomicExport, PaperContextMenu, PaperTopStrip, PaperToolbar).
- [x] **Local AI upscaler: one-click install + app-managed runtime (see docs/notes/802, commit `b09c2c1`).** The "Local CPU AI upscaler" option dead-ended at "runtime is not configured" — the client protocol (`localCpuUpscaler.ts`) had NO runtime implementing it anywhere. Shipped: `ops/local-upscaler/local-upscaler.mjs` (zero-dep HTTP wrapper around realesrgan-ncnn-vulkan; realesrgan-4x/-anime, multi-pass past 4x, bearer auth; NOTE the pinned 20220424 build rejects `-g -1` — device is ncnn auto/GPU, no CPU mode); Electron-managed install/start/stop/status IPC (downloads the pinned Real-ESRGAN v0.2.5.0 platform zip → userData, spawns the bundled script under ELECTRON_RUN_AS_NODE, health-checks `/v1/capabilities`, persists port+token in meta.json, auto-starts with the app; runtime ships via extraResources); an "Install & start local upscaler" CTA in the Print Image Upscale blocked state that also writes `localAiCpuEndpointUrl`/`localAiCpuAuthHeader` live. Verified: real 128→512px Real-ESRGAN upscale through the server in ~6s on the RX 5700 XT; tsc -b clean; 54 focused tests green; installed build carries the feature (asar strings + resources/ops/local-upscaler). REMAINING: owner live-test in the installed app; Android = the in-app NPU directive below.
- [~] **Android in-app NPU upscaling — REALITY CHECK 2026-07-02 (see docs/notes/803, commits `7f47fdb`/`7d3a4e9`; supersedes the note-802 "build a localhost service" plan):** the in-app path ALREADY SHIPPED — `SignalLoomImageUpscalerPlugin` spawns the bundled Local Dream 2.6.4 backend (`--upscaler_mode`, 127.0.0.1:8081, QNN/HTP libs from APK assets, per-chipset model downloads) and the `android-native` route is fully wired in the Image + Paper upscale dialogs. Emulator-verified this session (plugin round-trip + graceful bitmap fallback); FIXED: calls no longer re-pay the 15s backend probe (5-min negative cache; 15.0s → 2ms second call). REMAINING = OWNER ITEMS: (1) **LICENSE DECISION — Local Dream is CC-BY-NC-4.0 and the app is sold**: ask xororz for commercial permission / replace the backend with a permissive realesrgan-ncnn-vulkan(MIT)+ncnn(BSD-3) Android port / strip the runtime from paid builds + scrub the public-repo history (the binaries + Qualcomm-proprietary QNN libs are committed to the public GitHub repo); the new Settings → Open-Source Licenses inventory flags both "under review". (2) Real-NPU verification on the S26 Ultra. SD **generation** in-app stays blocked on the same license decision.
- [x] **FIXED: Source Library cross-workspace wipe on ingest + WnM1 recovery (see docs/notes/801).** `ingestConnectedItems`'s stale-envelope cleanup deleted every library item whose envelope wasn't connected to the *active tab's* sourceBin node — the library is global across flow-workspace tabs, so ingesting in one tab (COVER) mass-deleted all other tabs' ingested assets (WnM1: 73→1 items; the save 5s later persisted it and the capability-registry replace made images blank at the save instant). Fix: cleanup only prunes envelopes that exist in the caller's graph (`graphNodeIds` option, App.tsx passes the active flow's node ids); no context → no pruning. Red-first tests in `sourceBinIngestCleanup.test.ts`. WnM1.sloom recovered: 74 items merged from the auto-backup, 62 node refs repaired, layout preserved, all pixels verified on disk.
- [x] **Linux app-menu launcher now runs an installed packaged build (see docs/notes/800).** `scripts/install-desktop-launcher.sh` reworked: builds (`npm run build` + `electron-builder --linux dir`, skippable via `--no-build`), rsyncs `release/linux-unpacked/` → `~/.local/opt/signal-loom/`, and points `signal-loom.desktop` at the installed binary — no more per-launch rebuild via `node electron/launcher.cjs` / `npm run electron`. New alias `npm run install:linux`. Verified: fresh dist (tsc -b + vite exit 0), installed asar carries today's `index-5vUg1pu3.js` bundle, `desktop-file-validate` clean, installed binary ran 12s headed with no sandbox errors (same Wayland/ANGLE-Vulkan flags as the dev launcher — `main.mjs` self-applies `linux-windowing.cjs`).
- [x] **Provider wiring gap audit — broken endpoints fixed + dormant model features exposed (see docs/notes/794).** Evidence-first sweep of every Flow provider path against live docs + installed SDK types. BROKEN→FIXED: Stability **Search & Recolor** sent `search_prompt` instead of the required `select_prompt` (guaranteed 400); Stability **Replace Background & Relight** treated as sync `image` upload when it's the async `subject_image` + `background_prompt` endpoint polled at `/v2beta/results/{id}` (new `stabilityAsyncResult.ts`, wired into BOTH the Flow node and the editor gen-fill adapter); Stability Erase no longer sends a prompt (prompt-less endpoint); **Gemini TTS always failed in vertex-adc mode (the default)** — now runs on the API key ("requires" wording keeps the no-key error non-retryable for the backoff classifier); **Vertex-authenticated users couldn't pick Google video** (`getConfiguredProviders` now surfaces gemini for video in vertex mode; audio stays key-gated, both regression-tested); `eleven_ttv_v3` (voice-DESIGN model) filtered out of Speech mode. EXPOSED (capability-gated, no new tools): ElevenLabs **voice_settings (stability/similarity/style/speed) + seed** in the Audio node speech mode; OpenAI gpt-image **output_format honored** (was silently ignored, result mime now matches), **quality** select, `moderation:'low'` per the app censorship-off default, and **multi-image edits** (`images.edit` image[] up to 16 → references no longer hard-rejected; first-party-OpenAI-gated so the Atlas OpenAI-compat route keeps its verified single-image contract); HF image **negative_prompt/seed/guidance_scale** pass-through; **Gemini 3.x image_size 1K/2K/4K** select wired through BOTH API-key and Vertex paths (model-guarded); Atlas video negative-prompt field un-hidden; two stale misdirecting error messages rewritten. ALSO repaired pre-existing 0.9.8 breakage the full suite exposed: `colorSwatchListNode`/`loraSpecNode`/`slimgNode` missing from `shared/project-schema.json` (Electron project validation didn't know them) + their `getDefaultNodeTitle`/`getEnvelopeSourceLabel` labels; stale settingsStore byteplus key-shape test; committed-WIP PaperToolbar test asserted an unbuilt 2-column palette — aligned to the shipped 3-column layout (flip `grid-cols-3` in the test if the 2-col design is wanted; owner unresponsive when asked). VERIFIED: `npm run build` (tsc -b + vite) exit 0 w/ fresh dist; full suite **3489/3490** — the 1 fail is `androidSplashSource.test.ts`, an I/O-load timing flake (3.1s baseline vs 5s budget; passes 2× standalone), pre-existing. Live-verifiable with owner keys NOW: Stability recolor/relight, ElevenLabs voice settings + mode lists, Vertex video provider + 2K/4K image size. Unit-verified only (no keys in env): OpenAI gpt-image, HF params, Gemini API-key TTS/imageSize.
- [x] **Release 0.9.7 multi-platform + sloom.studio refresh (see docs/notes/774).** Coordinated `0.9.7` / versionCode 9 wave bumped across `package.json` + `android/app/build.gradle`. Built + staged all four real, checksum-verified installers under the site `downloads/` (Win `SignalLoom-Setup-0.9.7.exe` 150 MB, Linux `SignalLoom-0.9.7.AppImage` 174 MB + `-amd64.deb` 128 MB, Android `.apk` 64 MB; `SHA256SUMS.txt` regenerated, `sha256sum -c` green; old 0.9.1–0.9.4 artifacts pruned) and pointed `downloads/latest.json` at 0.9.7 (drives in-app About / Check-for-Updates). **Website**: new public **`changelog.html`** (newest-first 0.9.7→0.9.0, "Current" badge, shared site.css shell) linked from nav + footer on every page; `index.html` Image-desk hero swapped to `desktop-image-hero.png` + Android section reframed "One project, two screens, live" with the baton + bidirectional diagram; `docs.html` gained a `#cross-device` section (two-way sync + "Working here ⇄ Phone" baton + presence, honestly framed as snapshot-on-handoff), brush "Modifier gestures" (Shift/Alt), "encrypted at rest" + "Encrypted settings backup", and `paper-comic-spread.png` in the Paper section; `thanks.html` desktop cards bumped 0.9.2→0.9.7; `sitemap.xml` adds changelog + lastmod 2026-06-27; `verify-site.mjs` now validates `changelog.html` (gate **passes: 5 pages, 18 assets**). Two real screenshots curated from ~/Pictures. **Deployed** to the Hostinger VPS via gated `deploy.sh root@2.24.206.11 --go` (dry-run reviewed first). **Play**: 0.9.7 AAB (versionCode 9) confirmed already on the **internal testing** track (re-upload returns the expected "version code 9 already used"). **itch.io desktop DONE**: the existing `sloomstudio/signal-loom` project was converted Android→desktop (owner-confirmed), so 0.9.7 was pushed to all three desktop channels (`windows`/`linux`/`linux-deb`) via butler — `butler status` confirms all three at 0.9.7. Closes #37.
- [x] Brush Shift straight-lines + tool modifier/hotkey audit + encrypted settings backup (see docs/notes/757). (A) Photoshop-style **Shift straight-lines** across the whole brush family via a shared `brushLineAnchor.ts` registry (keyed by toolKey+docId; records the stroke end on pointerUp, returns it as the line start on a Shift+down): brush, eraser, clone stamp, spot-heal/healing, dodge, burn, sponge (saturate/desaturate), blur, sharpen, smudge — segment/stamp/heal/tone/sponge families paint `*Between(lineStart,point)`; controller family (blur/sharpen/smudge) does `anchor(lineStart)+moveTo(point)+previewInto` on the down event (stroke iface gained `lastDocPoint`). (B) **Alt-source**: clone stamp already set its sample origin on Alt+click; **spot-heal now doubles as the healing brush** — Alt+click sets `healSourcePoint`/resets `healAlignedOffset`, and `healAt` samples from source (aligned: `target+offset`; non-aligned: `source+(target−strokeStart)`) while preserving the original auto spot-heal when no source is set. Audited the full modifier matrix + verified every image tool has a non-colliding keyboard accelerator (B/E/S/J/R/U/O + Shift/Alt variants) gated against typing, and that the per-workspace gamepad binding model routes the same command bus (no keyboard-only or pad-only tool). (C) Opt-in **encrypted settings backup** (`settingsBackup.ts`): export/import a portable blob of API keys + prefs sealed with a **passphrase** (PBKDF2-SHA256 250k → AES-GCM-256, fresh salt+IV, self-describing versioned JSON envelope) — deliberately NOT the device-bound at-rest cipher, so it survives reinstalls/machine moves; local + user-initiated, never transmitted. Store orchestrates collect→encrypt / decrypt→sanitize-merge (mirrors the persist `merge`); SettingsModal gets a Backup section (passphrase+confirm, file download + clipboard fallback, file/paste import) gated on `settingsBackupSupported`. `npm run build` green (tsc -b + vite); `npx vitest run` 3281 passed (brush-anchor 9/9, settings backup 8/8, settings/store 220/220) — the sole failing file is the user's uncommitted PaperToolbar WIP (out of scope). Tasks #43–#47 closed.
- [x] Distinct/glanceable tool icons per workspace — no two tools share a glyph (see docs/notes/758, task #48). Image: `marquee`→`SquareDashed`, `backgroundEraser`→`ImageOff`, `magicEraser`→`Sparkles`, `smudgeBrush`→`Wind` (resolves the square/eraser/brush collisions). Paper: `panel`→`Frame`, `pentagon`→`Pentagon`, `hexagon`→`Hexagon`, `shape`(free polygon)→`Waypoints`, **`thought`→`Cloud` (was sharing the speech bubble's `MessageCircle`)**, `gutterKnife`→`Slice` (distinct from Cut's `Scissors`). Locked with a per-toolbar regression test that extracts each tool button's sorted `lucide-*` class signature and asserts all signatures are distinct (`ImageEditorToolbar.test.tsx`, `PaperToolbar.test.tsx`). `npm run build` green.
- [x] Decouple the KDE6 Plasma global menu from GPU acceleration (see docs/notes/759, task #49). The coupling was never fundamental: the menu needs an X11 window (KDE's AppMenu registrar is XID-keyed), and forcing XWayland *used* to kill the GPU only because the old ANGLE `gl-egl` backend couldn't get an EGL config under XWayland → SwiftShader → ~1fps. The current **ANGLE-Vulkan** backend gets a real GPU surface under XWayland too (verified: 2d_canvas + raster + vulkan all on with `--ozone-platform=x11`), so the *only* remaining gap was that Chromium never exports a DBusMenu. Closed it by exporting `com.canonical.dbusmenu` as a **pure D-Bus service from the Electron main process** (which does no GPU work — GPU is a separate child process), so menu and GPU are genuinely independent. New `electron/globalMenu/{dbusMenuModel,dbusMenuExporter,globalMenuController,x11WindowId}.cjs` (model is pure, fed by the SAME `shared/workspaceMenus.json` as the in-window menu; roles→synthetic `role:*` commands; every D-Bus call guarded → in-window menu is the fallback). Wired into `main.mjs`: register each workspace window on `ready-to-show`, unregister on close, `role:*` handled natively, `refreshShortcuts` on shortcut change, `stop()` on quit. **Opt-in** via `SIGNAL_LOOM_ELECTRON_GLOBAL_MENU=1` (one flag both forces XWayland in `linux-windowing.cjs` and enables the controller) — default stays native-Wayland+GPU so the hard-won default isn't reversed on unverified ground. **Safety-critical**: `resolveX11WindowId` returns an XID only on confident title/managed-window match, NEVER a foreign window (would hijack another app's global menu) — dedicated anti-hijack regression test. Removed the dead `appmenu-gtk-module` env hack (no-op for Chromium menus). **Verified on this machine**: exporter serves the correct 9 Image menus over GetLayout; `registrar.RegisterWindow`+`GetMenuForWindow` round-trips to our object (`registrarMatchesUs`); ANGLE-Vulkan GPU stays accelerated under XWayland; 27 pure-logic unit tests + full build/vitest green. **Needs the user's eyes** (this automation session keeps Electron on the Wayland backend so no real XID is obtainable here — an env artifact, not a code defect): the menu visibly appearing in the Plasma panel — run `SIGNAL_LOOM_ELECTRON_GLOBAL_MENU=1` + the kept `electron/diagnostics/global-menu-probe.cjs`. This supersedes the notes/712–713 "global menu and GPU are mutually exclusive" verdict: ANGLE-Vulkan-on-XWayland breaks that trade-off. NEXT: user on-device confirmation of the visible Plasma menu (they said they'll bring the phone back for on-device testing).
- [x] Atlas image request-shape fix — Flow image nodes + generative-fill gating reconciled with the live Atlas API (see docs/notes/732 + 733). ROOT CAUSE (visually confirmed in 732, not inferred): Atlas names the source-image input differently per model (`images` array on 31 models, singular `image` on 12, `image_urls` on 2) and **silently ignores the wrong field** — and there is **no `reference_images` field on any model** (references must ride in the source array). The app hardcoded singular `body.image` + `body.mask_image` + `body.reference_images`, so on the 31 array-models the source was dropped (an "edit" degraded to a fresh text-to-image) and on **every** model the references were dropped — making comic-panel character consistency impossible. FIX: added a per-model resolver in `src/lib/imageEditorAi/atlasNativeImage.ts` (`resolveAtlasSourceField`, `applyAtlasImageInputs` → `[source, ...references]` under the real field, `atlasModelSupportsMask`) and wired it into **both** the Flow native-Atlas builder (`flowExecution.ts`) and the editor generative-fill adapter (`runAtlasNativeGenerativeFill`). Also made capabilities honest: the two configured Qwen edit models have no mask field in their schema but were flagged `maskInpaint:true` with a `mask` control + `mask-inpaint` op — corrected to `maskInpaint:false`, dropped the `mask` control + op (`imageProviderCapabilities.ts`), so Flow's `supportsTrueMaskInpaint` gate now returns a clear error instead of silently dropping a mask. Generative fill was already correct for non-mask models (it crops to the selection and composites back through a layer mask — `GenerativeFillArtifacts`/`GenerativeFillLayer`); true-mask routing (gpt-image bare ids → OpenAI-compatible `images.edit`, alpha mask) is unchanged. The multi-reference node already exists (`IMAGE_REFERENCE_HANDLES` renders up to `min(14, maxReferenceImages)` handles) — it was broken only by the field mapping, so no new node was needed. `npm run build` green; new/updated tests assert `images:[source,ref1,ref2]` for the array path and singular `image` + no `mask_image`/`images`/`reference_images` for qwen edit; all touched suites green (flowExecutionImageProviders, imageProviderCapabilities, imageEditorOperations, atlasNativeImage, maskConventions, providerCatalog, GenerativeFillBar + broader flow/image/node surface). Tasks #40/#41/#42 closed.
- [x] Website redesign — sloom.studio rebuilt as a crafted "instrument" site (commits `66180c0`→`c18dad0`): the prior generic AI-SaaS template (rejected by the owner as "the same generic AI-built website") was thrown out and the home page rebuilt to look like the app — dark instrument panels, dot-grid canvas, app-window "monitors" with live readouts, an animated node-graph hero canvas, an **interactive Flow/Image/Paper/Video workstation tab-switcher** (verified live: clicking a tab swaps the screenshot + accent colour + capability chips), a provider "patch bay", a phone→LAN→PC diagram, and a receipt-style **$9.99** license block (strike $19.99 regular). The system is carried across **Docs/Examples/Privacy** via a rewritten shared `assets/site.css` (instrument tokens + legacy-var aliases so each inner page's inline styles keep resolving — no content rewrites). Fixed a real redesign bug: docs/examples/privacy linked `index.html#pricing` but the redesign renamed that section to `#license` — all 8 broken anchors repointed + the nav CTA unified to "Get it — $9.99". `verify-site.mjs` rewritten as a real deploy gate (home-page markers, shared shell on every inner page, every asset reference resolves, no `#pricing`, privacy policy present) and **passes (4 pages, 13 assets)**. Added `deploy.sh` (gated rsync, dry-run by default, post-deploy privacy-URL 200 check). Deploy itself is owner-gated (VPN creds). Remaining: (superseded by the 2026-07 flow-graph redesign + workspace replay sims, notes 796–797)
- [~] Shared state: phone as data authority (task #20) — **Phases A + B SHIPPED (read-only mirror + live source-library sync); C still pending (see docs/notes/724 design, 755 Phase A, 756 Phase B + HTTP security).** Root cause of empty served sessions: the LAN host serves static assets only with **no Capacitor bridge**, so a desktop browser at `http://<phone-ip>:8723` is a fresh web origin and all persistence is **origin-scoped** (projects = IndexedDB `flow-project-library`, source bin = `localStorage`, asset blobs = IndexedDB `flow-local-assets`) — none of it the phone's. Design **reuses the transport-agnostic versioned reconciliation engine** (`SourceLibraryNativeEvent` / `applySourceLibraryNativeChange` / version-gap repair from `sourceLibraryNativeSync.ts`, shipped for the desktop multi-window clobber fix, note 719) with the **phone as authority** and an HTTP/SSE `/__loom/api/*` transport. **Phase A (done, note 755):** read-only GET API `/__loom/api/{health,projects,projects/:id,source-library,asset/:id}` relayed to the WebView's live stores (`LanAppServer.java`, `androidLanServer.ts`); a boot-probe served-client detector + source-library seeder (`remoteHostClient.ts`, wired into `main.tsx` before render); `projectLibrary`/`assetStore` reads routed to the phone with served-session writes made no-ops (read-only, no clobber); host source-library snapshot handler in `sourceBinStore`; a "viewing your phone's library — read-only" banner. Build green; `remoteHostClient.test.ts` + `androidLanServer.test.ts` 8 passed (incl. sloom.studio HTML false-positive guard). **Security without HTTPS (done, note 756):** dropped self-signed TLS (browser warnings, no access control) for plain HTTP + a **pairing PIN → per-session bearer token** + **same-origin-only CORS** on `/__loom/api/*` (`/health`+`/pair` open; everything else requires the token; 5-strike 30s lockout; PIN shown on phone, entered once on desktop via `RemoteHostBanner`). Removed the `androidLanServerSecure` setting. **Phase B (done, note 756):** live source-library sync via **long-poll** (the relay is request→single-response, so SSE is impossible) — `lanHostService.ts` versioned change log + `GET /source-library/events?since=N` (parked waiter) + `POST /source-library/mutate` + `PUT /asset/:id`; client subscriber applies events via `setState` (no echo loop) and pushes desktop-origin changes back (size-capped asset upload then mutate). Reuses `applySourceLibraryNativeChange` verbatim. Build green; `lanHostService.test.ts` + extended `remoteHostClient.test.ts` + `androidLanServer.test.ts` = 20 passed. **NOT on-device-verified yet** (Java native layer compiles only in an APK build, not run this session). **Phase C** (still pending) = bidirectional project writes (autosave + version/repair conflict handling) — adds a second live writer to a real project over a flaky LAN (data-safety-critical; needs its own test suite + on-device soak). **Phase C is being built as a workspace-AGNOSTIC unified op-sync rather than per-workspace plumbing** (design note 764): **#50 DONE (note 765)** = the generic channel core — one monotonic channel-tagged op-log + the `ProjectSyncChannel` registry in `projectSyncService.ts`, with `lanHostService` refactored to be channel `'source-library'` over it (zero behavior change, 29 tests). **#51 IMPLEMENTED, NOT on-device-verified (note 766)** = the **Flow graph channel** + the generic `/project/:channel/{snapshot,events,mutate}` transport + the generic served-client seed/subscribe/publish loop (`projectSyncClient.ts`) + `flowSyncChannel.ts` (passive zustand emit subscription with an `applyingRemote` echo guard, a `canEmit` phone-authority gate so a served client can't clobber the phone on connect, and a mid-drag skip) + `flowStore.applyRemoteFlowGraphChange`. 65 sync tests + full suite 3351 pass (1 unrelated pre-existing PaperToolbar WIP fail), `npm run build` green. **Stays in-progress until a paired phone confirms a live Flow edit crosses screens** (no phone in this env). **#52 IMPLEMENTED, NOT on-device-verified (note 767)** = the **Paper workspace channel** — a new pure op model (`paperDocumentNativeSync.ts`: frame add/move/update/remove ops + a structural-fingerprint diff that collapses any page/guide/setup change to a snapshot) + `paperStore.applyRemotePaperDocumentChange` (+ `reconcilePaperSelection` for when a remote snapshot deletes the selected page/frame) + `paperSyncChannel.ts`, reusing the **exact same transport + client loop from #51 with ZERO edits** (proves the generic core: a workspace is now additive). One deliberate difference from Flow: Paper has no store-visible drag flag, so the emit is **coalesced** (90 ms trailing debounce, 220 ms max-wait) to collapse a frame-drag's per-pointer-move store writes instead of a `node.dragging` skip. 25 new Paper tests (65 combined across the paper+flow+core sync suites); `npm run build` (tsc -b + vite) green **after the gate caught 7 real type errors** the green vitest run could not (test drafts missing `widthMm`/`heightMm`, a stale `axis` guide literal — fixed). **Stays in-progress until a paired phone confirms a live Paper edit crosses screens.** **#53 IMPLEMENTED, NOT on-device-verified (notes 768 design + 772 implementation)** = the **Image workspace channel**, the first channel whose payload can't fit in the op stream (layer pixels are a live multi-MB `OffscreenCanvas`, non-serializable). The JSON op stream carries metadata + a pixel **pointer** (`{layerId, bitmapVersion, hasBitmap, hasMask}`), and pixels travel **out-of-band, content-addressed by `layerId@bitmapVersion`** (+ a `:mask` sibling) so a receiver decodes a version at most once (makes even a full snapshot cheap) — reusing the `defaultImageLayerPixelCodec` base64-PNG codec. All three increments landed + unit-tested: (1) pure canvas-free op core `imageDocumentNativeSync.ts` (18 tests) + the live-store seam `imageEditorStore.applyRemoteImageDocumentChange` + `applyRemoteLayerPixels` (preserves surviving live bitmaps by id, atomic version+pixel flip; 7 seam tests); (2) the out-of-band binary transport — host LRU store in `projectSyncService.ts`, `GET`/`PUT /project/:channel/asset/:assetId` routes in `androidLanServer.ts`, and the role-branched client façade `projectSyncAssets.ts`; (3) `imageSyncChannel.ts` — passive coalesced emit that **PUTs bytes before publishing the pointer op**, an inbound `applyRemote` that fetches+decodes+flips with a per-layer version dedupe, and a `snapshot` that pre-publishes every layer's bytes so a fresh seed isn't blank; echo-guard + `canEmit` authority gate identical to Flow/Paper; codec+transport injectable so the round-trip tests run canvas-free (4 channel tests). `npm run build` green; full unified-sync suite green (8 files / 69 tests). **FLAKE FIX (note 773):** the full 3444-test suite was intermittently red (~1 run in 2) with a TDZ `ReferenceError: Cannot access 'applyingRemote' before initialization` — root-caused to the floating `void import('../lib/imageSyncChannel').then(initialize)` self-bootstrap at the bottom of `imageEditorStore` (and the identical flow/paper ones): 35+ test files import the store, each firing an unawaited channel-init side-effect that across vitest's multi-file worker resolves into a half-evaluated module context. Fixed by guarding all three store bootstraps with `import.meta.env?.MODE !== 'test'` (production imports the store once and settles fine; the channel tests init explicitly). 3-file repro now 10/10 green; full suite deterministically down to only the pre-existing uncommitted `PaperToolbar.test.tsx` WIP fail. Source-library stays on its bespoke loop until on-device verification is possible. **Stays in-progress until a paired phone confirms a live Image edit (brush stroke / layer op) crosses screens + a fresh connect seeds a non-blank canvas** (no phone in this env). Next: #54 same-project-open + drop the "read-only" banner (DONE, note 771). **SERVED FLOW IMAGES FIX (note 769, built + tests green + sideloaded to phone `R3GL40ABQXM`, awaiting on-device confirm):** movement synced but image nodes were blank in the served browser because both the import path (`sourceAssetUrl`, stripped by the sync projection) and the generate path (`data.result` = phone-local source-bin URL) point at URLs the browser can't fetch. Fix resolves bytes out-of-band by stable id (the same `/source-asset/:id` / `/asset/:id` the Source Library uses): imported nodes via `resolveImportedAssetDataUrl`; generated nodes by threading `sourceBinItemId` onto each `NodeResultAttempt` (flow.ts + resultHistory.ts + flowNodeResultRestore.ts + 3 flowStore generation call sites) and resolving from the selected attempt in `ImageNode` + per-attempt in `AttemptHistory`. Also this phase: moved the LAN **server toggle** out of the deep menu onto the top-bar phone drawer (`TopNavbar`, gated by `isAndroidLanServerAvailable()`), and made the Settings **provider help cards collapsible** (collapsed by default; `ImageProviderHelpCard` in `SettingsModal`).
- [x] Collapsible Image and Paper floating tools panel — Add a toggle button on the handle of the tool panels (Paper and Image) that collapses them to just the color pickers (foreground/background) and expands them back to full tool dialogs, making them easier to get out of the way on mobile screens without closing or removing them.
- [x] Android integration pass — AI providers + file open/save + file-manager intents (see docs/notes/718): the release-blocking on-device breakages (AI models returning API errors / failing to fetch images; `.slimg` failing to open with *"PK is not valid JSON"*; generative fill billing then 403 on download) plus a full detailed sweep. Root cause cluster: the Android WebView origin is `https://localhost` and provider APIs send no CORS headers, and several paths assumed the Electron bridge and silently no-op'd. Fixes: enable `CapacitorHttp` so provider `fetch` routes through native HTTP (no CORS — verified `api.openai.com` returns a real 401, not a CORS `TypeError`); `readBinaryImageResponseBlob` content-detects + decodes the **base64-wrapped binary POST responses** the patched fetch returns (wired into stability/hf/generic/localOpen adapters); `fetchProviderResultBlob` downloads signed-CDN results via the **direct, non-proxied** native path (kills the 403); `classifyOpenedFile` routes opens by **magic bytes** (ZIP `PK` → image/paper, JSON → project) so OS pickers that ignore `accept` don't `JSON.parse` a ZIP; Image/Paper **Save As** now streams `.slimg`/`.slppr` to `Documents` via `Filesystem.writeFile` and **Open** uses the content-aware picker; and **file-manager "Open with"** works via `@capacitor/app` ACTION_VIEW handling + `AndroidManifest` VIEW intent-filters (pathPattern, content/file schemes) reading `content://` bytes through `convertFileSrc` + the unpatched `CapacitorWebFetch` (filesystem plugin rejects content URIs). Vertex/scratch-folder (desktop-bridge-only) degrade with clear "requires desktop app" errors. Verified end-to-end on the `S26_Ultra_API36` emulator via CDP-over-ADB: intent open (cold + warm) loads the Neon Grimoire demo (42 nodes); manifest resolves 6/6 (3 ext × file+content); save lands on device; CORS reaches the server. Build green (tsc -b + vite), full suite **3149 tests / 452 files**, eslint 0; signed AAB rebuilt → `release/play-store/SignalLoom-1.0.0.aab`. Follow-up: ACTION_SEND share-to (needs a native EXTRA_STREAM shim); re-verify against a real file manager on physical hardware.
- [x] Krita-feel sketching at 4K — dirty-rect compositing (see docs/notes/715): the renderer recomposited the WHOLE 4096² document every frame during a stroke (15.3ms full vs 0.3ms for a dab region — 50-200x). Now the paint tools report their touched region (`brushTool.dabsDocRect` → `env.markDirty` → `CompositeRenderer.markStrokeDirty`) and the renderer keeps a persistent scratch "projection", recompositing only the accumulated dirty rect each frame (clip + clear + backdrop + `compositeLayerRangeInto`), full-composite on stroke start, with a `rangeIsDirtyRectSafe` gate (effects/adjustments/groups bleed → full fallback). Verified pixel-identical (400 dabs vs full composite = 0 mismatched bytes) and 50-200x cheaper. tsc clean, lint 0 errors, build green. Follow-up: extend markDirty to the layer-mask/retouch paint paths.
- [x] (superseded — see docs/notes/714, 727; GPU default later reversed to ON on native Wayland; dirty-rect compositing shipped)
- [x] Stylus latency — low-latency canvas + sync present + the zero-copy/global-menu verdict (see docs/notes/713): after 712 turned the GPU on, pen→screen lag persisted vs Krita (4K, blank canvas). Traced the present path: the visible composite canvas used a plain `getContext('2d')` presented through the DOM compositor's frame queue (2–3 frames), and the dab waited a further rAF. Fixes (both work on XWayland → **keep the KDE global menu**): (1) `desynchronized: true` low-latency 2D context so Chromium presents the canvas straight to screen; (2) `CompositeRenderer.renderNow()` + `dispatcher.onMove` presents the painted dab synchronously in the same frame as the input (gated on `isPaintingStroke` + `STROKE_PAINT_TOOLS`, one sub-2ms fast composite per event). Verdict on "true zero-copy + global menu" (user wants both): proven **mutually exclusive for Electron** three ways — XWayland's only GPU-compositing backend (GLX-ANGLE) segfaults (139) under every stabilizer; native Wayland needs `org_kde_kwin_appmenu`/`gtk_shell1` which Chromium's Ozone backend doesn't speak; the AppMenu.Registrar is X11-window-id keyed. Krita gets both only as a native Qt/Wayland app. Resolution: make XWayland latency native-class (the two fixes above) so zero-copy isn't needed, keeping the menu. tsc clean, dispatcher+renderer tests green, build green. Next: user live-tests; if still short, native-Wayland zero-copy is the only remaining gain (loses global menu — a user decision).
- [x] Desktop Linux GPU acceleration — the real brush-latency fix (see docs/notes/712): the Image workspace felt ~10–15% of Krita on desktop despite notes 707/711 driving the composite to sub-2 ms, because those numbers were measured in the **browser dev server (GPU on)**, not the Electron app — which `main.mjs` ran with `--disable-gpu` on Linux (a note-104 stability workaround for a KDE GPU-process crash). Every canvas blit/composite was on SwiftShader (CPU); the GPU-accelerated Android WebView felt better than the strong AMD desktop. A direct Electron probe on this machine confirmed the default ANGLE backend **segfaults** (exit 139), but pinning ANGLE to the native GL driver via EGL (`use-gl=angle` + `use-angle=gl-egl` + `disable-gpu-sandbox`) brings up a healthy GPU process with hardware-accelerated 2D canvas + WebGL. Fix: `electron/linux-windowing.cjs` now owns the GPU policy (`getLinuxGpuSwitches`/`resolveLinuxGpuPolicy`/`applyLinuxGpuCommandLine`, pure + tested); `main.mjs` defaults the GPU **on** with an automatic crash→software-fallback sentinel (6 h self-heal) so stability-first is preserved. Opt-outs: `SIGNAL_LOOM_ELECTRON_DISABLE_GPU=1`; opt-in for true zero-copy compositing: `SIGNAL_LOOM_ELECTRON_NATIVE_WAYLAND=1` (trades the KDE global menu). Electron suites 37/37, lint 0 errors, build green, real `main.mjs` boots clean with no GPU crash. Follow-up: re-measure a live stylus stroke in the packaged app and tune the X11-readback vs native-Wayland default.
- [x] Multi-select linked transform (#11; see docs/notes/710): layers can now be multi-selected (Ctrl/Cmd-click toggle, Shift-click range, plain click collapses) and move together as one group via the Move tool — not just the explicit `linkGroupId` pairing. New pure `ImageGroupTransform.ts` (resolve/toggle/range/bounds/translate/rotate, 10 tests); `ImageDocument.selectedLayerIds` + store `setSelectedLayers`/`toggleLayerSelection` (and `setActiveLayer` collapses); layers-panel row `selected` highlight + modifier-click wiring; `moveTool.resolveMoveToolLinkedOrigins` unions the ad-hoc selection with link-group members so a drag translates all selected layers by the same delta (single-selection path + snap/parity metadata unchanged; undo pushes a whole-layers `layerOp` for >1 layer). Honest descriptor flip: added `multi-select-linked-movement` supported state (translation only; `linked-layer-transform-propagation` for rotate/scale stays unsupported). Full suite 3040 (435 files), tsc clean, lint 0 errors, build green. Follow-up: extend the transform overlay (rotate/scale/skew) to operate on the whole multi-selection as a rigid group.
- [x] Brush-preview cursor + Ctrl→eyedropper modifier (see docs/notes/698): `BrushCursorOverlay` replaces the system cursor over the canvas with a Photoshop/GIMP-style outline that follows the pointer and shows brush size/shape (round/square, roundness, angle) scaled to zoom; system cursor hidden (`cursor-none`); pen-hover works on Android, touch unaffected. Holding Ctrl over a paint tool samples colour (eyedropper) without switching tools, with the cursor swapping to a sampler crosshair (`shouldUseEyedropperOverride` pure+tested; dispatcher routes the Ctrl-held stroke). tsc clean, lint 0 errors, full suite 2967 (+3); verified live on desktop; APK rebuilt + installed. Follow-up: hardness ring + brush-tip pattern preview inside the outline.
- [x] Atlas Cloud video models (see docs/notes/697): root cause of "Atlas configured but not working" was that Atlas was never a video-capable provider. Pulled the real video slugs live from the user's account via the on-device CDP bridge (no guessing), added Atlas to the video capability (Veo 3.1, Seedance 2.0, Wan 2.7, Grok-imagine-video) + `executeAtlasVideoNode` (POST `/model/generateVideo` per Atlas's published schema, start-frame upload for image-to-video, prediction poll, CDN-CORS-safe materialize). tsc clean, lint 0 errors, full suite 2964 (+3), APK rebuilt + installed; slug confirmed in the shipped bundle and the app runs cleanly on-device. A real paid generation (~$0.20/clip) is the only end-to-end step left — trigger from the video node. Follow-up: refresh the stale Atlas IMAGE slugs (`atlascloud/qwen-image/edit*` → live `qwen/qwen-image-2.0/*`, add `google/nano-banana-2/*`).
- [x] On-device pass 2026-06-16 (see docs/notes/696, phone connected via ADB): added `ops/android-smoke/cdp-eval.mjs` (CDP-over-ADB DOM bridge into the device WebView); diagnosed + fixed the Flow Source Bin/Bookmarks **full-height rails → content-height compact handles** (root cause was a runtime inline `top:` + class `bottom-0`; now only stretches when a panel is expanded) — verified on a physical Galaxy (633px→168/170px); rebuilt + installed the debug APK and QA'd launch/all-4-workspaces/nodes/composition-create. tsc clean, lint 0 errors, full suite 2961.
- [ ] BLOCKED on owner resources: ALOS Desktop-profile emulator run (needs Android Studio); Android physical smoke-test QA (needs a connected device); mobile live-DOM traces for Flow rails + Video timeline cramming (needs on-device trace / a composition with clips); one real paid Atlas video generation; remaining Image parity atoms note (each a large feature slice). (4-panel comic build QA is superseded — the owner's real Weights & Measures comic production, 2026-07-02, IS that QA, passed on a real project.)
- [x] Backlog batch 2026-06-16 (see docs/notes/695): (1) mobile expanded app-menu now dismisses only on taps OUTSIDE the drawer (added `mobileMenuRef` to the drawer panel in shared `TopNavbar`); (2) ALOS/ChromeOS surface detection locked as desktop-class via regression test (never the phone shell); (3) **Cintiq pen-blob fix** — `readBrushPressure` no longer returns full size for pen `pressure<=0` (contact/release), so strokes taper instead of blobbing; (4) **Crop preset management** — save/apply/delete custom aspect-ratio presets (`cropPresets.ts` + `custom:${number}` preset + persisted `customCropPresets`), Crop parity burned down 62→66. tsc clean, lint 0 errors, full suite 2961, dist rebuilt.
- [x] Doodle Node (see docs/notes/694, commit `ffe66f5`): new Flow node (Inputs & Data) with a blue-pencil sketch canvas + aspect-ratio selector; thumbnail opens an expanded canvas (full-screen on phones, pointer events for mouse/touch/pen + S Pen/Wacom, draw/erase/move modes). Outputs an asset package = sketch image (reference) + description (own text box, or an attached Text node which overrides it); the Image node consumes the sketch as a reference image and the description as its prompt via `collectImageInputFromSource` + `evaluateNodeSignal`. Registered across node-types/catalog/theme/label-switches + the Electron schema mirror. tsc clean, lint 0 errors, full suite 2953, dist rebuilt; live-confirmed registered/searchable. Follow-ups (non-blocking): in-canvas pinch-zoom/pan + pen-pressure width.
- [x] Image fix/declutter batch 2026-06-16 (commits `e004c0a`→ HEAD): (1) clipboard "no image" fixed via native Electron `clipboard.readImage()` bridge + diagnostics (needs Electron restart to confirm); (2) drop a Source Bin image onto an open document inserts it as a canvas-fitted, undoable layer (`imageLayerInsert.ts`); (3) Clone Stamp Alt-click source now prompted on-canvas (tool functionally works — was a discoverability gap; Spot Heal needs no source and works); (4) Filters/Effects add-grids collapsed into compact "Add…" dropdowns; (5) Properties panel decluttered — leads with "Tool Options", document sections (histogram/color-proof/bit-depth/artboards/geometry/comic) behind a collapsed "Document Properties" disclosure. tsc clean, lint 0 errors, full suite 2945, dist rebuilt.
- [x] Per-workspace application menus — Phase 2 CONTINUED — COMPLETE 2026-07-02 (commit `0763006`): the dialog library (`workspaceDialogStore` + `DockableDialog`) + Image > Adjustments dialog landed earlier (`5a88764`, notes/693); Image and Paper already had Window > Panels toggles for every dockable panel; the remaining gap was VIDEO — its Window menu now toggles all nine panels (source bin, source/program monitors, inspector, timeline, sequence settings, export presets, parity checklist, diagnostics) + Reset Video Panels, same hide/restore-docked contract as Image, with a parity test pinning one toggle per panel and the native-vs-integrated menu equivalence test covering Electron. Per-dialog mobile/DeX polish continues under the mobile-exposure workstream.
- [x] Per-workspace application menus — Phase 2 foundation + Adjustments (see docs/notes/693, commit `5a88764`): reusable `workspaceDialogStore` (per-workspace open-dialog state; tested) + `ImageAdjustmentsDialog` reusing `AdjustmentLayerControls` via undoable `imageAdjustmentActions`; new `image:adjust-*` commands in `NATIVE_MENU_COMMANDS` + Image>Adjustments menu, forwarded through App's renderer bus. tsc clean, lint 0 errors, 2938/2938 tests, build green, dist rebuilt; live menu→dialog confirmed on :5174.
- [x] Per-workspace application menus — Phase 1 (wiring; see docs/notes/692, commits `d929463`→`2ba2b9d`): single shared `shared/workspaceMenus.json` source of truth consumed by BOTH the native Electron menu (`electron/menu.cjs`/`main.mjs`, per-window menu following each window's workspace, focused window drives the macOS bar + KDE Plasma global menu) and the integrated renderer menu (`src/lib/appMenuModel.ts`/`TopNavbar.tsx`). Each workspace shows only its own idiomatic bar (Image=Photoshop, Paper=InDesign, Video=Premiere, Flow=its own); the Window menu carries the four cross-app launchers in every workspace; no greyed other-workspace menus. New `workspaceMenus.test.ts` parity guard + rewritten `electronMenu`/`appMenuModel`/`appSmoke` tests (31/31 menu tests pass), `tsc` clean, menu files lint-clean, `npm run build` green, dist rebuilt. Native bar / KDE global menu still needs the user's real-Electron restart check.
- [ ] Image live checklist atom burn-down batch: target the open red-X atoms reported by `/status.json` for Android parity, smart/source-linked layers, raster shapes, PSD/file interop, XCF/export, and adjacent low-progress Image rows; add red-first tests for exact checklist atoms, update only implementation-backed descriptors, verify focused tests, TypeScript, Android sync/install where practical, and confirm `/status.json` progress changes. **2026-07-02 batch (`b2c1ba7`):** raster-shapes 'overlapping custom shape booleans' flipped via a real Greiner-Hormann polygon clipper (ImagePolygonBooleanClip, pure canvas-free module, 13 geometry tests + 100 suite tests; snapshot 83.3%→85%); note the remaining open atoms across rows are each multi-session feature slices (refine workspace, press-ready separations, live boolean stacks, full PSD semantics), not quick atoms.
- [x] Vertex AI in-app ADC authentication and execution: built-in credential-file choose/paste, authorized-user and service-account token mint/refresh, desktop external/impersonated credential support through Google Auth, standard ADC discovery on Windows/macOS/Linux, project/region/quota selection, Resource Manager project discovery, encrypted-at-rest settings, and direct Android text/image/video execution. JSON import requires no terminal or Cloud SDK; the optional desktop browser-sign-in compatibility button uses an installed Cloud SDK. Live Linux ADC token smoke, cross-platform path/token tests, Android Gradle, and full repository verification pass. See `docs/vertex-authentication.md` and `docs/notes/901-flow-node-provider-vertex-audit.md`.
- [ ] Optional Vertex follow-up: mobile-paired desktop proxy/credential sync after the pairing layer exists, native Google OAuth on mobile after a registered restricted OAuth client is available, physical Windows/macOS launch smoke, and physical-Android generation smoke. External-account/impersonated ADC remains desktop-only until a native Android subject-token broker is implemented.
- [x] (duplicate — shipped, see docs/notes/697 entry above; remaining follow-up = Atlas image slug refresh, handled 2026-07-02)
- [ ] Aluminum OS (ALOS) readiness: Google's Android+ChromeOS desktop OS (mandatory in Android 17, ~Fall 2026); Signal Loom's Android app + tablet/DeX≈desktop surface model fits directly. Test via Android Studio's new "Desktop" device-profile emulator (added 2026). Lower priority until ALOS tooling stabilizes; verify the app runs in the Desktop emulator profile and the surface detection treats it as desktop-class. (Queued — forward-looking.)
- [ ] Android physical smoke-test QA (see docs/notes/677): reusable harness `ops/android-smoke/smoke.sh` built; verified launch/tabs/Flow/long-press-context-menu/app-menu on device. NEXT: fix expanded mobile app-menu tap-outside dismissal (stacking-context-careful, shared TopNavbar), confirm New Project clears the flow graph (bear = shared Source Library, persists by design — re-test), then drive the 4-panel comic build node-by-node with the harness (needs live AI generation). S Pen + Wacom Cintiq confirmed working in Image (requirement, not a bug).
- [ ] Mobile UI polish + Photoshop-style panel scrolling (see docs/notes/674): DONE so far — Paper tools palette can move past the left source-bin handle (removed the 40px left gutter, symmetric with the right; test updated), and the redundant Tools/Panels/Assets buttons are hidden on phone in the Image controls bar (edge handles cover them); built + sideloaded. REMAINING — Flow full-height SOURCE BIN/BOOKMARKS rails → compact handles (needs live-DOM trace; code paths read don't explain the full height, so don't guess at core docking), the no-inner-scroll/Photoshop panel-resize rule across DockablePanel desktop+mobile, the Video mobile-shell timeline cramming (can't see a track), and finishing the Video+Paper mobile sweep.
- [ ] Atlas AI enhancement sweep (Image/Paper/Flow; research + opportunity tracker at `docs/superpowers/specs/2026-06-28-atlas-ai-enhancement-research.md`). DONE so far — **P0 (docs/notes/781):** Generative Fill no longer offers "Inpaint" on models that can't honour a mask (removed the silent inpaint→whole-image-edit fallback; added an honest "Edit (whole image)" op); ground truth = only `gpt-image-1/edit` truly masks on Atlas (flux exposes `mask_image` but Atlas ignores it); corrected the stale `ATLAS_MASK_MODELS`/honored sets to the 4 schema-accurate mask-field models. **P1a (docs/notes/782):** AI quick-mask upgraded from bounding-boxes to pixel-precise Gemini segmentation masks (box fallback). **P1c (docs/notes/783):** gen-fill reference control gated by the model's real `maxReferenceImages` (+ Add capped at the limit). All verified `npm run build` exit 0 + focused tests green; app rebuilt/restarted. REMAINING — **P1b:** cloud upscalers (Atlas/Stability) in Image+Paper upscale (paid API calls → needs a verification pass with the user's key); **P0b:** regenerate `maxReferenceImages` from schema `maxItems` (minor catalog drift, e.g. qwen-image/edit=3); optional first-class "Select by description" toolbar surface; Paper in-frame generation.
- [x] Atlas sequential models output every image, not just the first (see docs/notes/780): Seedream Sequential models (`bytedance/seedream-v{4,4.5,5.0-lite}/{,edit-}sequential`) return up to 15 cohesive images from ONE prompt in ONE call, but Flow kept only the first and dropped the rest. The `max_images` request side was already wired (schema-generated integer param on all six models → `node.data.atlasParams` → `applyAtlasModelParams` → accepted-field filter); the gap was the response/capture side. Added `additionalResults` to `ExecutionResult` + `extractAllAtlasOutputUrls` (flatten all output URLs), changed `pollAtlasPredictionResult` to return `string[]`, made `executeAtlasNativeImageNode` materialize every URL, and made `runNode`'s single-generation path expand `additionalResults` into an envelope (each image → Source Bin with shared `envelopeId`/incrementing `envelopeIndex` + `envelopeItems` on the node), mirroring the loop path. Single-output runs unchanged. Removed the temporary `[SL-DBG]` diagnostics. `npm run build` exit 0; targeted vitest 6 files/66 tests green. Runtime "N images land in bin/list" check is the user's to run (paid, account-bound generation — not auto-triggered).
- [x] Image editor batch (see docs/notes/682): (1) Move tool now drags the selected PIXELS, not just the marquee outline — `tools/selectionPixelMove.ts` lifts the active image layer's masked pixels and re-composites them at the drag offset with a `paint` undo op (selections previously only fed generative fill / mask transforms); (2) added Undo/Redo/Cut/Copy/Paste buttons to the Image tools palette (Paper parity), wired via a shared `imageClipboardActions.ts` to avoid the workspace↔toolbar import cycle; (3) side-docked sidebar panels (Layers/Properties/History) expand to content instead of inner-scrolling via a `DockExpandContext` so only the whole sidebar scrolls ("no iframes"); (4) shared Source Bin no longer overlaps the Image workspace controls row (top offset 112→144px); (5) transparency-checkerboard tiles clamped to the document bounds so a non-multiple-of-16 doc no longer shows a dashed strip past the image. Verified in-browser on AMD; full suite 2904/2904, tsc clean; built + sideloaded the Android debug APK (`studio.sloom.signalloom`).
- [x] Real GPU brush acceleration for the smudge/blur/sharpen retouch family (fixes "practically unusable" smudge; see docs/notes/680 + 681): ROOT CAUSE — the earlier "GPU/backend routing" was inert (the `gpuBrushEngine`/`gpuAcceleration` toggles were set+echoed but never consumed; no WebGL/WebGPU path existed) and each interpolated stamp did a full-canvas `getImageData→clone→putImageData` (plus full `cloneBitmap`+composite rebuild in allLayers mode) → `O(canvas)` per stamp. DELIVERED 2026-06-15: a stroke-scoped, dirty-rect `BrushBackend`/`StrokeSession` engine under `src/lib/brushEngine/` — upload the layer once, process each dab over only its bounding-box rect, read back once on commit. P1 region-bounded CPU backend (reference; commits `d99e666`→`98ed05b`), P2 WebGL2 fragment-shader backend (`texelFetch` disc/average matching the CPU math; `f6234f5`) measured ~128× faster and byte-exact-within-1-LSB on AMD RX 5700 XT, GPU acceleration **on by default** (`fef1618`) behind the now-honest "GPU acceleration (<resolved backend>)" toggle (on→auto/WebGL2, off→forced CPU), and a one-time GPU correctness self-test gate that falls back to CPU if a driver compiles the shaders but computes garbage (`357e04d`). Verified in-browser on real AMD hardware (auto→webgl2, full stroke commits correctly); full suite 2904/2904, `tsc` clean. DEFERRED (planned, with reasons): P3 WebGPU backend — unverifiable here (`requestAdapter()` returns null), needs an async-interface rework, and adds no coverage for AMD/Adreno (both served by WebGL2); P4 migrating normal paint/eraser onto the engine — would regress the already-fast Canvas2D path. Adreno/Android Qualcomm path is served by the same WebGL2 backend, guarded by the self-test.
- [x] Android fullscreen toggle works (native immersive mode): the top-nav toggle used the web Fullscreen API, a no-op in the Android WebView. Added a native `SignalLoomSystemUi` Capacitor plugin (`setFullscreen({enabled})` using androidx `WindowInsetsControllerCompat` immersive-sticky, registered in `MainActivity`), a `src/lib/androidSystemUi.ts` bridge (`isAndroidNativeFullscreenAvailable`/`setAndroidFullscreen`), and an Android branch in `TopNavbar.toggleFullscreen` (web path kept for desktop/browser). Added explicit `androidx.core:core` dep. 3 new tests, `tsc -b` clean, `npm run build` + Gradle `assembleRelease` green, signed APK sideloaded.
- [x] Flow canvas two-finger pinch-zoom anywhere (over nodes): fixed pinch only working on empty canvas — a finger on a node previously dragged the node instead of zooming. `FlowWorkspaceShell` now intercepts 2-finger gestures in the capture phase (guarded to touches starting inside `.react-flow`), `preventDefault`/`stopPropagation` so node-drag/pane handlers can't claim them, and drives `useReactFlow().setViewport` via the pure, tested `computePinchViewport` (pan by midpoint delta + scale about the midpoint, clamped to store min/max zoom); `nodesDraggable`/`panOnDrag` disabled while pinching, `zoomOnPinch` left on for desktop trackpad. 7 new tests (5 math + 2 jsdom behavioral incl. pinch-starting-on-a-node) + existing flow tests green, `tsc -b` clean, `npm run build` green, signed APK sideloaded.
- [x] Mobile UI overhaul — settings, dialogs, and video workspace phone support: fixed the Settings screen on phones by giving `DockableDialog` a `mobilePhoneInterface.enabled` full-screen sheet branch (slim title bar + close + safe-area padding) instead of the off-screen 520px-min floating panel — which also makes EVERY dialog phone-usable — and making `SettingsModal`'s overflowing header a compact scrollable segmented control + icon Refresh on phone. Built the approved "preview + tabbed NLE panels" mobile video editor (`VideoWorkspaceMobileShell`) that reuses the real `videoPanels` content (program-monitor preview pinned on top; Timeline/Clip/Source/Sequence/Export/Readiness/Diagnostics as a scrollable tab bar), wired via an early phone return in `VideoWorkspace` that leaves the desktop NLE layout untouched. 12 new focused tests + 54 adjacent green, `tsc -b` clean, `npm run build` green. Remaining/iterative: on-device polish of each panel at phone width, optional tab keep-alive, and continuing the general sweep (Flow canvas controls).
- [x] Google Play Store release kit assembly: rename Android package to `studio.sloom.signalloom` (v1.0.0), generate the `sloom-upload` keystore (CN=Sloom Software, valid to 2053) wired through git-ignored `android/keystore.properties`, build and verify a signed release AAB (`release/play-store/SignalLoom-1.0.0.aab`, jarsigner-verified), produce store graphics (clean loom-knot 512 icon + 1024×500 feature graphic), and write the full release kit under `release/play-store/` (RELEASE_GUIDE.md, listing.md with limit-checked copy + verify-listing.sh, pricing.md recommending $14.99 one-time, privacy-policy.md, data-safety.md, content-rating.md). Hardened `.gitignore` so the keystore/passwords/binaries never enter git; documented the new-personal-account 12-tester/14-day closed-test gate. Minimized Android permissions to the correct set — removed unused `READ_MEDIA_IMAGES/VIDEO/AUDIO` (import uses the system file picker; `@capacitor/filesystem` never requests them) and scoped the legacy `READ_EXTERNAL_STORAGE` (maxSdkVersion 32) / `WRITE_EXTERNAL_STORAGE` (maxSdkVersion 29) to where the plugin actually needs them, then rebuilt + re-signed the AAB and verified the shipped manifest. Local repo only — nothing pushed.
- [x] Provider catalog + pricing sync automation slice (Plan 3 of 3): confirm the runtime in-app catalog auto-refresh (App.tsx effect) now pulls live models for every configured provider via Plan 2 discovery + inference, add a committed `npm run sync:catalog` dev script (`scripts/sync-provider-catalog.mjs`) that fetches each provider's live `/models`, writes `src/data/providerModelCatalog.generated.json`, and prints drift, and confirm unverified-price models surface a "provider-billed" label while remaining runnable; 96 focused tests green, `tsc -b` clean. Completes the provider-integration/mask spec (Plans 1–3).
- [x] Atlas catalog inference + cross-provider parity slice (Plan 2 of 3): add `src/lib/imageModelInference.ts` that derives image-model capabilities/operations/controls from the `vendor/model/operation` slug convention + vendor hints, make `getImageModelDefinition` infer capabilities for uncurated/discovered slugs (so Atlas models expose all their capabilities), refresh the Atlas seed with the current families (nano-banana-2, qwen-image-2.0, wan-2.7, grok-imagine, ERNIE) while keeping still-offered FLUX/Seedream/Z-Image, guarantee nano-banana reference parity across Gemini/Vertex/Atlas, and enable native Atlas model discovery; 69 focused tests green, `tsc -b` clean. Remaining: Plan 3 (all-provider catalog + pricing sync automation).
- [x] Mask painting integrity + object-detection slice (Plan 1 of 3): add a shared `src/lib/imageMask` canonical-mask core with per-provider normalization (fixes the inverted OpenAI/Atlas-GPT mask bug; Stability/Atlas-native get white-on-black), allow painting a mask on a Flow Image node's own generated/imported image, and add pluggable Gemini object-detection masking in both the Flow mask painter and the Image editor generative-fill bar; 40 focused tests green, `tsc -b` clean. Remaining: Plan 2 (catalog inference/seed/prune/parity) and Plan 3 (catalog+pricing sync), plus live-provider smoke.
- [x] Image Brush / Eraser Engine 62-atom completion slice: restore checklist accounting to 62 atoms, implement velocity dynamics, texture sampling, dual-brush composition, wet-media alpha build-up, GPU/backend routing for desktop AMD/Nvidia and Android Qualcomm/Adreno, Android/gamepad brush-control descriptors, ABR metadata fidelity, compact Brush panel controls, focused tests, TypeScript, and refreshed parity snapshot confirmation.
- [x] Image Vector Masks completion slice: add active-layer retained vector-mask point editing UI in the Paths panel, preserve pixel masks and bitmap versions while editing vector-mask path metadata, keep enabled/invert/link controls available, retain boolean/PSD semantics as explicit unsupported caveats, and verify Paths panel/vector-mask/parity tests, TypeScript, and live dashboard progress.
- [x] Image Hand / Navigation completion slice: add stable mixed-tool interaction descriptors, active-tool-preserving temporary hand-pan metadata, pointer-capture/cursor policy metadata, and concrete top-toolbar/floating-tool/canvas/wheel/touch navigation affordance descriptors, then mark the hand-navigation checklist done with focused navigation/parity tests, TypeScript, and live dashboard confirmation.
- [x] Development dashboard checklist-complete status guard: force parity-run table rendering and app-side Image parity status counts to derive done/partial/remaining from completed Boolean checklist atoms/objective coverage instead of stale manual status or legacy estimate fields, refresh the snapshot artifact, and verify no checklist-complete row is serialized as partial.
- [x] Development dashboard checklist-only snapshot correction: make parity snapshot artifacts use computed checklist statuses, count 100% checklist rows as done even when source status is stale, remove legacy estimate/verification-confidence fields from the snapshot artifact, refresh the artifact, and verify dashboard regressions plus live `/status.json`.
- [x] Image Dodge/Burn/Sponge non-destructive retouch output slice: add New Retouch Layer output mode for Dodge, Burn, Saturate, and Desaturate strokes, preserve source-layer pixels, commit generated retouch layers with undoable layer operations and metadata, expose the output control in the finishing brush panel, update parity/checklist evidence, verify focused runtime/descriptors/UI tests, TypeScript, Android sync, and live dashboard progress.
- [x] Image keyboard selection nudge slice: route Arrow-key nudges to active committed Image selections before layer movement, preserve Shift large-step behavior, push undoable selection history, update parity/docs, and verify focused keyboard/selection tests, TypeScript, and Android sync. (Verified 2026-06-16 already implemented: `ImageEditorWorkspace.handleKey` nudges the committed selection via `nudgeSelection` and `return`s before the layer-move branch; `step = shiftKey ? 10 : 1`; pushes an undoable `kind:'selection'` op. Covered by `PhotoshopQuickActions.test.ts` (pure `nudgeSelection`) + `ImageEditorWorkspaceNavigation.test.tsx` behavioral test "nudges the active committed selection with Arrow keys before moving the active layer".)
- [x] Image selection nudge parity completion: record direct undoable selection nudging in the Image parity ledger, keep the selection-tools row honest about remaining AI subject/magnetic/refine gaps, verify focused parity and selection-manipulation tests, TypeScript, and Android sync.
- [x] Paper mobile left source drawer safe-inset follow-up: restore the visible left Source Library affordance after the viewport-overlay move by shrinking it to a compact `w-7` handle, placing the portaled handle at a fixed 10px viewport inset instead of the unsafe edge, raising viewport handles above workspace chrome, and verifying focused Paper mobile tests plus phone-sized screenshots of both the handle and opened drawer.
- [x] Paper mobile source drawer viewport overlay regression: move phone Paper source/panels/assets drawer handles and drawers into a viewport-level overlay for the full workspace, keep the left source tab small but visible above workspace/floating-tool layers, preserve inline shell tests, and verify red/green Paper mobile tests, TypeScript, phone-sized browser evidence, and Android sync.
- [x] Image Paths/Pen anchor add-delete slice: add retained straight-path anchor insert/delete helpers, expose Add/Delete Anchor controls in the Paths panel selected-anchor UI, update anchor-session readiness and parity wording so straight anchor structure edits are no longer reported as missing, and verify focused path/panel/parity tests plus TypeScript and Android sync.
- [x] Paper mobile source drawer visible-strip regression: restore the left Source Library drawer affordance on phone Paper by giving the source edge handle a guaranteed visible compact strip (`left-2`, `w-8`, rounded-right pill), tag it for regression coverage, and verify focused Paper mobile shell tests plus Android web asset sync.
- [x] Image Magic Wand anti-alias runtime slice: add one-pixel partial-alpha edge weighting to committed Magic Wand selection masks when anti-alias is enabled, update Magic Wand readiness/workflow descriptors and Image parity wording so anti-alias is no longer reported as unsupported for selections, keep Paint Bucket fill anti-alias/gap-close/channel caveats honest, and verify red/green runtime, adjacent selection/parity tests, TypeScript, and Android sync.
- [x] Paper mobile source drawer safe-edge regression: move the hidden-chrome left Source Library drawer handle off the extreme Android screen edge, add a focused red/green reachability regression, verify Paper/Image mobile shell detection, TypeScript, phone-sized Playwright handle/drawer evidence, and Android sync.
- [x] Image selection feather runtime slice: apply Selection Tool feather settings to real Marquee, Lasso, and Magic Wand selection masks through the shared SelectionInteraction path, preserve captured base selections during add/subtract/intersect, update tool readiness/parity descriptors to remove stale hard-edge feather blockers, add red/green runtime regressions, verify focused selection/parity tests, TypeScript, and Android sync.
- [x] Android phone DPR-1 source drawer regression: classify Galaxy-class Android WebViews that report physical pixels at devicePixelRatio 1 as phone surfaces, restoring Paper/Image mobile edge shells and the left Source Library drawer handle while keeping tablet and DeX layouts on the full interface; verify red/green detector coverage, Paper/Image mobile shell regressions, TypeScript, and Android sync.
- [x] Android phone source-drawer detection regression: keep Paper/Image phone shells enabled when Android WebView reports Galaxy-class high-density physical pixels, restoring the left Source Library drawer handle on phone screens while preserving tablet/DeX desktop layout, with focused mobile detector/shell tests, TypeScript, and Android sync.
- [x] Image retouch cancel completion slice: restore uncommitted Blur, Sharpen, Sponge, Clone Stamp, and Spot Heal preview pixels on tool cancellation, avoid undo/dirty writes for canceled strokes, add red/green cancel regressions, verify retouch/parity tests, TypeScript, and Android sync.
- [x] Image retouch cancel hardening slice: restore uncommitted Burn/Dodge tone-brush and Smudge preview pixels on tool cancellation, avoid undo/dirty writes for canceled strokes, add red/green cancel regressions, verify retouch/parity tests, TypeScript, and Android sync.
- [x] Image mobile hidden-chrome source drawer regression: keep the Image phone edge shell mounted when Tab/hide-interface mode is active, restore the left Source Library drawer handle with compact Paper-style sizing, add pointer-release activation for pen/touch drawer taps, and verify focused Image/Paper mobile shell tests, TypeScript, Android sync, and phone-sized browser handle rectangles.
- [x] Paper mobile source drawer handle visibility regression: make the left Source Library tab visible in compact hidden-chrome mode, keep it above the floating tools palette, tag edge handles for regression coverage, and verify focused Paper mobile shell/store tests, TypeScript, production build, and Android asset sync.
- [x] Image selection drag handoff slice: make Move-tool drags inside an active selection mask start/reuse a Transform Selection session, update the live selection preview instead of moving the active layer, keep apply/cancel/Enter/Escape commit behavior, update selection parity wording, and verify focused Move/parity tests plus TypeScript.
- [x] Image Move runtime snapping slice: apply existing Move tool snap-guide planning during live drag, update parity descriptors from preview-only to runtime snapping, verify focused Move/parity tests, and fix descriptor readonly typing in the integrated TypeScript pass.
- [x] Paper mobile hidden-chrome drawer handle regression: keep the Paper phone edge shell mounted when top chrome is hidden, render compact left/right/bottom handles instead of removing them, keep side drawers independent from hidden top chrome, and verify focused Paper mobile shell/store tests.
- [x] Paper mobile Source/Panels/Assets drawer restoration: keep Paper Source Bin available as the left phone edge drawer, suppress only the duplicate legacy shared source rail on phone Paper/Image, embed the Source Library without its internal collapse rail, keep edge handles above drawers and vertically centered in the full workspace, rebuild/sync/install on the connected Android phone, and verify source/panels/assets drawer switching with Android UI dumps and screenshots.
- [x] Image quick AI edit Atlas/mobile portrait slice: add Atlas Cloud to the Image quick edit provider picker, expose Atlas edit-capable models through the existing Image provider catalog, constrain the quick edit panel to a portrait phone viewport with internal scrolling and compact control grids, verify red-first quick edit tests, catalog tests, TypeScript, Android sync/build/install, and phone runtime evidence.
- [x] Paper mobile toolbar activation slice: reproduce the Android WebView pointer-contact failure with a red toolbar regression, activate Paper tools/actions on primary pointer down with duplicate pointer-up/click suppression, rebuild/sync/install on the connected phone, verify exact toolbar nodes are exposed in the Android UI tree, and capture phone launch/after-tap/crash-log evidence.
- [x] Android Atlas generated-asset materialization slice: download native Atlas Cloud remote image outputs into app-owned blob results before Source Library persistence, fall back to app-private Android Data storage when public Documents permission is denied, rebuild/sync/install on the connected phone, and verify focused provider/source-bin/storage tests plus TypeScript/build/Android launch evidence.
- [x] Android/mobile application chrome shortcut slice: promote Tab into a first-class `view:toggle-interface` command across renderer shortcuts, command palette, Electron menus, gamepad/native command dispatch, and Android phone smoke verification so Tab toggles the Sketchbook-style interface instead of browser focus traversal.
- [x] Mobile Image phone shell design prototype: build and visually verify a standalone Galaxy Ultra-class HTML/canvas mockup with top document/tool drawer, left Source Library drawer, right accordion dialog drawer, bottom asset drawer, draggable compact tools palette, hide-all restore button, and portrait/landscape simulation before moving the pattern into production UI.
- [x] Paper touch navigation mode slice: add an unobtrusive touch-device toggle that routes finger input to canvas navigation while preserving pen/mouse editing, support configurable pan/pinch gestures, keep Paper/Image floating tool palettes visible in collapsed phone chrome, verify desktop/DeX/tablet compatibility, and run Android phone validation.
- [x] Android and desktop launch splash readiness slice: use the provided Signal Loom artwork for Electron startup, Android native launch theme, and a web boot overlay; fix Android launch drawable resource linking, rebuild/sync Android, install and cold-launch on DeX, capture splash/post-launch evidence, and surface launch-splash telemetry on the development dashboard.
- [x] Development dashboard Image parity reporting slice: preserve always-expanded static checklist atoms for every parity row, keep row progress checklist-only, normalize worker artifacts so lane-based chips/highlights follow `ops/dev-dashboard/artifacts/image-parity-workers-latest.json`, and verify focused dashboard regression coverage with auto-refresh still enabled.
- [x] Image Raster/Vector shape readiness slice: deepen retained shape/path metadata, fill/stroke editability, rasterize readiness, boolean support matrix, unsupported Bezier/live-stack/overlap states, focused tests, and TypeScript verification.
- [x] Image History / Actions readiness slice: deepen deterministic named snapshots, rename state, macro playback, batch-open-doc diagnostics, arbitrary-command/file-folder batch caveats, preview signatures, focused tests, and TypeScript verification.
- [x] Image Move / Free Transform readiness slice: deepen deterministic numeric geometry, pivot, apply/cancel preview, handle metadata, transform support matrix, source safety, snapping/distribution caveats, focused tests, and TypeScript verification.
- [x] Image Brush engine readiness slice: deepen deterministic brush dab options, smoothing/pressure/tilt/symmetry descriptors, preset catalog/import-export, unsupported dynamics states, route summaries, focused tests, and TypeScript verification.
- [x] Image Layer mask readiness slice: deepen reveal/hide/from-selection readiness, selection blockers/summaries, density/feather, overlay/mismatch/copy/link/refine caveats, stable signatures, focused tests, and TypeScript verification.
- [x] Image Channels and spot-channel readiness slice: deepen deterministic RGB/alpha/spot workflow descriptors, active RGB routing, alpha save/load roundtrip, spot preview/export warnings, direct-paint unsupported states, focused tests, and TypeScript verification.
- [x] Image Typography/Text readiness slice: deepen deterministic retained text editability, style packages, font fallback/persistence, OpenType caveats, find/replace and spellcheck/readability planning, native PSD text warnings, focused tests, and TypeScript verification.
- [x] Image PSD/XCF file interop readiness slice: deepen native construct roundtrip-risk descriptors, retained text/effects/source-link/filter metadata warnings, XCF export-only/import unsupported policy, fallback routes, focused tests, and TypeScript verification.
- [x] Image Layer Styles readiness slice: deepen deterministic supported-effect catalog, bevel/blend-if unsupported states, global light, preset portability, flattened export warnings, alpha/opacity caveats, focused tests, and TypeScript verification.
- [x] Image Adjustment Layer workflow readiness slice: deepen deterministic kind/parameter completeness, histogram readiness, mask/clipping implications, preset import/export serialization, unsupported preset-family warnings, focused tests, and TypeScript verification.
- [x] Image Layer Groups hierarchy/readiness slice: deepen nested group normalization, visibility/lock/opacity inheritance, pass-through/group-mask caveats, batch-operation blockers, preview signatures, focused tests, and TypeScript verification.
- [x] Image universal upscale readiness slice: deepen Image upscaling route descriptors for on-device preference, cloud/bitmap fallback, print-resolution policy, SFX exclusion, dependency blockers, focused tests, and TypeScript verification.
- [x] Image Quick Mask readiness slice: deepen QuickMask enter/exit, selection-to-mask/mask-to-selection, overlay opacity/color, brush routing, channel interop warnings, focused tests, and TypeScript verification.
- [x] Image Color Modes / Color Proof readiness slice: deepen deterministic RGB/CMYK/grayscale/indexed/Lab policy, bit-depth caveats, proof/gamut-warning descriptors, print/export implications, focused tests, and TypeScript verification.
- [x] Image document export/readiness slice: deepen Export As / Save for Web descriptors for format capabilities, transparency/animation/vector/text/layer flattening, DPI/profile warnings, batch preset readiness, focused tests, and TypeScript verification.
- [x] Image Magic Wand / Paint Bucket readiness slice: add deterministic target-channel, opacity, blend-mode, preserve-transparency, preview-signature, anti-alias/gap-close/channel caveats, focused tests, and TypeScript verification.
- [x] Image Source Library / Suite Handoff readiness slice: add deterministic visible/mask export, source packaging, Flow/Video/Paper handoff, source-id/blob-only warnings, preview signatures, focused tests, and TypeScript verification.
- [x] Image Standalone Quick Edit save/open readiness slice: add deterministic quick-edit/source-linked/export-only save policy, destructive overwrite, flattened export, native roundtrip caveats, preview signatures, focused tests, and TypeScript verification.
- [x] Android Image parity readiness slice: add deterministic Android-native QNN/local/cloud route metadata, DeX/open-document/imported-file evidence states, runtime/model/provider blockers, focused tests, and TypeScript verification.
- [x] Image Retouch workflow readiness slice: add deterministic clone/heal sample-source, aligned/restart state, overlay/transform unsupported, patch/remove caveat, active-layer/non-destructive output, smudge composite sampling, blocker, preview-signature metadata, focused tests, and TypeScript verification.
- [x] Image Pen Tool readiness slice: add deterministic straight-segment path, live preview/commit/cancel, retained path layer output, selection/fill/stroke/vector-mask interop, Bezier/curvature/text-on-path/one-step-vector-mask caveats, operation blockers, focused tests, and TypeScript verification.
- [x] Image Paths Panel readiness slice: add deterministic path entry, work/saved/layer-backed classification, load-selection/fill/stroke/vector-mask readiness, operation blocker, independent saved path/thumbnail/Bezier caveats, focused tests, and TypeScript verification.
- [x] Workspace launch/icon readiness slice: add deterministic launch command, standalone shared-binary entry, transparent-edge icon metadata/check, Windows/macOS/Linux packaging caveat, signed-package evidence, focused tests, and TypeScript verification.
- [x] Image Generative edit readiness slice: add deterministic selected-region/reference/provider/model/cost/upscaler readiness, missing credential/provider blockers, SFX print-resolution exclusion, Photoshop Generative Fill unsupported state, focused tests, and TypeScript verification.
- [x] Image Gradient readiness slice: add deterministic mode/preset/stop/transparency/dither/export readiness, editable-native-gradient-layer/mesh/noise/gradient-map caveats, focused tests, and TypeScript verification.
- [x] Image Document/Canvas size readiness slice: add deterministic image/canvas resize, DPI/print readiness, destructive resize, resampling, anchor offsets, transparent expansion, high-bit-depth caveats, focused tests, and TypeScript verification.
- [x] Image Source-linked readiness slice: add deterministic source-link status/history, replace-contents readiness, Edit Original metadata-only caveat, snapshot availability, relink/repair blockers, smart-filter caveats, PSD Smart Object unsupported state, suite handoff safety, focused tests, and TypeScript verification.
- [x] Image Quick Action macro readiness slice: add deterministic fixed-command summaries, arbitrary-param unsupported states, batch readiness, per-document blockers, mutability, undoability, native caveats, stable signatures, focused tests, and TypeScript verification.
- [x] Image Layer Masks readiness parity slice: add deterministic create/edit/apply/delete/invert/density/feather readiness planning, mask-vs-pixel target mismatch detection, copy/link/apply/refine caveats, overlay preview status, unsupported linked-mask/refine-workspace states, focused tests, and integrated TypeScript verification.
- [x] Image PSD native construct readiness slice: add deterministic native PSD construct preservation/readiness metadata for groups, retained text, effects, adjustment layers, source-linked/smart-object metadata, smart-filter caveats, flattened/metadata-only states, warning codes, round-trip risk, focused tests, and integrated TypeScript verification.
- [x] Image Channels workflow planning slice: add deterministic RGB/alpha/spot channel operation planning for active RGB routing, selection-to-alpha, alpha-to-selection, alpha persistence readiness, spot-channel caveats, direct paint support states, signatures, focused tests, and integrated TypeScript verification.
- [x] Image Adjustment Layers readiness slice: add deterministic adjustment kind/parameter completeness, histogram dependency/readiness, mask/clipping status, preset serialization/import/export readiness, pure preset application, unsupported preset-family caveats, focused tests, and integrated TypeScript verification.
- [x] Image Typography workflow planning slice: add retained text layer summaries, find/replace planning and pure replacement helpers, spellcheck/readability planning, unsupported dictionary/native-font/text-on-path states, focused tests, and integrated TypeScript verification.
- [x] Image XCF import readiness policy slice: add XCF extension/MIME detection, explicit unsupported import policy, PNG/TIFF/PSD/source-library fallback routes, export compatibility level, caveats, stable signatures, round-trip risk metadata, focused tests, and integrated TypeScript verification.
- [x] Image Move Tool snapping/distribution planning slice: add deterministic geometry, snap-guide/candidate, snapped-delta, align/distribute readiness, lock/blocker, and preview-signature helpers with focused tests and integrated TypeScript verification.
- [x] Image Lasso magnetic planning slice: add descriptor-only Magnetic Lasso planning with snap radius, contrast threshold, frequency, candidate counts, cursor distance, pixel-source/refine-edge unsupported states, focused tests, and integrated TypeScript verification.
- [x] Development dashboard worker-lane status hardening: normalize `completed` worker artifacts to non-active `complete` status, keep active worker counts/borders honest, preserve always-visible static Image parity checklists, and verify dashboard regression coverage plus live model output.
- [x] Image Background Eraser parity slice: add a dedicated bounded Background Eraser tool path with toolbar/shortcut/menu integration, Properties controls, active pixel-layer alpha clearing, deterministic tolerance/limit/protect-foreground behavior, parity wording, focused tests, TypeScript, and production build verification.
- [x] Image Content-Aware Fill / Remove / Patch parity slice: add deterministic local content-aware repair planning/execution metadata for fill, remove, and patch operations, update quick-action planning and honest parity caveats, and verify focused content-aware/quick-action/parity tests plus integrated TypeScript.
- [x] Image Object Selection / Select and Mask readiness slice: add deterministic object-selection refinement and Select and Mask readiness descriptors with explicit local/unsupported caveats, focused object-selection/select-mask/parity tests, and integrated TypeScript cleanup.
- [x] Image Layer Organization boundary parity slice: add bounded multi-select/group-boundary planning metadata and caveats for layer organization, update layer-stack/group parity wording, and verify focused layer-organization/parity tests plus integrated TypeScript cleanup.
- [x] Image Camera Raw policy parity slice: add deterministic RAW detection/develop-first handoff descriptors, source metadata preservation/readiness tests, explicit unsupported raw-development parity wording, focused file/source/parity tests, TypeScript, and refreshed parity snapshot.
- [x] Image Puppet Warp bounded pin-session slice: add deterministic bounded pin helpers/session planning, max-pin enforcement, explicit on-canvas/non-destructive/smart-object caveats, focused puppet/parity tests, TypeScript, and refreshed parity snapshot.
- [x] Image Liquify session/control descriptor slice: add discoverable liquify session and brush-preview descriptors, requested-vs-supported mode metadata, explicit reconstruct/smooth/face-aware/non-destructive warnings, focused liquify/parity tests, TypeScript, and refreshed parity snapshot.
- [x] Image Magic Eraser parity slice: add a dedicated Magic Eraser tool, Shift+E shortcut/menu command, toolbar/Properties controls, tolerance-based contiguous/global alpha clearing on the active pixel layer, undoable paint operations, descriptor/parity updates, dashboard worker-lane telemetry, focused tests, TypeScript, production build, and Android web asset sync.
- [x] Image Text/Typography font-stack and OpenType slice: add retained style-level OpenType feature persistence, a standard font-stack catalog and picker, visible OpenType toggles for selected text layers and the Text tool panel, parity tracking updates, focused typography/UI/parity tests, TypeScript, parity snapshot, production build, and Android web asset sync.
- [x] Development dashboard static checklist runtime verification: confirm the Image Parity Run checklists render expanded by default with no expand/collapse controls, no internal checklist scrolling, visible overflow/static max-height behavior, one-second `/status.json` polling, and fresh Playwright 1920x1080 evidence.
- [x] Image Layer Styles inner-glow/gradient-overlay slice: add persisted runtime `innerGlow` and `gradientOverlay` layer effects, deterministic content rendering, compact effect controls for inner glow size and gradient start/end colors/angle/scale/reverse, capability catalog/group/descriptor updates, parity tracking, focused style/UI/clipboard/parity tests, TypeScript, and refreshed parity snapshot.
- [x] Image Magic Wand / Paint Bucket parity slice: add Paint Bucket fill blend-mode math, preserve-transparency fill behavior, persisted tool settings for fill mode/transparency, compact Properties-panel controls, descriptor/signature updates, parity tracking, focused Paint Bucket/tool/UI/store/parity tests, TypeScript, and refreshed parity snapshot.
- [x] Image History/Snapshots named-state slice: add custom snapshot naming, undoable snapshot rename controls, durable `updatedAt` snapshot metadata through project restore/export, descriptor rename targets/signatures for named snapshot states, update History / Actions parity tracking, verify focused snapshot/history/store/project/parity tests, TypeScript, parity snapshot, production build, and Android web asset sync.
- [x] Image Layer Styles satin/pattern overlay slice: add persisted runtime `satin` and `patternOverlay` layer effects, deterministic content rendering, compact Layer Effects controls for satin distance/size/angle/invert and pattern foreground/background/pattern/scale, supported capability/portability descriptors, updated Layer Styles parity tracking, focused style/UI/interop/parity tests, TypeScript, parity snapshot, production build, and Android web asset sync.
- [x] Image Channels spot-channel metadata slice: add persisted document-level spot channels, sanitize them on project restore, surface a panel-backed Spot Channels section with create/rename/delete plus tint/opacity/solidity/visibility controls, integrate spot previews/export warnings into channel planning descriptors, update Channels parity tracking, verify focused Channels/store/project/parity tests, TypeScript, parity snapshot, production build, and Android web asset sync.
- [x] Development dashboard live checklist tightening: make Image parity checklists explicit static table content with no collapsible controls or internal checklist scrolling, reduce dashboard polling to one second, and trigger an immediate `/status.json` poll so progress updates without a manual refresh.
- [x] Image Gradient parity slice: add a standard multi-stop preset library, persisted preset IDs, arbitrary stop add/remove/offset editing, per-stop opacity transparency stops, deterministic ordered dithering in render/tool descriptors, update Gradient parity tracking, verify focused Gradient/store/panel/parity tests, parity snapshot, production build, and Android web asset sync.
- [x] Android Image parity evidence slice: add an artifact-backed Android DeX 1080p opened-document edit telemetry lane, surface edit-present evidence from the existing open-document artifact, update Android parity wording to distinguish blank-canvas opened-document edit evidence from still-missing imported-file editing coverage, and verify dashboard/parity tests plus TypeScript, parity snapshot, and production build.
- [x] Image non-overlapping polygon boolean parity slice: broaden exact retained vector boolean materialization beyond rectangles/identical polygons to disjoint closed simple polygons, preserve retained path outputs and vectorBooleanSource metadata, keep overlapping polygon/Bezier/live-stack gaps explicit, update parity tracking, and verify focused vector/parity tests plus TypeScript/dashboard/parity snapshot.
- [x] Image vector shape-to-path parity slice: add retained rectangle/ellipse Convert Shape to Editable Path support, wire it into the Layers context menu as one undoable vector-layer operation, preserve style/source metadata without rasterizing, update Vector/Raster Shape/Paths parity tracking, and verify focused vector/Layers/parity tests.
- [x] Image gradient angle parity slice: add angle/conical gradient mode to Image gradient settings, renderer math, tool parity descriptors, Properties panel UI, and Gradients parity tracking while keeping mesh/noise, dither, preset library, and richer stop editing listed as open gaps.
- [x] Image vector boolean UI slice: add direct Layers context-menu Vector Boolean actions for adjacent retained vector operands, materialize exact rectangle/identical simple-polygon results into retained path layers in one undoable layer-stack operation, keep unsupported inputs non-mutating with a visible warning, and update parity tracking honestly.
- [x] Development dashboard checklist usability slice: keep Image parity feature checklists always visible as static non-scrolling Boolean atom lists, remove any expand/collapse affordance expectation, and hot-swap dashboard content from `/status.json` polling without requiring a manual or full-page refresh.
- [x] Image hand/navigation parity slice: add shared navigation command helpers, compact top-bar Fit / 100% / zoom in / zoom out controls, Ctrl/Cmd zoom/focus shortcuts, update Hand/Navigation parity tracking, and verify focused navigation/workspace/parity tests.
- [x] Image path-to-vector-mask parity slice: add retained vector-mask creation from selected Paths-panel path layers onto the active target layer, store target-local vector-mask metadata without rasterizing existing masks, update Pen/Paths/Vector Masks parity wording, and verify focused path/vector/parity tests.
- [x] Image clipping-mask parity slice: add group-aware clipping-base rendering for visible group descendants, add context-menu batch create/release clipping controls for layers above a base layer, update clipping-mask parity rows/tests, and verify focused Image/dashboard tests, TypeScript, parity snapshot, and production build.
- [x] Image vector boolean/dashboard live checklist slice: materialize exact supported vector boolean results into retained editable path layers with source metadata, update Image parity rows/tests, make dashboard parity checklists static/non-scrolling/visible by default, add `/status.json` auto-refresh polling, and verify with focused Image/dashboard tests, TypeScript, parity snapshot, and production build.
- [x] Development dashboard progress transparency slice: replace Image parity progress bars with completed Boolean checklist atoms divided by total atoms for every Image parity row, add expandable per-feature done/open checklists, preserve legacy parity estimates only as context, and add artifact-backed worker lane row highlighting for active subagent ownership.
- [x] Image vector-mask live render parity slice: make retained path-backed vector masks participate in shared preview/export flattening, intersect them with raster layer masks, include vector-mask metadata in renderer cache signatures and low-res preview scaling, remove the live-render unsupported warning from default vector-mask plans, update parity tracking, and verify focused render/vector-mask/source/parity tests.
- [x] Image blur/sharpen retouch parity slice: add real current-layer/current-and-below/all-layers sample-source support for Blur and Sharpen descriptors and live brush strokes, expose compact finishing-brush sample-mode controls, keep Smudge honestly current-layer previous-point only, update parity rows/tests, and verify focused retouch/tool/UI/parity tests.
- [x] Image Sponge retouch parity slice: add vibrance-weighted Sponge saturation response, optional luminance preservation, live tool-handler wiring through retouch settings, dedicated Sponge controls in the Finishing Brush panel, descriptor/parity updates, a refreshed dashboard parity snapshot at 43.8 overall audit progress, and focused retouch/UI/tool/parity tests plus TypeScript and production build verification.
- [x] Image finishing-brush parity slice: implement dodge/burn tonal range targeting and protect-tones behavior, surface dedicated finishing-brush controls in the Image Brush properties panel, update retouch capability descriptors and parity rows, refresh the dashboard parity snapshot to 43.7 overall / 53.7 high-priority audit progress, and verify with focused retouch/UI/parity tests, TypeScript, and production build.
- [x] Image parity evidence/dashboard slice: retry Android DeX 1080p after display churn, capture current display-14 open-document evidence, add schema-compatible dashboard telemetry for Android open-document and dockable tab UI artifacts, verify compact Image tools/no-Dock and stable tab groups at 1920x1080/2560x1440, refresh the parity snapshot, and rerun dashboard/parity/dockable, TypeScript, and production build checks.
- [x] Image parity rolling descriptor/evidence slice: integrate completed text, gradient, layer-style, filter-stack, channel, retouch, content-aware, source-link, PSD, transform, blend-mode, source-handoff, Android Dex, Image Automation, dockable-tab model, dashboard telemetry, and parity snapshot lanes into the Image audit ledger; verify with focused parity tests, a 378-test combined Image/dashboard/automation/dockable batch, artifact JSON checks, and repo-wide TypeScript.
- [x] Image parity concurrent helper batch: use rolling four-worker lanes plus local integration to deepen gradients, batch/action manifests, vector booleans, object selection, Select & Mask helpers, format/color policies, liquify, puppet warp, layer-effect capability metadata, retouch planning, text/OpenType descriptors, histogram feedback, vector masks, brush preview metadata, layer-group planning, artboard print/export planning, quick-action macro manifests, document geometry planning, parity tracking, and focused regression coverage without claiming full UI parity.
- [x] Image parity concurrent dashboard/progress slice: use the four-agent session cap plus a local lane to land bounded Image parity helpers, add RAW/bit-depth status, add an Image Parity Run dashboard table, and close the confusing audit-versus-readiness gap by making audit-backed parity the primary progress percentage while reporting stricter readiness as separate verification confidence.
- [x] Image parity concurrent slice: use the four-agent session cap plus a local lane to improve clone/heal retouch source controls, RGB channel paint routing, layer-style global light/presets, text style presets, color proof metadata, vector path-to-mask creation, separate Image automation node scaffolding, docked-panel scroll usability/tab metadata, viewport-safe context menus, parity tracking, focused regression coverage, build/type verification, Android sync, native smoke, and fresh Playwright desktop evidence.
- [x] Image parity concurrent dashboard/interop slice: replace the misleading plain-average “weighted” dashboard percentage with a stricter readiness score using feature weights, implementation ceilings, UX/test/desktop/Android evidence gates, integrate concurrent workspace launch/icon, RGB channel target, and PSD/source-link metadata preservation lanes, and update Image parity rows without claiming full channel paint routing or native PSD Smart Object parity.
- [x] Image parity concurrent slice: add dashboard capability tracking from the live Image parity ledger, deepen layer styles with inner shadow, add bounded three-stop custom gradients, add retained text baseline/kerning/caps typography controls for new and selected text layers, update parity progress, verify focused tests, build, Android sync, native smoke, and fresh Playwright desktop evidence.
- [x] Development dashboard progress telemetry slice: make Image parity visibly update with weighted checklist progress, completed/open Image task counts, and a goal progress bar so the dashboard reflects movement toward the active Image workspace goal instead of static bucket counts.
- [x] Dockable UI quality slice: add Adobe-style tabbed dialog/panel groups that can hold multiple docked or floating panels, switch active panels by tabs, keep multiple independent groups, support drag-to-tab grouping, and preserve stable floating geometry without ballooning.
- [x] Image automation architecture slice: keep Image editor automation flows separate from the main Flow workspace with a visually distinct Image Automation workspace/canvas, an Image-specific node catalog, directory/file read-write batch nodes, grouped batch operations, saved output folder flows, macro/image-operation nodes, and AI-assisted variable/parameter filling without polluting the main Flow node set.
- [x] Image top-chrome obstruction follow-up: stop the workspace-aware usage estimator from intercepting workspace switcher/top toolbar clicks in Image at 1920x1080+ while still keeping it off the active document.
- [x] Image parity feature slice: complete Crop straighten and rotate-crop behavior with persisted tool options, apply/cancel preview semantics, undoable crop commits for destructive and non-destructive crop modes, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add direct on-canvas anchor handles for selected straight-segment Pen path layers, including drag-to-move anchor behavior, undoable path layer updates, renderer/selection/fill/stroke parity after edits, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add editable retained path point controls for Pen-created vector path layers, including selected-path point listing, numeric point movement with undoable layer updates, raster/selection/fill/stroke parity after edits, focused regression coverage, build verification, Android sync, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a first Pen tool workflow with straight-segment retained vector path layers, live preview plus commit/cancel behavior, Paths-panel selection/fill/stroke support for `path` vector shapes, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image dock usability follow-up: make side-docked panel columns render as one scrollable same-width stack of static panels, avoid nested per-panel scroll traps at 1080p, and preserve drag/drop docking and arbitrary panel order.
- [x] Image parity feature slice: deepen History / Actions with saved quick-action rename controls, batch playback across currently open Image documents, per-run result summaries, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add real quick-action recording/playback inside the History workflow with persisted action sets, record/stop/save controls, replay/delete controls, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add destructive vs non-destructive crop behavior with a persisted `Delete Cropped Pixels` tool option, preserve hidden pixels and masks when cropping non-destructively, add focused crop regression coverage, then verify build, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: deepen editable filter stacks with persisted per-filter blend mode controls and renderer support, add focused regression coverage for filter blend math and Layers-panel mode changes, then verify build, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: deepen non-destructive layer filters with reorderable filter stacks and per-filter opacity controls in the Layers panel, add focused regression coverage for render ordering/blending and UI reorder actions, then verify build, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image right-dock 1080p follow-up: make the Full Suite right-panel stack keep lower panels like History and Paths reachable at 1920x1080 without relying on switching to a slimmer layout preset.
- [x] Image parity feature slice: add a first-class Paths panel for vector shape layers with persisted path entries, rename/delete controls, load-selection actions, fill/stroke layer generation, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add center-axis brush symmetry painting for brush and eraser with persisted symmetry mode controls, mirrored layer/QuickMask/layer-mask behavior, visible canvas guide overlays, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: deepen the Brush preset library with a larger built-in standard brush set, previewable preset tiles, persisted user preset save/rename/delete controls, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: convert Rectangle/Ellipse Shape tools from destructive raster paint into editable vector-backed layers with persisted fill/stroke properties, Layers-panel editing controls, rasterize-layer support, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: expand the Gradient tool with persisted mode/preset/reverse controls, radial gradient support, foreground-to-background vs foreground-to-transparent behavior, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a real History panel with visible undo/redo state lists, clickable history navigation, and dedicated snapshot controls, with focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add layer-side warp controls for Move / Free Transform with live preview/apply-cancel behavior, direct on-canvas warp handles, numeric Properties controls, renderer/export parity, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add layer-side perspective controls for Move / Free Transform with live preview/apply-cancel behavior, direct on-canvas perspective handles, numeric Properties controls, renderer/export parity, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add layer-side skew/distort controls for Move / Free Transform with live preview/apply-cancel behavior, direct on-canvas mode-aware handles, numeric Properties controls, renderer/export parity, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Video native render preview bugfix slice: preserve locally rendered composition blobs through Source Library/native scratch materialization so Electron Program Monitor previews resolve to durable `signal-loom-asset://` URLs instead of renderer-local `blob:file:///...` URLs; verify with focused regression tests, build, Android sync, and native video-render smoke.
- [x] Image selection UX follow-up: keep the generative selection-edit overlay dismissed while selection transforms/refinements are in progress unless the user explicitly reopens it, so selection workflows do not re-cover the active document after dismissal.
- [x] Image parity feature slice: add direct skew/distort controls for Transform Selection with live preview/apply-cancel behavior, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a direct on-canvas rotation handle for Transform Selection with live pointer rotation preview, shift-snap behavior, apply/cancel session reuse, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add selection rotation to Transform Selection with live preview/apply-cancel behavior, numeric Move-panel controls, undoable selection-history commits, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add direct on-canvas handles for Transform Selection with drag-to-move, eight resize handles, live session preview updates, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a real Transform Selection workflow with selection-preview bounds, numeric X/Y/W/H controls, apply/cancel behavior, undoable selection-history commits, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: expand Magic Wand and Paint Bucket with sample-all-layers and contiguous/global matching controls, wire the settings into selection/fill behavior, add focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a real Select & Mask workflow in Image with preview modes, feather/smooth/contrast/shift-edge controls, output to selection/QuickMask/layer mask/alpha channel, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add real selection refinement controls with grow/shrink/feather/border/smooth operations, undoable selection-history commits, Image Channels-panel UI, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add direct layer-mask painting with persisted layer-vs-mask edit targeting, Layers-panel/row target controls, brush and eraser mask editing with undoable mask paint history, active-mask overlay feedback on canvas, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add QuickMask-style visual selection editing with a real masked-area overlay, `Q` toggle and Channels-panel controls, brush/eraser selection painting while in QuickMask, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a real Channels panel with persisted alpha/saved-selection channels, selection-to-channel and channel-to-selection workflows, undoable document-state commits, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add real layer mask density and feather controls with persisted layer state, shared render/apply/adjustment mask processing, undoable Layers panel commits, sanitizer coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add a real Move / Free Transform apply-cancel preview session for overlay-driven rotate/resize/pivot edits, with Enter/Escape handling, on-canvas action controls, correct undo operation typing for transform vs destructive resize, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: add persistent Move / Free Transform pivot support with numeric pivot fields in the Move panel, an on-canvas pivot handle, renderer/export parity, transform-history coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: complete Move / Free Transform numeric properties by making the Move panel directly edit X/Y/W/H/rotation with undoable commits for bitmap and text layers, focused regression coverage, build verification, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Video workspace bugfix slice: make Program Monitor rendered-preview mode clearly report waiting/error states when no playable preview is available, tighten rendered-video refresh behavior, and verify with focused tests plus build evidence.
- [x] Video workspace UI follow-up: stop the centered usage estimator overlay from intercepting Program Monitor controls like `Create Composition` at default desktop sizes, with focused regression coverage and layout verification.
- [x] Image parity feature slice: add real crop properties with aspect presets and composition guides, make crop preview honor fixed aspect ratios, add focused regression coverage, and verify build, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image parity feature slice: complete the next Move / Free Transform gap by adding direct edge transform handles with correct side-resize geometry and focused UI/math regression coverage, then verify build, Android sync, native smoke, and fresh Image Playwright evidence.
- [x] Image UI regression slice: make packaged Electron startup apply the same KDE Wayland -> X11 floating-panel compatibility policy as the launcher/native smoke path, so user-facing Image undocked dialogs and palettes do not fall back onto unsupported native-Wayland child-window behavior; include focused launcher/main-process tests, build verification, Android sync, native smoke, and notes.
- [x] Image UI regression slice: reassert native floating popup size during drag for standard undocked dialogs as well as fixed compact palettes, so compositor surface drift cannot balloon Image floating panels mid-move, with regression coverage, build, Android sync, native smoke, and notes.
- [x] Image UI regression slice: reassert fixed native compact palette size during drag and passive sync so compositor popup-surface drift cannot make the Image Tools palette balloon mid-move, with screencast-guided regression coverage, Playwright 1920x1080/2560x1440 evidence, Android sync, native smoke, and notes.
- [x] Image UI regression slice: repair fixed-size native floating palette default migration so stale persisted Image Tools geometry/minimums cannot balloon the compact palette, while preserving desktop-screen placement for normal undocked dialogs; include red/green tests, Playwright 1920x1080/2560x1440 evidence, Android sync, native smoke, and notes.
- [x] Image parity feature slice: add linked layer movement groups with persisted link metadata, undoable Layers panel link/unlink controls and row badges, restore sanitization, linked drag/nudge movement, parity tracking, focused tests, build verification, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image parity feature slice: add first-class layer groups/folders with persisted group metadata, undoable create/assign/collapse controls in the Layers panel, group row badges/filters, render-time child visibility suppression, parity tracking, focused tests, build verification, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image parity feature slice: add real layer lock variants for pixel edits and position, with persisted metadata, undoable Layers panel controls/context actions, filtering/badges, enforcement in move/transform/nudge and raster-edit tools, parity tracking, focused tests, build verification, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image UI regression slice: make native external floating dialogs and tool palettes use explicit desktop-screen placement when moved outside the owning workspace window, while staying parented/off-taskbar, preserving size during move/resize, avoiding snap-back docking, with focused regression tests, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image UI regression slice: harden fixed floating tool palettes against stale persisted geometry and native child-window minimum-size bleed, so Image Tools stays compact/non-dockable/non-resizable while dragging without ballooning, with focused tests, browser visual evidence, Android sync, and native smoke verification.
- [x] Image parity feature slice: add real clipping masks to the Image layer stack with persisted metadata, undoable layer UI controls/context action, compositor behavior, parity tracking, focused tests, build verification, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image parity feature slice: add Layers panel search/filtering and color labels with persisted layer metadata, undoable label changes, parity tracking, focused tests, build verification, Playwright 1920x1080/2560x1440 evidence, Android sync, and native smoke verification.
- [x] Image UI regression follow-up: stop host/render and persisted-layout sanitation from snapping native external floating dialogs/palettes back into the owner window, while preserving docked-panel recovery bounds, with focused tests, build, Playwright 1920x1080/2560x1440 drag evidence, and native smoke verification.
- [x] Image UI regression slice: fix Image Tools native floating palette ballooning and preserve desktop/multi-monitor undocked panel geometry without owner-viewport size clamping, with focused tests, build verification, Playwright 3829x2161 browser evidence, and native Electron popup metrics.
- [x] Image parity feature slice: add histogram-aware Levels/Curves adjustment controls using the layer stack below the active adjustment, with focused tests, build verification, and Playwright 1920x1080/2560x1440 layout evidence.
- [x] Image parity feature slice: add channel switching and shadow/highlight clipping readouts to the Image Properties histogram, with focused tests, build verification, and Playwright 1920x1080/2560x1440 layout evidence.
- [x] Image parity feature slice: add a tested active-document luminance histogram to Image Properties, update the Image parity checklist from missing to partial for histogram support, and verify the panel at 1920x1080/2560x1440 without covering the document.
- [x] Development dashboard and Image parity tracking slice: restore `npm run dashboard` on localhost port 7890, surface live Current Status tasks, recent notes, and audit-grounded Image parity counts, and replace the stale five-row all-done Image Photoshop parity module with a broad truthful checklist.
- [x] Image parity workflow slice: add named Image workspace layout presets for Quick Edit, Full Suite, Retouching, Painting, Typography, and Print Prep, with TDD coverage, build verification, and Playwright 1920x1080/2560x1440 evidence that Quick Edit removes nonessential Image panels while Full Suite restores them fully onscreen.
- [x] Image parity baseline slice: clamp expanded shared context menus against the viewport using recursive submenu height estimates, with red/green regression coverage, build verification, and Playwright 1920x1080/2560x1440 bottom-edge evidence.
- [x] Image parity baseline slice: make the project usage estimator workspace-aware so Image/Paper keep it in the top chrome instead of overlaying the active document canvas, with focused tests, build verification, and Playwright 1920x1080/2560x1440 evidence.
- [x] Image workspace Photoshop/GIMP parity audit: inventory current Image tools/features, estimate parity, identify top 20 implemented-but-incomplete and top 20 missing features, verify 1920x1080 UI behavior, and write a `/goal` implementation prompt in `docs/notes/444-image-workspace-photoshop-gimp-parity-audit.md`.
- [x] Signal Loom demo project: create and browser-verify a two-page, ten-panel magic/cyberpunk hacker comic project that demonstrates multi-provider model nodes, loop/branch logic, verification, source-library packaging, and Paper layout.
- [x] Android native upscaling correction: replace the bitmap-only in-app upscaler with a one-application Local Dream-style QNN runtime path, bundling the native backend/QNN assets into the Signal Loom APK and downloading upscaler models into Signal Loom app-private storage.
- [x] Video provider integration: add Vertex AI and Atlas Cloud video provider support, audit the current Atlas Cloud video model catalog, and build first-class Video/Flow nodes for the top six Atlas Cloud video models with provider settings, cost/usage metadata, and smoke coverage. (Both shipped — Atlas Cloud video models: docs/notes/697; Vertex AI video: the `signal-loom:vertex-generate-video` IPC path.)
- [x] Android/input follow-up: replace native color inputs with an app-controlled advanced picker, add in-app Android image upscaling for Image/Paper/Flow image paths, and add cross-platform gamepad bindings with per-workspace tabs and analog/trigger controls.
- [x] UI follow-up: compact Image and Paper Tools into fixed-size undockable Photoshop-style palettes with zero icon gaps, integrated Image foreground/background swatches, and Paper eyedropper sampling support.
- [x] UI/packaging audit slice: make Image Tools a two-column floating-only Photoshop-style palette with a draggable handle, verify 1080p+ layout geometry, sync/build Android, create a Windows NSIS installer, and add a documented macOS build process with a Linux ZIP fallback.
- [x] Cross-app production hardening slice: make normal Source Library project imports create a durable shared Project imports envelope, sync across Flow/Video/Image/Paper, and persist through save/open.
- [x] Cross-app production hardening slice: preserve Paper OS File Manager image-drop Page N import envelopes through Flow, Video, Image, and Paper Source Libraries, including Video envelope-label display and native multi-window smoke coverage.
- [x] Cross-app production hardening verification: run the native release gate after the Flow schema parity Electron manifest change, and fix any native `.sloom`, Source Library, Paper, Video, or multi-window regression it exposes.
- [x] Cross-app production hardening verification: run the local release gate after the Flow schema parity and toolbar changes, and fix any build, lint, unit, or browser-smoke regressions it exposes.
- [x] Cross-app production hardening slice: add a Flow node schema parity gate so runtime node types, node catalog entries, renderer project validation, Electron `.sloom` validation, and usage-ledger sanitization stay in sync.
- [x] Cross-app production hardening slice: add configurable native real-project soak performance budgets for heap, DOM nodes, event listeners, and per-cycle duration, with dashboard-visible pass/fail evidence.
- [x] Cross-app production hardening slice: run an extended native real-project soak against the restored Chronicle `.sloom`, exercising Flow/Video/Image/Paper workspace switching and Source Library churn with dashboard-visible evidence.
- [x] Cross-app production hardening verification: run the native real-project soak and Paper PDF parity gates against the restored Chronicle `.sloom` so the dashboard Native Gate Evidence covers multi-window Source Library churn and real Paper PDF visual parity artifacts.
- [x] Cross-app production hardening verification: run the native real-project PDF smoke against the restored Chronicle `.sloom` so the dashboard Native Gate Evidence includes current Paper PDF export proof from a real project.
- [x] Cross-app production hardening verification: confirm OS File Manager image drops onto Paper pages populate Page N imports Source Library envelopes, place linked frames, and persist through native save/open.
- [x] Cross-app production hardening verification: refresh the native Video render smoke artifact so the dashboard Native Gate Evidence card has current desktop render proof instead of a missing video-render report.
- [x] Project development dashboard slice: treat docs/status-only verification commits as non-implementation commits for active-workstream checkpoint freshness so the dashboard does not show false stale warnings after status updates.
- [x] Cross-app production hardening verification: reverify OS image file drops on Paper pages create Page N imports Source Library envelopes, place linked page frames, and survive native save/open round-trip.
- [x] Project development dashboard slice: align the Vite app dev server port with the dashboard service URL and show an offline start command when the app server is down.
- [x] Android accelerator companion productionization slice: include exact redacted rerun commands in stale Android Local Dream gate contract warnings so dashboard remediation is copyable.
- [x] Project development dashboard slice: stack Android Local Dream stale-gate warnings so contract-stale evidence still explains when the current prepared fork is source-only or otherwise not live-validation ready.
- [x] Android accelerator companion productionization slice: mark Android Local Dream gate evidence stale when a passed `/v1/capabilities` report lacks current one-app contract markers such as setup proof.
- [x] Android accelerator companion productionization slice: add a guided source-only setup mode to `npm run setup:android-localdream` so no-device fork refreshes record dashboard evidence without bypassing the setup workflow.
- [x] Cross-app production hardening slice: run the native stress gate after Paper OS-drop page round-trip coverage so repeated Source Library churn still passes with the stronger Paper import smoke.
- [x] Cross-app production hardening slice: extend Paper OS image-drop native evidence to page-specific Page N imports and save/open durability for imported linked frames.
- [x] Cross-app production hardening slice: add repeatable native/Electron smoke evidence for Paper OS image file drops into Page N imports envelopes and linked page frames.
- [x] Android accelerator companion productionization slice: refresh the ignored source-only prepared Local Dream fork after setup-proof patch changes and verify the dashboard readiness evidence no longer reports stale source hooks.
- [x] Android accelerator companion productionization slice: surface exact prepared-fork regeneration guidance on the dashboard when verifier evidence is stale after setup-proof patch changes.
- [x] Android accelerator companion productionization slice: persist in-app Test API/Test Generate/Test Upscale setup proof into `/v1/capabilities` and require it for replace-package production gate readiness.
- [x] Android accelerator companion productionization slice: include exact redacted replace-package rerun commands in capabilities readiness failure evidence so dashboard remediation is copyable after first-run setup.
- [x] Project development dashboard slice: capture compact redacted stdout/stderr for release-gate steps and render those diagnostics in the dashboard release-gate ledger.
- [x] Android accelerator companion productionization slice: summarize `/v1/capabilities` gate evidence with downloaded model/upscaler counts, mode, and warnings so the dashboard stays readable during live validation.
- [x] Android accelerator companion productionization slice: make the Android Local Dream production gate parse `/v1/capabilities` and fail early with a clear first-run redownload/in-app smoke-test message when the replacement app has no downloaded model or upscaler.
- [x] Android accelerator companion productionization slice: make replace-package guided setup stop at a first-run redownload and in-app smoke-test checkpoint before production gate validation, so the replacement app is not asked to generate/upscale before its own model and upscaler downloads exist.
- [x] Android accelerator companion productionization slice: add in-app Signal Loom generation and upscale smoke-test controls on the Android setup screen so users can prove the one-app companion can run production operations before returning to desktop workflows.
- [x] Android accelerator companion productionization slice: add an in-app Signal Loom gateway self-test on the Android setup screen so users can verify `/v1/capabilities` from the companion app before trying desktop generation or upscaling.
- [x] Project development dashboard slice: mark Android Local Dream gate evidence stale or blocked when the current prepared fork is source-only/not-ready so old passing live-gate reports do not imply the current one-app companion is live-validation ready.
- [x] Android accelerator companion productionization slice: show Local Dream runtime asset readiness on the Signal Loom Android setup screen so replace-package users can distinguish APK/runtime asset readiness from model and upscaler download readiness.
- [x] Android accelerator companion productionization slice: show live model/upscaler download progress, extracting, success, and failure states on the Signal Loom Android setup screen so replace-package users can monitor required asset redownloads from the one-app companion.
- [x] Android accelerator companion productionization slice: make model downloads first-class on the Signal Loom Android setup screen, with per-model download controls and verifier coverage so replace-package users can redownload required model data inside the one-app companion.
- [x] Project development dashboard slice: surface guided Android Local Dream setup evidence so dry-run previews, destructive replace-package confirmation blocks, step outcomes, report paths, and linked gate evidence stay visible without adding dashboard-run mutation controls.
- [x] Cross-app production hardening slice: surface native Video segment assembly success/fallback metadata in the Program Monitor so users can tell whether cached spans were actually assembled or a full-output fallback was used.
- [x] Cross-app production hardening slice: import OS-dropped image files on Paper pages into a Page N imports Source Library envelope and place linked image frames automatically.
- [x] Cross-app production hardening slice: reconcile the screencast-confirmed external floating popup size-drift fix into the active implementation branch without regressing dock-to-floating drag continuation.
- [x] Cross-app production hardening slice: clean up Video render-cache contract language so executable native artifact assembly is described honestly without claiming true dirty-span-only rendering.
- [x] Cross-app production hardening slice: fix dock-to-floating drag continuation so Flow Bookmarks and other docked panels keep following the pointer after detaching, without expanding to full dock height.
- [x] Cross-app production hardening slice: fix Image workspace right-sidebar dock resizing so panel order stays stable, the column width actually changes, and undocked dialogs preserve size while being moved.
- [x] Cross-app production hardening slice: execute native Video segment assembly by concatenating materialized reusable cached spans with newly rendered dirty-span artifacts when a safe assembly plan is available.
- [x] Cross-app production hardening slice: add native Video segment assembly planning for concatenating reusable cached segment artifacts with newly rendered dirty-span artifacts.
- [x] Cross-app production hardening slice: fix the Flow top toolbar hit-testing/layout overlap where node category controls sit under the top navigation layer at desktop widths, blocking normal click interaction.
- [x] Cross-app production hardening slice: persist returned native Video segment artifacts through media composition, Flow execution metadata, and Video render-cache storage so dirty spans can become reusable cached assets after render.
- [x] Cross-app production hardening slice: wire native Video segment artifact execution into an opt-in `/render` JSON response while keeping default raw MP4 responses unchanged, with focused tests, real service smoke, and dashboard verification.
- [x] Cross-app production hardening slice: add a native Video segment extraction executor helper that turns extraction plans into serializable artifact payloads, with focused tests and dashboard verification.
- [x] Cross-app production hardening slice: carry native Video render segment extraction plans into validated render jobs, with focused tests and dashboard-visible progress toward executable partial render artifacts.
- [x] Cross-app production hardening slice: add native Video render segment extraction planning for dirty spans, with focused tests and dashboard-visible progress toward true partial render reuse.
- [x] Cross-app production hardening slice: retain proven reusable Video render-cache segment artifacts after successful renders instead of clearing the whole artifact map, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make Video render-cache assembly manifests inspectable in the Program Monitor, with reusable/dirty span detail, focused tests, and browser verification.
- [x] Cross-app production hardening slice: add a native Video render assembly-manifest boundary for reusable cached spans and dirty spans, with focused tests and browser verification.
- [x] Cross-app production hardening slice: design and implement the next Video render-cache architecture step for changed-span reuse planning, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make Video render-cache misses explain stale full-composition invalidation instead of showing a misleading initial render plan, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make Video render-cache signatures include text clip content, audio clips, stage objects, canvas/export settings, and cache-safety guards, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make Video Render reuse the previous rendered preview when no timeline spans have changed, with persisted cache signatures, focused tests, and browser verification.
- [x] Cross-app production hardening slice: keep undocked/floating panels and dialogs from changing size while being dragged, with focused tests and browser verification.
- [x] Cross-app production hardening slice: restore a usable default Video docked layout with visible source/program monitors above a resizable timeline, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make Flow variable autocomplete offer explicit all-items tokens for list/envelope variables, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make project usage-ledger entry IDs include the resolved operation for durable auditability, with focused tests and browser verification.
- [x] Cross-app production hardening slice: align project save/open validators and usage-ledger node sanitizers with newer Flow node types, with focused tests and browser verification.
- [x] Cross-app production hardening slice: make the Flow Crop Image node source preview crop box match the actual cropped output, with focused tests and browser verification.
- [x] Project development dashboard slice: surface active workstream checkpoint freshness/staleness when the committed checkpoint metadata lags behind the latest git commit, with focused tests and browser verification.
- [x] Cross-app production hardening slice: add a visible variable reference rail to the Flow expanded prompt editor so declared result/list/envelope variables can be inserted without memorizing syntax, with focused tests and browser verification.
- [x] Cross-app production hardening slice: surface the configured Video render backend in the Program Monitor so GPU/native/browser render intent is visible before export, with focused tests and browser verification.
- [x] Cross-app production hardening slice: block Video renders when export readiness reports missing media errors, with focused tests and browser verification.
- [x] Cross-app production hardening slice: add a persistent Video export-readiness indicator for missing timeline/source media and honest dirty-span analysis status, with focused tests and browser verification.
- [x] Cross-app production hardening slice: keep Paper preflight readiness visible from the main topbar even when the Preflight panel is closed, with first-issue navigation, focused tests, and browser verification.
- [x] Cross-app production hardening slice: make Video Program Monitor chroma key visible in the live preview so green-screen clips have preview/export parity instead of an export-only disclosure.
- [x] Project development dashboard slice: add an active implementation workstream ledger and dashboard card so in-progress changes, checkpoints, verification steps, and next implementation slices stay visible while development continues.
- [x] Cross-app production hardening slice: add a deterministic native Video render smoke gate that launches Electron, clicks the Video Render control, verifies a rendered preview, captures a real MP4 artifact from the native render service, and surfaces the evidence on the dashboard.
- [x] Cross-app production hardening slice: make frontend project parsing/restore tests work in isolated git worktrees by removing reliance on ignored local `.sloom` fixtures.
- [x] Project development dashboard slice: surface Android companion readiness by checking ADB availability, authorized device state, Local Dream package/split visibility, package mode, and Android SDK hints.
- [x] Project development dashboard slice: surface JSON health details for local services, especially native render available backends and recommended AMD VAAPI acceleration status.
- [x] Android accelerator companion productionization slice: add a no-mutation Local Dream fork doctor/preflight mode that checks required tools, ADB device readiness, installed Local Dream APK/splits, package mode, and Android SDK hints before clone/patch/build steps.
- [x] Project development dashboard slice: persist release-gate step summaries as a local JSON ledger and surface the latest gate run on the dashboard alongside native artifact evidence.
- [x] Project development dashboard slice: surface native release-gate evidence, artifact paths, real-project readiness, soak metrics, and Paper PDF parity reports so development status stays observable while hardening continues.
- [x] Cross-app production hardening slice: run the native release gate after the recent hardening work and fix any startup, save/open, multi-window, Source Library, soak, or Paper PDF parity regressions it exposes.
- [x] Cross-app production hardening slice: run the local release gate after dialog/startup hardening and fix any build, lint, unit, or browser-smoke regressions it exposes.
- [x] Cross-app production hardening slice: suppress known benign ResizeObserver loop browser noise at startup without hiding real application errors, with tests and browser verification.
- [x] Android accelerator companion productionization slice: add an explicit Local Dream fork replace-package build mode and user-approved uninstall/reinstall/redownload documentation so the one-app Signal Loom Android path can be packaged without relying on Play Store private model data.
- [x] Cross-app production hardening slice: replace remaining blocking browser alerts with a shared themed alert dialog/status path across Project, Flow Source Library, Image, Video, and Paper, with tests and browser verification.
- [x] Cross-app production hardening slice: replace the Image editor Text tool browser content prompt with the themed async text-input dialog, with tests and browser verification.
- [x] Cross-app production hardening slice: replace Flow Image node browser delete confirmation with the themed async confirmation dialog, with tests and browser verification.
- [x] Cross-app production hardening slice: replace Paper workspace browser text prompts with the themed async text-input dialog for new documents, source envelopes, and parent pages, with tests and browser verification.
- [x] Cross-app production hardening slice: replace Flow Source Library browser text prompts with a themed async text-input dialog for creating and renaming source bins, with tests and browser verification.
- [x] Cross-app production hardening slice: replace remaining Flow blocking native confirmations with the themed async confirmation dialog for node deletion and run-cost/envelope guardrails, with automation-safe tests and browser verification.
- [x] Activity Trail review hardening: expand direct workspace action coverage across Flow/Video/Image/Paper, strengthen sensitive-data redaction, broadcast clear events across windows, and document browser verification.
- [x] Cross-app production hardening slice: add a unified Activity Trail that records recent app/menu/palette commands and key workspace actions across Flow, Video, Image, and Paper, with command-palette access, clear status labels, bounded persistence, and browser verification.
- [x] Adobe hardening Phase 6 UX workflow command palette: add a global searchable command palette with workspace-aware commands, keyboard access, panel/project/settings actions, and browser verification for discoverability across Flow, Video, Image, and Paper.
- [x] Adobe hardening Phase 4 Image editable-document durability: clone mutable layer bitmaps/masks on duplicate, preserve vector/source-linked layer metadata and lightweight Image snapshots through project save/open sanitization, and verify browser-level Image workspace persistence behavior.
- [x] Adobe hardening Phase 2 Source Library ACK/version repair: retry failed native Source Library ACKs with authoritative snapshots, detect native version gaps, show degraded sync state with manual repair, and publish same-id asset updates across windows.
- [x] Adobe hardening Phase 2 opaque native asset URLs: use `signal-loom-asset://asset/<id>` for new native Source Library media, keep legacy `signal-loom-asset://file/<path>` compatibility, and verify real protocol access still rejects unregistered and symlink-escaped media.
- [x] Adobe hardening Phase 2 native asset protocol abuse smoke: exercise the real Electron `signal-loom-asset://` handler with registered Source Library assets, unregistered scratch files, and scratch symlink escapes.
- [x] Adobe hardening Phase 2 native asset capability slice: register active Source Library native asset paths, reject unregistered `signal-loom-asset://` protocol reads, and cover the capability registry with focused tests.
- [x] Add a local Signal Loom project development dashboard that tracks active implementation status, git/checkpoint state, task progress, notes, verification commands, and local service health during development.
- [x] Run an Adobe-style Signal Loom product audit with sub-agent review across architecture, media/performance, UX/workflow, and QA/release readiness; document strengths, must-improve risks, and roadmap recommendations.
- [x] Add production-oriented Video clip effects: chroma key/green screen, per-clip stroke/border, expanded filter effects, preview/export parity, and incremental render dirty-region signatures.
- [x] Rework the Video workspace default layout so Source and Program monitors are visible side by side above a usable resizable timeline, add drag handles between docked panels, and fix undocked panel/window growth while dragging.
- [x] Add an expandable large editor for Flow Text prompt node text areas so longer prompts can be viewed and edited at once.
- [x] Add Flow prompt variables so generated attempts, lists, and envelopes can be named and referenced in prompt/template text with autocomplete and indexed list access.
- [x] Fix the Video workspace dockable layout regression and shared undocked panel/window growth while dragging.
- [x] Add a large popup mask-painting workspace inside Flow image inpaint/outpaint nodes, with connected-mask priority and node-owned painted masks for mask-aware providers.
- [x] Add a Flow Crop Image node that accepts one image, crops it locally, and outputs the cropped image for downstream image/video/reference inputs.
- [x] Add Paper page assets to the Video workspace so flattened Paper pages can be browsed, dragged to the timeline, and used as robust storyboard/reference media.
- [x] Add a Flow Color Swatch node that builds reusable palettes and feeds image/video nodes textual palette guidance for visual consistency.
- [x] Flow node/edge system audit and function/group node pass: add first-class reusable Function and Group nodes, configurable function input/output bindings with flow/constant/expression modes and transform chains, selected-node collapse-to-function, Flow copy/cut/paste/delete/select-all shortcuts, runtime/cost/source-list routing for function outputs, and focused regression coverage.
- [x] Fix the user-reported Flow Canvas startup recovery-boundary crash caused by image-node/store update feedback loops.
- [x] Fix Paper speech/thought bubble text-box movement so the bubble text remains visible while the text box drag/resize layer is active.
- [x] Complete the first production stability gate by auditing unstable store selectors, adding no-op write regressions, and hardening Paper/Image store updates that previously emitted unchanged state.
- [x] Add the first production smoke harness: `npm run test:smoke`, workspace/source-library smoke contracts, and a browser/Electron QA checklist.
- [x] Run a production-readiness audit across Flow, Video, Image, and Paper, documenting crash/freeze risks, incomplete UX flows, missing tests, performance hazards, and standards that must improve beyond the current repository baseline.
- [x] Paper: support connected/chained speech and thought bubbles for multiple utterances by one character, including connector lines, editable chain ordering, export/PDF/flatten parity, and intuitive canvas controls.
- [x] Paper/Image text UX: allow direct in-workspace text editing for text boxes/bubbles/titles/subtitles, strengthen typography/effects for cover-art and comic production, and add copy-style/paste-style for Paper frame/text/border/opacity styles.
- [x] Add a comic-book sound-effects plugin for onomatopoeia text such as screech, bang, kapow, and whirrrrr, with a designer dialog that previews the final effect before placement, custom text/effect controls, preset application, trailing-off effects, warps, fills, strokes, halftone/speed effects, reusable styles, and Paper/Image integration.
- [x] Paper: add style copy/paste, text stroke/shadow/skew/scale rendering and inspector controls, plus Paper toolbar/context-menu comic SFX preset insertion.
- [x] Paper: add a right-click quick image edit flow that lets users describe an edit directly in the Paper workspace, runs the selected cloud/local image-edit provider, saves the result to the Source Library, and replaces the selected image frame without destroying the original asset.
- [x] Image editor: add provider/model-aware selected-region operation controls, reference descriptions/image URLs, search/outpaint controls, per-operation cost labels, and undoable image-size/canvas-size/upscale document controls without requiring live provider credentials during development.
- [x] Image editor: add Photoshop-grade selected-region editing with paint/select/mask workflows, multiple reference inputs, region prompts, seamless edge integration, upscaling, custom document sizes, image resize, and canvas resize.
- [x] Image provider platform: complete capability-specific nodes for each selected cloud/API provider, expose each model's full supported controls, show realistic per-generation cost before execution, and document sign-up/config/use in built-in wiki/help.
- [x] Cross-app production hardening slice: revoke Source Library blob/object URLs when scratch/fallback media items are removed or replaced by project restore/hydration so long-running Flow/Video/Image/Paper sessions do not leak preview URLs.
- [x] Cross-app production hardening slice: add guarded Electron automation paths for native `.sloom` open/save, native media import, Paper PDF export, and Paper webcomic image-directory export so release-gate smoke can run without blocking on GUI file dialogs.
- [x] Comic SFX designer refinement: make the Paper/Image sound-effect plugin preview-first and expose every effect parameter already supported by the SFX model, including trail scale, burst stroke, speed-line geometry/opacity, and halftone radius/opacity.
- [x] Image Comic SFX editability: retain placed SFX designer recipes on Image layers and allow selected SFX layers to reopen the designer for non-destructive rerasterized updates instead of forcing a new bitmap placement.
- [x] Cross-app production hardening slice: fix the native Paper PDF hidden-window readiness hang so Electron automation smoke completes `.sloom` save/open, media import, Paper PDF export, and named webcomic PNG directory export.
- [x] Cross-app production hardening slice: add `npm run smoke:native` as a repeatable Electron release-gate script for native `.sloom` save/open, media import, Paper PDF export, Paper PNG directory export, all-workspace window opening, shared native Source Library snapshot visibility, file signature checks, and recovery-boundary detection.
- [x] Cross-app production hardening slice: prevent renderer project-restore startup from republishing stale Source Library snapshots over newer native bridge changes, and prove a native Source Library item becomes visible in Flow, Video, Image, and Paper windows.
- [x] Cross-app production hardening slice: add `npm run smoke:native:stress` for repeated native Source Library add/rename/remove churn from rotating Flow/Video/Image/Paper windows, with per-cycle snapshot/UI convergence and recovery-boundary checks.
- [x] Cross-app production hardening slice: add `npm run smoke:native:project` for isolated real `.sloom` project startup/window/Paper PDF export smoke against Chronicle-sized projects instead of only synthetic tiny projects.
- [x] Cross-app production hardening slice: fix native project-open recovery when a `.sloom.bak-*` backup points at a missing derived scratch folder, so real project backups can fall back to the saved scratch directory instead of throwing ENOENT.
- [x] Cross-app production hardening slice: protect `.sloom.bak-*` backup files from destructive normal Save by refusing direct backup writes, defaulting Save/Save As to a restored `.sloom` copy path, and avoiding backup paths in remembered startup state.
- [x] Paper PDF parity: route the default native Paper PDF export through flattened page snapshot images so PDF text/bubbles match the Paper/webcomic raster output instead of using a separate live HTML text layout path.
- [x] Paper PDF export: add a persisted raster preset for default desktop PDF export, with Balanced JPEG, Print PNG, and Proof JPEG modes so users can choose realistic file-size/quality tradeoffs instead of always producing huge lossless raster PDFs.
- [x] Cross-app production hardening slice: isolate native smoke runs in a temp Electron user-data directory so release-gate automation does not overwrite the user's remembered project/settings state.
- [x] Paper workspace usability follow-up: make comic sound effects place as a single editable decal object, size short SFX text safely by default, pin the Document / Export toolbar as a static top bar, remove wasted Paper titlebar brand space, and make Paper Inspector sections collapsible.
- [x] Paper titlebar workspace-space follow-up: move the Paper Document / Export toolbar into the existing app topbar slot instead of a separate row, reclaim the vertical canvas space, and keep Paper side panels aligned directly under the topbar.
- [x] Paper top bar layout condensation: condense the 17 individual export buttons inside the Paper workspace top bar (PaperTopStrip) into a unified, glassmorphic pop-out drawer dropdown.
- [x] Image editor context menu: fix the right-click context menu so it opens at the mouse cursor instead of pinning to the top of the window, with a regression test.
- [x] Dockable UI stability: fix undocked dialog dragging so panel dimensions stay constant while moving, and stabilize Image Layers/Properties right-dock column resize behavior so docked panels do not jump or resize unpredictably.
- [x] Image workspace Source Library regression: clicking a Source Library image should open it in the Image editor again, and dragging an image from Source Library onto a blank Image canvas should create/open an editable image document.
- [x] Chronicle project recovery: create a non-destructive rebuilt Paper `.sloom` copy from the recovered editable project and exported PNG references, preserving cover/crop image transforms, tightening text fit, auditing bubble tails, and adding conservative chained bubbles where same-speaker utterances are consecutive.
- [x] Cross-app production hardening: umbrella DECOMPOSED 2026-07-02 into the concrete open slices below (the mission statement itself is not executable; ~60 completed slices in this file are its history).
- [x] Cross-app production hardening slice (P0, DATA LOSS) — RESOLVED 2026-07-02: the main save/open wipe was already fixed by `2678f8c` (2026-06-20, ImageLayerProjectPixels bitmapData codec through exportProjectSnapshotWithPixels/restore + sanitize pass-through — the bug memory was stale within hours of being written); the residual hole is now closed by `5b1372d`: restoreProjectDocument's error rollback used a pixel-STRIPPED snapshot, so any failure after the image section (e.g. native source-library reconcile throwing) still blanked live canvases — rollback now restores the exact live document objects via the sanitize-free restoreLiveProjectRollback (red-first test proves the old code fails it). Remaining nicety: a native-smoke assertion that a saved+reopened .sloom keeps Image pixels (owner re-verification in the app also pending).
- [ ] Cross-app production hardening slice: shrink `.sloom` files by externalizing embedded base64 assets (97–98% of file size per the Root & Relay/WnM measurements) into scratch-referenced or portable-copy storage with backward-compatible open of legacy embedded projects; measure before/after on a real project.
- [ ] Cross-app production hardening slice: long-running AI operation lifecycle — audit cancellation, retry/backoff, and orphaned in-flight state when a workspace window closes mid-generation across providers; assert no stuck spinners or ledger entries after window close.
- [ ] Cross-app production hardening slice: startup time-to-interactive budget for Root & Relay-sized projects — measure cold open in the native soak harness, set a budget, and fix the top offender.
- [x] Flow multi-workspace milestone 1: add baseline metrics, `flowWorkspaces` project migration, a Flow workspace registry, topbar workspace switching UI, and active-runtime unload/hydrate behavior so inactive Flow workspaces release memory instead of only hiding UI.
- [x] Flow multi-workspace milestone 2: scope Flow multi-window commands and usage tracking by Flow workspace id so multi-window actions and telemetry target the correct Flow workspace instance.
- [x] Flow large-project Source Library slice: add a reusable source asset handle pool and virtualize the generated-pool list so large projects mount fewer sidebar rows while asset URL lifetime is managed intentionally.
- [x] Flow large-project Source Library slice: flatten Source Library bin/envelope rows and virtualize regular bin contents so large Project imports envelopes do not mount every card at once.
- [x] Flow large-project import pipeline slice: add a worker-backed native media normalization contract so Electron import-menu file classification stops doing batch kind/mime inference inline on the main thread.
- [x] Flow app modularity slice: extract a dedicated Flow workspace shell, isolate Flow canvas drop-import logic, and add smaller flow runtime/document store boundaries without changing current behavior.
- [x] Flow multi-workspace acceptance slice: harden native real-project smoke/soak automation with isolated X11 Electron launches, CDP retry handling, direct renderer-command Paper export, and explicit workspace-switch / renderer-heap budget evidence.
- [x] Turn the existing Flow multi-workspace and performance design note into an execution-ready implementation roadmap with ordered slices, concrete file targets, and release-risk gates.
- [x] Cross-app production hardening slice: recover missing Source Library entries from Paper frame scratch assets during native `.sloom` open so existing Paper layouts keep their placed images after reopen.
- [x] Cross-app production hardening slice: add a repeatable native real-project soak gate that opens a restored `.sloom` in an isolated Electron profile, cycles Flow/Video/Image/Paper, churns Source Library changes across workspace windows, records CDP performance metrics, and writes a JSON evidence report.
- [x] Paper/PDF parity hardening slice: add a native PDF parity evidence gate that captures overlay-free editor page screenshots from a restored `.sloom`, exports the current raster PDF path, rasterizes matching PDF pages, creates diff images/metrics, and documents the output package.
- [x] Paper export: add webcomic PNG/page-image export alongside PDF with output resolution/quality controls, project/page-based file naming inside a named directory, and fix Paper PDF text layout/font-size parity against the editor.
- [x] Fix Paper click-to-create frame tools so a plain click creates and selects a normal-sized speech/thought/text/image frame instead of losing the interaction or creating only a minimum-size frame.
- [x] Fix release-gate workspace interaction blockers: Image `Open image` imports local raster/SVG/TIFF/GIF files without shared-panel hit-test interception, and Video Program Tools `Text` remains clickable with compact monitor controls and auto-creates a composition when needed.
- [x] Add production advanced image provider support for BFL FLUX.2, Stability AI edit/generation APIs, and Local/Open image endpoints with capability-aware Image node controls, mask input, cost telemetry, setup/help docs, and adult-capable provider research safeguards.
- [x] Add user-friendly persistent provider and production-output configuration, including Vertex environment/project setup, print-ready Paper upscaling method defaults, and built-in setup instructions.
- [x] Finish the large-project performance caveats by syncing the Source Library through the Electron native bridge and virtualizing offscreen Paper spreads/pages.
- [x] Add large-project performance hardening with a pre-change snapshot, `.sloom` media reference normalization, a Source Library service contract, and lazy media preview loading for large Source Libraries/Paper documents.
- [x] Add Vertex AI gcloud ADC image support for Gemini/Imagen image models, including Imagen 4 Fast/Generate/Ultra model choices, desktop-native token handling, and Image workspace Gemini generative-fill routing through Vertex mode.
- [x] Add provider/model-aware image aspect-ratio support so Gemini image nodes and config nodes can request 2:3, 3:2, 3:4, 4:3, 4:5, 5:4, 9:16, 16:9, 21:9, and model-specific Gemini 2.5-safe subsets.
- [x] Fix intermittent `.sloom` project-open failures when IndexedDB cannot open its local asset backing store by falling back to saved file-backed Source Library asset URLs.
- [x] Generate and QA Issue #1 headless comic panel art, preserve generated media in the `.sloom` source library, and append the generated pages to the Paper workspace.
- [x] Fix Paper Cover/Crop so image frames do not fall back to stretch when image pixel metadata is missing, and hydrate natural image dimensions from loaded images.
- [x] Add Paper image-frame right-click print upscaling that creates a new source-library asset sized for the current document DPI and selected frame placement.
- [x] Paper print AI upscaling: replace the local/Vertex-only right-click print upscale path with a cost-aware dialog that supports Stability AI cloud upscalers, Vertex Imagen, local browser final fitting, and exact-DPI source-library replacement while preserving crop/fit/scale.
- [x] Android accelerator integration slice: research Local Dream/Snapdragon NPU paths, document the companion API, add a buildable Android gateway scaffold, add persistent Android accelerator settings, add a tested Signal Loom client, and route Paper print upscaling through a zero-provider-cost Android NPU/GPU method when configured.
- [x] Android accelerator gateway device validation: install the scaffold APK on the attached Galaxy device, grant notification permission, start the foreground LAN server, verify bearer-token auth, CORS preflight, and `/v1/capabilities` through `adb forward`.
- [x] Android accelerator live Local Dream bridge validation: detect the installed Local Dream backend over phone loopback, expose `local-dream-active`, convert Local Dream SSE/raw-RGB generation output into Signal Loom JSON/PNG, and verify real QNN-backed generation plus 4x upscaling through the Signal Loom gateway.
- [x] Make the configured Android phone upscaler the universal Image/Photos upscale path, with local fallback and visible method/status feedback; add a Flow image-generation auto-upscale toggle that uses the currently configured upscaler and includes paid upscaling in pre-run cost estimates.
- [ ] (superseded 2026-07-02 — owner directive, see docs/notes/802 + the Current Status entry) Android accelerator companion productionization: the companion-app direction is dead; Local Dream inference gets embedded INTO the Signal Loom Android app itself (no separate app at all), first cut = in-app localhost service speaking the desktop `/v1/upscale` protocol.
- [x] Android accelerator companion productionization slice: add a guided setup command for the Local Dream based Signal Loom companion that can run prepare/build/install/start/live gate with correct side-by-side and replace-package APK paths.
- [x] Android accelerator companion productionization slice: add a no-device source-only Local Dream fork preparation mode and tighten prepared-fork verification so build/live readiness requires extracted runtime assets.
- [x] Android accelerator companion productionization slice: start the generated Local Dream Signal Loom gateway through an app-owned exported activity intent so the production gate can launch the non-exported foreground service without weakening service export security.
- [x] Android accelerator companion productionization slice: make the Android Local Dream gate discover or configure the local Android SDK so the generated fork Gradle build can run without manual `local.properties` edits.
- [x] Android accelerator companion productionization slice: add a repeatable Android Local Dream production gate that records doctor, prepared-fork verification, build/install/start, adb-forward, and `/v1` API smoke evidence for dashboard review.
- [x] Android accelerator companion productionization slice: surface prepared Local Dream fork verifier status on the development dashboard so missing, failing, and ready one-app fork gates are visible beside ADB/package readiness.
- [x] Android accelerator companion productionization slice: add a no-mutation prepared-fork verifier that confirms the generated Local Dream fork contains the Signal Loom gateway service, screen, NSD advertising, integrated capabilities, backend startup hooks, and model/upscaler repository access before build/install.
- [x] Android accelerator companion productionization slice: add NSD discovery advertising, setup-screen copy/open/refresh controls, visible bridge status, foreground-service running status, and live ADB validation while the same-UID Local Dream fork remains open.
- [x] Android accelerator Paper export validation: run Paper print-upscale replacement through the phone upscaler against an actual Paper frame, save the replacement asset, and capture print/web export evidence.
- [x] Android accelerator/Paper print finalization status follow-up: show provider-specific Paper batch progress, expose visible Android companion job status, and re-queue stale print-upscaled assets when the current frame/document DPI target needs more pixels.
- [x] Android accelerator/Paper quick-upscale failure fix: replace browser multipart phone upscale uploads with JSON image payloads, add Android JSON decode support, and preflight Local Dream bridge availability before sending image bytes.
- [x] Android accelerator bridge diagnostics: verify the Local Dream SDXL NPU backend brings 127.0.0.1:8081 online, prove companion upscaling and generation pass through it, and preflight Android image generation before posting prompt jobs when no phone model is reachable.
- [x] Paper print AI upscaling: research CPU-safe local AI upscaler options for this workstation and keep the Local CPU AI option gated unavailable until a tiled native helper/runtime can run without out-of-memory crashes.
- [x] Paper KDP comic export wizard: after print-quality upscaling is complete, export KDP-ready comic interior/cover assets with page-role mapping, trim/bleed/DPI validation, print-upscale preflight, and user-friendly configuration.
- [x] Flow project spend tracker: persist project-level usage/cost ledger data in `.sloom`, broken down by provider/model/operation/workspace, show total project spend across sessions, and add live credit/balance lookup where provider APIs support it.
- [x] Make the Source Library live-sync generated/imported/edited assets and removals across Flow, Paper, Image, and Video workspace windows.
- [x] Fix native startup so it fully restores the remembered `.sloom` project with assets or starts from a clean blank project, with a startup splash while loading.
- [x] Cross-app production hardening slice: stop blank native startup from re-running on every App callback churn, so `Starting New Project` no longer loops `resetProjectDocument()` and Source Library native sync forever.
- [x] Keep undocked dialogue windows stacked above their owning workspace window by parenting and raising external floating popup windows.
- [x] Finish the theme implementation pass across shared menus, toolbars, dockable panels, workspace fallbacks, and external floating panel documents.
- [x] Fix Source Library item renaming so right-click Rename and the T button use an inline editor and sync renamed labels across workspace windows.
- [x] Fix undocked dialog dragging so external floating windows stay anchored under the cursor instead of reopening/jumping during pointer movement.
- [x] Add 20 interface theme variations with settings previews, theme-aware shell chrome, and regression coverage.
- [x] Fix dockable Bookmarks collapse so it releases its full dock width like Source Library, and keep undocked Tool Properties visible if its external floating window disappears.
- [x] Fix multi-window Source Library flicker after opening Paper by replacing whole-store BroadcastChannel snapshot sync with merge-only command messages and Flow-owned source ingestion.
- [x] Split Flow/Video/Image/Paper into separate Electron workspace windows, move Add Nodes into the fixed top Flow toolbar area, scope menus/hotkeys per focused window, and support monitor-level floating dockable dialogs/panels.
- [x] Restore generated Flow node result/history/envelope media from reopened `.sloom` Source Library assets so prior batches stay usable on the workspace.
- [x] Add Source Library item context menus with rename actions and collapsible envelope groups.
- [x] Add visual thumbnails to image/video Expander selections.
- [x] Preserve current Flow zoom when portals or bookmark waypoints recenter the workspace, and let connector drags jump to bookmarks.
- [x] Fix Source Bin image imports on Flow project reopen so dropped media nodes keep using the restored Source Bin asset instead of losing their preview/output.
- [x] Change source-bin ingestion so flow-generated media only lands in the Source Library when explicitly routed through source-bin connected flow outputs (including envelope-backed outputs), instead of auto-creating per-output source-bin envelopes for every generation.
- [x] Fix Paper PDF flattening so speech/thought bubble artwork survives raster export instead of exporting only the bubble text.
- [x] Fix raster Paper spread PDF export so print outputs do not include editor/proof-only guides, red gutters, or blank-page labels.
- [x] Fix the Paper speech-bubble arc regression so the bubble body stays inside the resize bounding box while only the tail can extend outside it.
- [x] Fix Paper PDF export fidelity by rasterizing page-based PDF output from flattened page snapshots and tighten speech-bubble paths so stems render as continuous outlines without seam artifacts.
- [x] Add unified keyboard shortcut routing across Flow/Video/Image/Paper, including Paper/Image undo-redo-copy-cut-paste/delete actions and visible hotkey labels in the global menus.
- [x] Add Paper professional print-production controls for PDF target/output intent, ink limit, black/spot policy, package/PDF metadata, and honest preflight warnings for browser PDF proof limitations.
- [x] Add paired Flow portal nodes with single entrance/exit connectors, hidden synthetic graph edges for execution logic, connection summaries, toolbar/context-menu creation, and viewport jump-to-exit behavior after connecting to the entrance.
- [x] Fix Paper speech bubble stems so they render as a continuous curved bubble outline and add a single curve handle/control for speech and thought bubble tails.
- [x] Refine Paper shape editing so normal selection shows the resize bounding box only, Ctrl/Cmd exposes vertex editing, Shift snaps vertex edits to frame borders, Shift locks resize aspect ratio, and resize handles exist on corners and edges.
- [x] Correct Paper polygon vertex editing so caption boxes keep editable corner handles, stale caption triangles are ignored, vertices can be inserted/deleted along edges, and edited caption/panel polygons export correctly.
- [x] Complete the cumulative Paper image-transform/vertex-editing work plus .sloom project media hardening and the Flow expander node.
- [x] Fix Paper bubble tails, snapping menu controls, Shift guide/ruler snapping, and two-axis image crop panning.
- [x] Add Gemini 3 Flash multimodal text support in Signal Loom after researching the current Google AI model surface.
- [x] Add Paper modifier-wheel zoom support so Ctrl/Cmd+wheel zooms the canvas, Shift accelerates the wheel zoom step, and ordinary wheel scrolling remains unchanged.
- [x] Fix the dockable Source Bin collapse path so the internal source-bin rail collapses the dock slot, the workspace margin follows the actual dock chrome width, titlebar action buttons are removed, and drag docking shows a visible snap preview.
- [x] Fix docked collapsed panel chrome so left Source Bin collapse releases full-width dock space instead of leaving a blank gutter.
- [x] Fix Paper stacking root cause so document z-index ordering is decoupled from canvas CSS layers, newly created frames stack on top by default, and collapsed left Source Bin sidebars release their workspace offset.
- [x] Fix Paper caption stacking/render ordering and move Paper panel toggles/reset into global menu commands instead of overlay buttons.
- [x] Fix Paper image-frame offset rendering so offsets pan the source crop without translating the image element and revealing frame fill.
- [x] Replace button-driven dock movement with header drag-and-snap docking, make shared Source Bin a flush docked left sidebar, support stacked side panels, and ensure floating/docked panel contents scroll inside constrained panel bounds.
- [x] Fix Video titlebar panel toggles so Source opens the dockable Source Monitor, and make undocked shared Source Bin/Bookmarks panels scroll correctly in Flow/Image/Paper.
- [x] Add controlled docked-panel resize handles, make the Video timeline resizable from the divider above it, and modernize the Video source-bin display for larger mixed media libraries.
- [x] Add menu-selectable workspace view defaults and harden dock/floating panel bounds so oversized panels cannot trap or cover the UI without a reset path.
- [x] Fix the latest Paper/Video workspace polish regressions: connected double-page spreads, overlay/menu stacking, creation-tool hit testing, dock sizing/scroll behavior, Paper selection menu commands, richer type controls, Paper-to-Image bubble export parity, and the cramped 4K Video timeline layout.
- [x] Historical checkpoint: all documented tasks were complete when this marker was added; newer production-hardening tasks now supersede it.
- [x] Blank-window startup crash fixes are green: persisted dockable/source-bin/editor/workspace/Paper/Flow state is sanitized before render or hydration.
- [x] User-reported dockable-panel `Maximum update depth exceeded` crash is documented as fixed and validated.
- [x] Malformed `.sloom` project-open crash fixes are green: validation, sanitization, transactional restore, rollback tests, and user-visible error handling are in place.
- [x] Error recovery UI is complete with app/workspace/dockable boundaries and safe persisted-state recovery actions.
- [x] Image workspace standalone file saving is expanded: direct downloads now support PNG, JPEG, WebP, AVIF, BMP, static GIF, TIFF, SVG, layered PSD, and GIMP-style XCF, while Source Bin / Flow / Video handoff remains on visible export formats.
- [x] Flow now contains the modularized Flow2 codebase: shared download/storage helpers, native menu subscriptions, split Image quick actions/layer/properties panels, feature-module Video/Paper workspaces, and Flow/Source Bin store slice boundaries are integrated into the single active codebase.
- [x] Paper and Image editor navigation/selection polish is complete: Paper supports modifier multi-select, group movement/stacking, Shift image-crop panning, and a hand tool; Image has a matching hand tool; interface themes and the installed Electron menu launcher are wired.

## Phase 1: Planning and Scaffolding
- [x] Analyze image and determine requirements
- [x] Create handoff documentation (`CLAUDE.md`, `AGENTS.md`, `docs/HANDOFF.md`)
- [x] Create initial task list and notes structure
- [x] Implement local state management for API keys and UI preferences

## Phase 2: UI Shell & Layout
- [x] Build Top Navigation Bar (Logo, Flow Name, Zoom Controls, Share, Configuration)
- [x] Build Bottom Floating Toolbar for Node Insertion (Image, Video, Audio, Text, Config)
- [x] Build the Settings Modal for configuring API Keys (OpenAI, Gemini, Vertex, Hugging Face, ElevenLabs)
- [x] Setup the base Infinite Canvas using `@xyflow/react`

## Phase 3: Node Types & UI
- [x] Create base Custom Node component wrapper (with headers, handles, and execution status)
- [x] Implement Text Prompt Node (Input)
- [x] Implement Image Generation Node (Output/Input)
- [x] Implement Config/Settings Node (Parameters like Model, Steps, Aspect Ratio)
- [x] Implement "Run" button per-node to execute a sub-graph

## Phase 4: Execution Engine
- [x] Build Execution Context (DAG parser for React Flow nodes/edges)
- [x] Implement Text-to-Image execution via Hugging Face / OpenAI / Gemini
- [x] Implement Text-to-Text execution via Gemini / OpenAI / Hugging Face
- [x] Implement Text-to-Video execution via Gemini / Hugging Face
- [x] Add explicit start-frame and end-frame support for Gemini/Veo video generation
- [x] Implement Text-to-Speech execution via ElevenLabs / Hugging Face
- [x] Pass outputs of one node as inputs to the next

## Phase 5: Polish & Persistence
- [x] Save/Load flow to `localStorage`
- [x] Add loading spinners / node status feedback
- [x] Replace free-text model and voice fields with provider-filtered dropdown selectors
- [x] Add local image/audio import support plus per-node save/download actions
- [x] Preserve per-node generation history and allow switching between prior runs with compact run selectors
- [x] Add a Composition node for combining video and audio with timeline-style alignment controls
- [x] Add larger clickable connector ports with compatible next-step action menus
- [x] Add a source-bin node plus a dedicated editor workspace toggle for manual timeline editing
- [x] Promote the editor workspace with separate source/program monitors and a sequencer-style layout
- [x] Make all source-bin nodes act as entry points into one shared editor media pool
- [x] Add local project save/open/import/export plus bulk media export from the editor workspace
- [x] Make the editor source bin project-owned with autosaved imported/generated assets that save and reopen with each project
- [x] Add editor panel toggles and drag-to-resize splitters for the source/program/bin/inspector layout
- [x] Expand composition editing beyond one-video-plus-audio into a real sequencer with reusable source-bin assets and richer track controls
- [x] Turn the source bin into a persistent local source library with shared entry nodes, direct media import, and a collapsible flow-workspace sidebar
- [x] Add richer manual-editor finishing controls including text overlays, clip transforms, motion keyframes, timeline splitting, and editor context menus
- [x] Make the editor monitors substantially more usable with larger preview stages, explicit program-canvas settings, a live edit-stage view, and select/cut timeline tools
- [x] Improve editor clip ergonomics with playhead-based cutting, bin-to-timeline drag/drop, video-audio placement on audio lanes, and monitor-stage scaling fixes
- [x] Tighten the editor into a denser NLE-style workspace with adjustable track heights, clip edge thumbnails, audio waveforms, and stronger render activity feedback
- [x] Add undo/redo controls and keyboard shortcuts for manual editor composition edits
- [x] Fix editor render feedback, add draggable timeline track-height handles, and carry opacity automation points into renders
- [x] Add multimodal text-node image context via connected image nodes and direct source-bin image drop for image-to-text prompting
- [x] Keep source and program monitor previews fitted inside their panels instead of bleeding into the timeline/editor chrome
- [x] Fix the monitor rendering regression that collapsed source/program previews into thin strips or blank-looking panels
- [x] Place newly added toolbar/editor nodes at the center of the current flow viewport instead of a fixed screen location
- [x] Respect non-square aspect ratios in image generation and image editing requests instead of silently defaulting to square outputs
- [x] Rebrand the app with a distinct product name and update the visible shell, defaults, and docs consistently
- [x] Replace the titlebar placeholder initials with a cropped Signal Loom logo asset and matching compact styling
- [x] Add collapsible model-generated text nodes with compact scrollable result previews
- [x] Add blurred/synthetic in-progress previews for image generation nodes while providers render the final image
- [x] Make generated source-bin videos durable for timeline editing by saving blob outputs into the project scratch store and referencing saved source-bin item IDs
- [x] Add visually distinct, dark-theme-safe color accents for each node type
- [x] Add fullscreen controls, right-click node title bookmarks, and a collapsible right-side bookmark navigator
- [x] Add virtual alias nodes so one upstream node can be reused from multiple places without moving around the canvas
- [x] Add filesystem project-folder save/open controls plus per-project scratch folder export for source-bin media
- [x] Add timeline clip context-menu export actions for first/last video frames into the source bin
- [x] Refresh the Hugging Face image/video/audio model catalogs with current popular options and make the image node show only the controls supported by the selected model
- [x] Add shared custom right-click menus across the flow and editor workspaces instead of relying on the browser default context menu
- [x] Add basic local image-editing tools starting with crop plus selected-region edits composited back into the source image
- [x] Add timeline automation curves for audio volume and visual opacity so clips can fade or duck over time with editable points
- [x] Add per-audio-track sequencer volume controls that scale clip volume automation during editor renders
- [x] Make the program monitor and final render use the same transform/layout math so the editor preview matches exported output more closely
- [x] Add a secure VPS-backed remote access path for Signal Loom with authenticated login, a reverse tunnel to this machine, and remote start controls for the local app service
- [x] Show image media previews in node cards using their real aspect ratios instead of forcing square crops
- [x] Add preview-only collapse controls for large media nodes so the flow workspace stays navigable
- [x] Add direct editor narration tools, richer per-clip source-audio controls, deeper transitions, and more complete NLE-style finishing features
- [x] Add an optional directory-backed project scratch workspace for autosave/export instead of browser-only storage
- [x] Add program-monitor text and rectangle stage objects with transform, opacity, color, and blend/filter controls
- [x] Add click-to-preview modal panes for image and video node thumbnails in the flow workspace
- [x] Add visual clip property copy/paste with selectable position, size, rotation, and opacity targets for clip starts/ends
- [x] Replace untimed program-monitor text and rectangle stage objects with reusable timeline-backed editor assets in a Source Bin tab
- [x] Add non-destructive clip edge trimming, selectable timeline gaps with Fill Gap, and crop/filter controls that preview and render identically
- [x] Add timeline snap markers, clip snapping, and Shift-to-whole-second timeline interactions
- [x] Harden editor performance by bounding timeline preview memory, deduping waveform/duration work, and avoiding base64 media during active editing
- [x] Make linked scratch folders the active media store so imports/generated editor assets stop filling Chrome's root-profile storage
- [x] Bound timeline thumbnail and waveform decoding so normal slicing cannot spike Chrome memory by decoding many whole media files at once
- [x] Make timeline text render as transparent free text with right-click editing from the timeline/source bin and keep the inspector scrollable
- [x] Make timeline text objects use natural text bounds with invisible selection boxes and scale-driven font animation
- [x] Add multi-point timeline keyframes for visual transforms, opacity, and audio volume with jump controls and shortcuts
- [x] Expand visual finishing with a reusable image/video/text filter and blend-mode stack (screen, darken, overlay, dodge, burn, etc.)
- [x] Final UI styling pass (dark mode as seen in the reference image)

## Phase 6: Hardening
- [x] Convert Signal Loom into an Electron desktop app with native KDE/globalmenu menus and native file open/save workflows
- [x] Investigate and fix the Composition-node freeze caused by repeated media-duration reloads during rerenders
- [x] Fix the current memory spike/crash by coalescing source-bin media ingestion and restoring linked scratch storage on reload
- [x] Fix Veo start/end-frame logic so interpolation mode follows actual graph wiring and matches the current Gemini contract
- [x] Fix composition export/timeline interaction bugs and surface miswired frame-image edges on the video node
- [x] Remove the React Flow built-in input-node skin leak behind custom text nodes
- [x] Auto-repair legacy or misdropped image-to-video edges onto the proper frame handles
- [x] Add upstream image-edit support to image nodes for Gemini and OpenAI image models
- [x] Give video start-frame and end-frame their own dedicated handles and replace stale same-handle edges on reconnect
- [x] Normalize Gemini Veo model aliases to canonical request IDs so frame-conditioned renders do not send unsupported inlineData payloads
- [x] Enforce Gemini Veo interpolation's current 8-second duration requirement in the video node UI and execution validation
- [x] Set Gemini Veo person-generation mode correctly for image-conditioned and interpolation video requests
- [x] Add video import mode plus scrubber-based still-frame export from video assets
- [x] Let image nodes extract first or last frame images from upstream video sources
- [x] Reuse already-generated upstream media outputs instead of rerunning completed branches
- [x] Add automated smoke tests for node creation, settings persistence, and graph execution
- [x] Prevent stale remote provider chunks from surfacing raw dynamic-import failures in generator nodes
- [x] Add an optional backend proxy mode so users do not need to expose provider keys directly to the browser
- [x] Add asset upload / input nodes for richer Flow-style multimodal chaining
- [x] Add pre-run spend estimation plus post-run usage telemetry with node-level and global summaries
- [x] Add Gemini 3.1 Flash TTS narration controls for accent, style, and delivery guidance
- [x] Normalize composition video-track connections when edges are drawn without the expected handle and replace video duration dropdowns with a three-stop slider
- [x] Add separate editable-source and reference-image inputs to image nodes so Gemini image editing can use style/outfit guidance images
- [x] Add richer per-node execution telemetry and cancellation for long-running video jobs
- [x] Close remaining ElevenLabs-style video parity gaps: negative prompts, batch generations, richer reference image controls, and richer export/history UX
- [x] Add an optional local native FFmpeg render path with multithreaded CPU fallback and AMD VAAPI acceleration for composition/editor exports
- [x] Fix non-destructive cut source-range instability by deduping clip preview extraction and using explicit source ranges consistently
- [x] Cut flow-workspace rerender/persist cost: stabilize per-node runtime callbacks, narrow node store subscriptions, memoize node components, debounce localStorage writes, batch imported-asset restores, lazy-load the manual editor workspace, and split provider SDKs into separate vendor chunks
- [x] Restore text/media/config node control focus by marking React Flow node controls as no-drag/no-pan/no-wheel interaction surfaces
- [x] Fix animated timeline text renders by measuring rasterized text bounds with real font metrics and safety padding
- [x] Fix small-scale animated text renders by aligning FFmpeg visual scaling with the program monitor's 10% minimum
- [x] Replace conflicting visual start/end animation controls with default Start/End keyframes as the authoritative transform model
- [x] Make native project open/save use `.sloom` files with per-project scratch folders so source-bin media reconnects from the File menu
- [x] Restore renamed `.sloom` source-bin media from legacy sibling scratch folders when project assets were saved before the final project name
- [x] Combine the editor header controls into the app titlebar and remove the redundant Manual Editor banner
- [x] Fix Composition-node mini-timeline handles so visible inputs align to actual lanes instead of floating over the node body/footer
- [x] Add source-bin starring, per-item collapse, collapse/expand-all controls, and thumbnail media preview popups
- [x] Surface flow-source-bin text and image assets in the editor Project Source Bin's Editor Assets tab
- [x] Keep generated and imported source-bin assets visible when durable browser/scratch persistence fails
- [x] Render shared node context menus from a viewport portal so rename menus open at the cursor
- [x] Make `.sloom` project save/open materialize binned source-library media so generated videos reopen after a fresh launch
- [x] Remove legacy JSON project affordances from the active save/open UI and focus project workspaces on `.sloom` files
- [x] Fix editor keyframe deletion and make timeline opacity handles easier to drag without accidentally creating keyframes
- [x] Make native `.sloom` Save/Save As embed temporary blob-backed generated videos before Electron writes the project workspace
- [x] Preserve source display-aspect metadata through render fit scaling so animated motion and borders match the program monitor
- [x] Add typed list nodes and envelope containers for looped media/text variations with nested source-bin output
- [x] Align envelope-driven Veo frame interpolation requests with the current Gemini API contract
- [x] Fix Gemini Veo media-input serialization to match the Google GenAI MLDev wire shape used by the live video endpoint
- [x] Add nested all-combinations loop mode for multi-list and envelope-driven runs
- [x] Wire image workspace titlebar zoom controls and layer thumbnail invalidation for raster edits
- [x] Wire image editor Cut, Copy, and Paste through the context menu and Ctrl shortcuts
- [x] Clip prompt-based image edits to the submitted selection with a Photoshop-style layer mask
- [x] Add Photoshop-style layer mask controls for creating masks from selections and managing them per layer
- [x] Add 70 total Photoshop-style image quick actions to the Image workspace context menu
- [x] Route edited Image workspace outputs back through durable Source Bin assets for Flow and video-editor reuse
- [x] Add 200 more implemented Photoshop-style image quick actions with generated, tested selection/layer/transform/pixel presets
- [x] Add real non-destructive adjustment layers and align Image handoff naming around Source Bin, Flow, and Video workflows
- [x] Add Photoshop-style layer effects for image layers with visible-output rendering through Source Bin, Flow, envelopes, and Video
- [x] Add multi-format Image workspace visible export for PNG, JPEG, WebP, and AVIF source-bin assets
- [x] Add Openverse free-resource image search/import into the Image workspace Source Bin workflow
- [x] Add stackable per-layer Image workspace filters that render before effects and export through Source Bin
- [x] Add Clone Stamp retouch tool with Alt-click sampling, brush controls, undo, and toolbar/shortcut integration
- [x] Add storyboard-oriented brush presets for paint, eraser, and clone-stamp workflows
- [x] Fix Linux Electron launch reliability by adding a tested GPU-disable fallback to the desktop launcher
- [x] Add PSD import/export interoperability for layered Image workspace documents
- [x] Add a Spot Heal retouch brush for automatic blemish blending
- [x] Add a Blur Brush retouch tool for localized softening
- [x] Add a Sharpen Brush retouch tool for localized contrast/detail boosts
- [x] Add a Smudge Brush retouch tool for dragged-pixel blending
- [x] Add Dodge and Burn brushes for localized exposure retouching
- [x] Add Sponge saturate/desaturate brushes for localized color retouching
- [x] Add a Paint Bucket tool for contiguous-region color fills
- [x] Add a Linear Gradient tool for foreground-to-transparent gradient fills
- [x] Add filled Rectangle and Ellipse raster shape tools
- [x] Add Noise and Pixelate non-destructive layer filters
- [x] Build toward 85% Photoshop desktop priority parity for storyboards and AI media production: painting/retouch, masks, filters, layer effects, free resource search/import, multi-format image support, and PSD support
- [x] Add a Paper workspace for print layout with pages, rulers, guides, grids, source-library placement, print HTML/PDF preview export, and comic/manga frame tools
- [x] Make workspace menus static but tab-specific, with Flow/Video/Image/Paper menus disabled when their workspace is inactive
- [x] Replace the Image workspace prompt-only text tool with configurable multiline text settings in the properties panel
- [x] Expand Paper document setup with common page sizes, custom dimensions, bleed, margins, columns, grids, and DPI controls
- [x] Make Paper frames directly manipulable with drag-create, move, resize, rotate, image fit/crop/offset/rotation controls, and bubble-tail controls
- [x] Add Image workspace comic/manga creation tools plus visible crop preview and layer rotation controls for Photoshop-style workflows
- [x] Add Paper right-click usability commands, source-library frame/page placement, stacking actions, and shape/border/gradient controls
- [x] Add persisted Image/Paper workspace layout customization with resizable/hideable panels
- [x] Add Paper ruler guide dragging, organic comic bubble handles, arrow-key nudging, and image-frame handoff into Image/Video workspaces
- [x] Close Paper free polygons by clicking an existing vertex after enough points have been placed
- [x] Fix Paper shaped image-frame strokes, Paper image Y crop offsets, accidental UI text selection, and Flow node rename/bookmark controls
- [x] Fix Image generative selection-edit layer placement so generated patches align with the selected document region
- [x] Allow Paper workspace frames to be created, placed, resized, hit-tested, and dragged outside page bounds
- [x] Fix Paper ruler-drag guide placement and add native Electron PDF export for Paper documents
- [x] Fix Paper PDF bleed interpretation, speech-bubble text alignment, guide movement, visible bleed overlays, configurable document backgrounds, and flattened page export to Source Library/envelopes/Image
- [x] Fix native Paper PDF trim alignment so bleed handling does not push exported layouts right/down
- [x] Force native Electron Paper PDF export to use the document's explicit page size instead of default paper centering
- [x] Replace the Image workspace basic line brush with a pressure-aware dab brush engine, grouped default brush library, and expanded brush configuration controls
- [x] Fix Paper ruler guide orientation and add a visible always-on trim/cut overlay above page content
- [x] Align Paper PDF and flattened Image export frame-content rendering with the live Paper canvas for image frames, shaped frames, and caption vertical alignment
- [x] Fix Paper PDF image/shape frame offsets from default figure margins and embed placed image assets before flattened Image export rasterization
- [x] Add dockable, movable, resizable floating dialog support for major app/help/media/video tool dialogs
- [x] Audit and fix Video workspace program-monitor vs final-render layout parity for positioned images, text, crops, and transforms
- [x] Add more print/document production support in Paper for comic and generative-media publishing workflows
- [x] Build side-by-side Adobe comparison and prioritization for InDesign/Paper, Photoshop/Image, and Premiere/Video workflow gaps
- [x] Complete the first Adobe porting wave/integration pass for Paper, Image, and Video
- [x] Complete the post-crash Adobe integration pass for started Paper/Image/Video parity features
- [x] Continue deeper Adobe parity follow-up: Paper parent pages/styles/package export, Image richer text/adjustment polish beyond the integrated controls, and Video transition/render-parity completion
- [x] Expand import/export format support across Source Bin, Image, Paper, Video, captions, packages, and Electron/native file filters
- [x] Add standalone Image workspace file-download options for common raster, layered, and workfile formats while preserving workflow-safe Source Bin, Flow, and Video visible exports
- [x] Harden persisted startup state for dockable panels, source-bin, editor, workspace layout, Paper, and Flow hydration; reproduced the startup infinite update/persisted dockable crash and verified the sanitized hydration path stays mounted
- [x] Harden malformed `.sloom` project open with validation, sanitization, transactional restore, rollback behavior, and restore-path regression tests
- [x] Add app-level, workspace-level, dockable-host, and dockable-panel render error recovery boundaries with retry/reset/cleanup recovery UI
- [x] Fix Paper cover/crop image panning so image frames render an aspect-correct movable image canvas instead of clipping a translated frame-sized image.
- [x] Harden native `.sloom` saves after the Chronicle data-loss incident: automation saves now always use isolated automation paths, and overwriting an existing project first creates a timestamped sibling `.sloom.bak-*` backup.
- [x] Build and verify a one-app Signal Loom Android Local Dream fork path with embedded LAN API, model/upscaler readiness UI, serialized generation/upscale jobs, Local Dream runtime asset extraction, and side-by-side debug install documentation.
- [x] Harden Paper print Android upscaling with conservative retries for transient phone/network/backend failures while failing fast for missing configuration or unavailable downloaded model/upscaler state.
- [x] Add Android accelerator setup diagnostics in Settings so users can distinguish the standalone bridge companion from the one-app Signal Loom Android build and see model/upscaler readiness warnings before print/generation jobs fail.
- [x] Resolve production packaging for Signal Loom Android model-data migration: a debug side-by-side app cannot read the Play Store Local Dream app's private downloaded model directory, so either a user-approved reinstall/redownload path or a signed migration/export path is still required.
- [x] Implement Elena's Gutter Knife Tool (Paper Workspace): Complete UI and store wiring for 'gutterKnife' tool, dragging interactions, preview lines, and button in PaperWorkspace.
- [x] Unify Image AI Cost Estimation (Image Workspace Interop): Bind 'estimateGenerativeFillCostUsd' to the shared ledger in costEstimation.ts.
- [x] Implement Hybrid PDF Printing (Paper Workspace): Modify 'paperPdfExport.ts' to rasterize backdrop artwork panels while keeping text, bubbles, and vector outlines as sharp vector elements.
- [x] Add Vector Layer Type (Image Workspace Interop): Add a 'vector' layer type supporting SVG XML recipes in canvas documents.
- [x] Update the documentation of Signal Loom with advanced story algorithm examples, including State-Driven Storyboards, Multimodal Screenplay Orchestration, and Closed-Loop Style Verification, complete with visual dark-theme HTML node graphs in SLOOM.html, project documentation updates, and standard library template integration.
- [x] Incorporate custom workspace icons (Flow, Video, Image, Paper) and design a beautiful, responsive, glassmorphic segmented workspace controller in the persistent top navigation bar.
- [x] Enable pressure and tilt sensitivity on external Wacom displays in the Image workspace editor (brush size, opacity, and flow scale with stylus pen pressure; brush angle follows pen tilt)
- [x] Add direct Image workspace layer transform handles plus visible crop Apply/Cancel controls, with tested crop state application and 1080p browser verification.
- [x] Image parity worker slice: Brush engine, free transform/move, history/actions, and vector shape readiness workers completed with focused tests.
- [x] Image parity worker slice: Retouch/clone/heal, crop/document geometry, gradient/fill/paint bucket, paths/pen, selection/refine, layer organization, layer filters, and histogram/adjustment readiness workers completed with focused tests.
- [x] Image parity worker slice: Blend/style portability, artboards/print proof, Camera Raw/import policy, and smart/source-linked layer readiness workers completed with focused tests.
- [x] Image parity worker slice: Liquify/Puppet Warp, color/proof/bit-depth, tool registry/shortcuts, and Android Image parity readiness workers completed with focused tests.
- [x] Image parity worker slice: Image automation/batch workflow, vector masks, content-aware fill/remove/patch, and workspace launch/icon readiness workers completed with focused tests.
- [x] Image parity worker slice: Parity wording/test expectations, hand/navigation, export formats, and text/typography readiness workers completed with focused tests.
- [x] Image parity acceleration batch: text-on-path, text find/replace/readability, brush presets, retouch rate/airbrush, masks, paths, RAW/import policy, layer styles, artboards, color proof/modes, export warnings, docked/undocked panel behavior, compact Image tool palette, Android picker/upscaling readiness, gamepad binding model/UI, Paper pre-flight hide/restore, Paper/Image eyedropper sharing, Video render preview status, Vertex/Atlas video provider planning, desktop packaging descriptors, API-key storage status, dashboard checklist telemetry, and Electron startup splash image completed with focused tests, TypeScript, Android sync/build/install checks.
- [x] Image parity concurrent descriptor batch: complete four worker lanes for PSD/XCF interop, vector/shape/Pen readiness, alpha/spot channel panel descriptors, and content-aware/generative/quick-action caveats while the main lane adds source-bit-depth document/export readiness descriptors, high-bit source warning groups, dashboard worker completion telemetry, focused 214-test regression coverage, TypeScript verification, and a refreshed parity snapshot.
- [x] Image parity and launch batch: install the supplied Signal Loom launch artwork across web, Electron, and Android splash paths; add AndroidX splash installation in MainActivity; verify Android build/sync/install on the connected Galaxy device; record blocked DeX visual verification honestly when keyguard owns the display; fix dashboard artifact status handling for explicit failed reports; complete marquee saved-selection operation readiness; integrate PSD/XCF, vector-shape, channel, content-aware, and marquee parity ledger updates; and restart the next four regular worker lanes while Spark quota is unavailable.
- [x] Fix Paper mobile left Source Library drawer handle visibility after compacting it too far: keep it flush-left, above the floating tools palette, visibly sized in hidden chrome, and activatable from pen/touch pointer release without relying on synthesized clicks.
- [x] Align Image retouch parity descriptors with implemented bounded Smudge current-and-below/all-layers stroke-start snapshot sampling; remove stale smudge composite blocker wording while keeping live-resampling limitations explicit.
- [x] Harden the Image Pen tool by adding first-anchor close-path behavior for retained closed straight-segment vector paths, descriptor capability coverage, and parity dashboard wording/tests without claiming Bezier or curvature support.
- [x] Add a `.slimg` Flow node (Flow ↔ Image bridge), **file-based**: Save .slimg & Open (connected image → new file + edit), Import (open an existing `.slimg` to edit + set output), Read disk (re-read the node's own saved file → flattened output, one-click via a new `signal-loom:image-read-path` Electron handler). Output wired through the image-input resolution incl. the edit/reference/mask resolver (`imageEditConnections`) so it passes to image nodes + shows the preview. Replaced the original in-memory live-sync. tsc/build green. See docs/notes/792.
- [x] UX review (docs: Signal_Loom_UX_Review.pdf) — **"Now" column** (string/CSS): F02 node-catalog labels show at ≥1600px (was 3200px, BottomToolbar); F08 visible "Editor"→"Video" ("Open in Video", "Video Assets"); F01 library naming consistency ("Source Pool"→"Source Library"; "Project Source Bins" kicker→"Saved Assets"); F04 node titles wrap to 2 lines (`line-clamp-2`) instead of single-line truncate. Build + BottomToolbar test green.
- [ ] UX review — **remaining**: F03 (raise sub-11px text floor + contrast; add Comfortable/Compact density toggle), F06 (collapse repeated node help text into a dismissible block), F07 (visible ⋯ menu button for right-click-only actions; touch-reachable), F05 (Video V1–V4 clip buttons → primary "+Add" + labelled track menu), F09 (group "Program Tools" into labelled clusters + share fit state with Inspector), F10 (actionable empty states + starter templates + Video phone gate).
- [x] UX review F03/F05/F06/F07/F09/F10 — completed in the 2026-07-02 "finish everything" batch (see docs/notes/805): density toggle, +Add track menus, dismissible node help, visible ⋯ node actions, Program Tools clusters, actionable empty states.
- [x] Vertex mobile-standalone execution (image/text/video) via direct REST on a service-account key (vertexDirectRest.ts); fail-closed config errors made non-retryable by TYPE (NonRetryableError) instead of message sniffing — missing API keys and malformed Veo requests now fail fast instead of entering 10x backoff (docs/notes/805).
- [x] UI layout audit round 1 (owner-reported): remove the Video workspace's 64px dead band under the menu bar (pt-16 legacy padding; Reset Video Panels moved fully into Window > Panels); move Flow's React Flow zoom controls to bottom-center so the docked Source Bin can never bury them (docs/notes/807).
- [x] Dockable panels: center split survives tab groups (no more silently vanishing Source Monitor); Ctrl-drag forces floating (floating was unreachable by drag in a fully-tiled workspace); non-split panels dropped on a center monitor tab-join instead of degrading the split; Paper's shared Source Bin 64px top offset removed; split dividers visibly tinted at rest (docs/notes/807).
- [x] Video workspace pro-editor gap audit with 1–100 need rankings + 1–10 confidence (docs/notes/808); waveforms row corrected — already shipped in-app.
- [x] Pro-editor quartet (owner-approved build order, docs/notes/809): (1) JKL/Space/Home/End transport with rAF playback over the timeline cursor + live rate chip; (2) waveforms verified pre-existing; (3) Source Monitor I/O marks + Insert (,) / Overwrite (.) three-point editing onto V1 with straddler splitting, one undo step each; (4) playhead-driven ripple (Q/W) + roll (E) trim with media/min-duration clamps. All pure-math cores unit-tested; K-key conflict resolved (keyframe add → Shift+K); F1 help overlay documents the full new key map.
- [x] Site sims accuracy pass (owner-directed, docs/notes/806/810): video replay rebuilt as the app's real tiled two-monitor layout from a live screenshot; Paper/SFX/Image/Flow sims re-skinned to real top bars, panel headers, Source Bin panel, and real menu words; workspace switcher uses the shipped ws-*.png icons; paper sculpt beat now interlocks the neighbouring frame (no trapped white space) with the art refit to the new bounds. NOT deployed — deploy carries the 17.99 pricing sweep and needs owner go-ahead.
- [x] **Site sims → film's high-fidelity `<sl-sim>` engine, deployed live (owner-directed, docs/notes/824).** Imported the Claude-Design "Signal Loom — The Film" project (via zip after `/design-login` was unavailable over Remote Control) and swapped the five inline home-page workspace animations for the film's much richer replays: added `assets/sims.js` (standalone Shadow-DOM custom element, kinds flow/paper/sfx/brush/video) + `sim-assets/`; replaced the five `.fd-stage` blocks with `<sl-sim>` (aria-labels preserved); removed the ~812 lines of dead inline driver JS (page-chrome loom/wire/minimap/reveal all kept). Verified on real Chrome (isolated harness + real page + **live sloom.studio**): 5 render, 0 broken images, 0 console errors, page chrome intact. Deployed via `deploy.sh root@2.24.206.11 --go` (verify gate passed, privacy 200). Follow-ups: dead inline sim CSS + unused `assets/demo/` left in place; not committed to git.
- [x] **ONE universal global menu (owner-escalated, docs/notes/827) — DELIVERED + LIVE-VERIFIED.** C++ fork of KDE's stock Global Menu applet (`desktop/kde/signal-loom-globalmenu/`, plasma-workspace v6.6.5, GPL headers kept, ~30-line `appmenumodel.cpp` patch): identical stock behavior for Qt/GTK/XWayland apps PLUS a fallback that imports Signal Loom's `com.canonical.dbusmenu` at `/org/signalloom/menus/active` when an SL window is focused. App side: `panelMenuService.cjs` now also exports that dbusmenu twin (per-workspace menus, live-switch via LayoutUpdated on focus). Installed user-locally on the owner's machine, fork placed far-left on both top panels, stock appmenu + superseded QML applet removed, installed app refreshed + launcher flag added. Verified live by screenshot: Dolphin's menus AND the real app's Flow menus (`Project Flow View Window Help`) at far left; GPU untouched (no XWayland). Owner-verify remaining: workspace-switch menu swap + clicking through menus in real use; Gabbee widget disappearance flagged (backup exists).
- [x] **KDE global menu WITHOUT XWayland (owner-requested, docs/notes/826).** The stock KDE global menu keys on an X11 window id → forces XWayland → kills the GPU on AMD/Mesa (notes 759–760), so it and hardware accel were mutually exclusive. Built a parallel path that needs no window id: a native-Wayland D-Bus service `org.signalloom.PanelMenu` (`electron/globalMenu/panelMenu{Model,Service}.cjs`, opt-in **`SIGNAL_LOOM_ELECTRON_PANEL_MENU=1`** — a DIFFERENT flag that deliberately does NOT force XWayland) publishing the focused workspace's menu as base64 JSON (same `shared/workspaceMenus.json` source of truth), plus a buildless Plasma 6 applet (`desktop/kde/signal-loom-panelmenu/`, pure QML via `gdbus`) that polls it and renders the menu in the panel. App-side fully verified: 13 new unit tests (26/26 with the existing suite), tsc -b + vite green, and a **real end-to-end D-Bus round-trip** on an isolated `dbus-run-session` bus (State/GetMenu/base64-decode/Activate all correct, command reaches the handler). **VERIFIED ON-DEVICE (2026-07-05):** installed on the owner's real Plasma 6.6.5 Wayland session and — after fixing two live QML bugs (illegal compile-time `SlMenu` self-recursion → runtime `Qt.createComponent`; Plasma-5-ism `Plasmoid.preferredRepresentation` → root `preferredRepresentation`) — **screenshotted rendering all 9 image-workspace groups in the top panel** (fed by a temp service harness on the session bus). Separately diagnosed why the owner's Add-Widgets picker was empty: a 7-day-old plasmashell running against pre-`6.11.1` Qt after the Jul-2 update — fixed by restarting the shell (unrelated to the applet). Not committed yet.
- [ ] Video: pointer drag-trim on clip edges (ripple on modifier) — trimEdit.ts math is the ready engine; keyboard Q/W/E shipped first.
- [ ] Video: timeline + clip markers with labels/colors (gap audit rank 74); audio crossfades per cut (72); track lock (66); waveforms on video clips' embedded audio (audit sliver).
- [ ] Dockable panels follow-ups: hand-verify Ctrl-drag float on real hardware (automation can't forward modifiers); consider auto-grouping bare non-split panels dropped into empty center space.
- [x] **Cross-device Image baton pass FIXED + VERIFIED (0.9.10 gate, docs/notes/819–821).** Three root causes, all owner-visible as "blank canvas / drawing never transfers": (1) high-res composite worker was a `Function.toString()` blob that crashed in every minified build (`_r is not defined`) → real Vite module worker; (2) CompositeRenderer served a stale cached composite because devices bump `bitmapVersion` independently → `remoteImageApplyEpoch` + hard cache invalidation on every remote apply; (3) the Android LAN relay dropped query strings (NanoHTTPD `getUri()`), so the baton write-gate rejected every served-client op as `edit-locked` and long-polls replayed the whole log → forward `getQueryParameterString()`. Verified fully agent-driven (adb + Playwright), both directions, with screenshots: phone Continue card renders all strokes; served browser seeds + renders; browser X-stroke ops accepted (`{"ok":true}`) and visible on the phone after handoff. **License activation verified on-device** (offline Ed25519, licensed to owner). Known follow-up: serving stops when the app backgrounds (needs foreground service).
- [x] **Video text = Paper typesetting + Image layer modes (Phase 2B, docs/notes/831).** New `videoTextFlow.ts` (reuses `flowPaperText`'s wrap core, re-derives alignment incl. justify, adds arc/curved-text glyph placement) wired into text-clip export (`renderTextCard`, now a direct canvas render instead of SVG), the free-floating stage text-object painter, and the comic-bubble typesetting block (weight/style/leading/tracking/align+justify/stroke/shadow/arc), plus matching live-preview CSS + per-glyph arc spans and new inspector controls on both the text- and comic-clip panels. `editorClipEffects.ts`'s clip blend modes widened 8→16 (full Image parity) and fixed a latent FFmpeg-name bug (`color-dodge`/`color-burn` were emitting invalid `colordodge`/`colorburn`; now `dodge`/`burn`). Follow-up: the separate free-floating stage-object blend system (`editorStageObjects.ts`) still needs the same 16-mode widening. `npm run build` + full `vitest run` (1042 files/7414 tests) green.
- [x] **Imported DOCX document elements are borderless — the "black boxes" answered + fixed (owner-flagged, docs/notes/854).** The boxes were NOT document content: `createPaperFrame` defaults a text frame to `#111827`/0.35mm stroke + white fill and the importer only made the TABLE branch borderless, so every imported paragraph/heading/image inherited the default stroke → boxed. Fix: import now sets paragraph/heading + image frames `strokeColor:'transparent'`/`strokeWidthMm:0` (paragraphs also `fillColor:'transparent'`), and the canvas render's `Math.max(1,…)` border floor now paints **no** border when `strokeWidthMm===0` (was forcing a 1px stroke-coloured box even at width 0; export was already honest). Each paragraph stays a separate placeable frame (owner's DTP preference) — just without the debug box. Live-verified on demo.docx: 0 visible borders, 0 white boxes, 8 margin guides on the 25.4mm margins, tables keep styling. Authoring inventory confirmed complete for a Word-like doc (Text tool, style presets, bullet/numbered lists, table insert+styling, drop caps, paragraph shading/borders/indents, per-run formatting, image place). Open (deferred #60, owner design call): a newly *drawn* text frame still gets the default border — borderless-by-default needs a non-printing "frame edges" view toggle first. `npm run build` + full `vitest run` (547 files/3996 tests) green.
- [x] **Borderless new text frames + light "Frame Edges" view toggle (owner chose Option 1, docs/notes/854 incr 2).** Owner: the toggle should be "a reference symbolic thing only… just for ease of user visibility… just like toggling guides or grid… light like both." Delivered: (1) `createPaperFrame` defaults `kind:'text'` to transparent stroke / 0 width / transparent fill (a Word body paragraph) — comic kinds (panel/caption/bubble) keep their strokes, only NEW frames affected, saved docs untouched; (2) `view.showFrameEdges` (default OFF) with a "Frame Edges" top-strip toggle beside Grid/Guides; (3) when on, each non-bubble/non-clip frame gets a light non-printing `outline: 1px rgba(148,163,184,0.5)` in the editor only (print/PDF/flatten re-render independently — no html2canvas — so edges never export). Live-verified beside Grid/Guides: ON → 87 frames outlined w/ real border 0, OFF → clean Word-like page; lists (bullets/1./roman/multi-level) render correctly. `npm run build` (tsc -b caught 2 top-strip test fixtures missing the new props → fixed) + full `vitest run` (547 files/3997 tests) green.
- [x] **Japanese DTP + manga lettering — Phase 1 (docs/notes/857).** Vertical writing 縦書き (`writing-mode: vertical-rl` + `text-orientation` on the frame content container, inherited by uniform + rich + editor paths), kinsoku 禁則処理 (`line-break: strict`, default-on in vertical), inline furigana ルビ via the standard `漢字《かんじ》` / `｜熟語《じゅくご》` Aozora notation → real `<ruby><rt>` at render time (works in bubbles AND rich runs, no model change; editors keep the raw notation), auto tate-chū-yoko 縦中横 for 1–2 digit numbers, 圏点 bouten emphasis marks (frame-level `text-emphasis` dot/sesame/circle), and 明朝/ゴシック/丸ゴシック JP font presets. New `PaperTypography.writingMode/textOrientation/lineBreakStrict/emphasis` + `renderPaperInlineText()` + `paperEmphasisMarkToCss()`; inspector "Japanese 縦組 · Manga" section (Direction/Orientation/Kinsoku/Emphasis + notation hint). Rendering verified live in-app engine (vertical flow, ruby beside kanji, TCY upright, Mincho/Gothic, manga bubble 「魔法の力だ！」); parser unit-checked; build green. Phase 2 (per-run furigana/圏点 emphasis-dots + DOCX round-trip + manga presets) and Phase 3 (vertical auto-fit + PDF/X vertical/ruby export) deferred.
- [x] **Manga speech bubbles at English parity (owner: "make Manga bubbles work at least as well as their English counterparts", docs/notes/857).** Phase 1 missed the dedicated bubble renderer (`PaperBubbleText` ≠ `PaperInlineText`). Fixed: (1) bubbles now render furigana/TCY via `renderPaperInlineText` (was raw `frame.text`); (2) vertical bubble text centers correctly — the flex text-box axes rotate under `writing-mode: vertical-rl`, so vertical now uses `justify-content:center` + `verticalAlign→align-items` (`paperTextBoxReactStyle`); (3) fixed `<ruby>` scattering across columns — `display:flex` blockifies each `<ruby>` into its own flex item, so the bubble's inline content is now wrapped in one inner block (also fixes horizontal ruby). Added a one-click "縦書き Vertical text (manga)" toggle to the Bubble inspector. Verified in the app's engine: chōonpu ー vertical bars, 「」 corner forms, small kana, 。、, ！？, ellipsis, furigana on the correct side, multi-column wrapping, English bubbles unchanged. Build + 35 paper tests green. Remaining: PDF/X *vector* export of vertical text is Phase 3 (raster/flatten export captures it correctly today).
- [x] **Japanese vertical text survives EXPORT (print HTML + PDF/X, docs/notes/857).** The flatten/print/PDF export re-generates HTML via `exportPaperDocumentToPrintHtml` (paperDocument.ts), NOT the live DOM — so it had no Japanese awareness and exported vertical pages as horizontal text with literal `《》`. Fixed: new shared `src/lib/paperJapaneseText.ts` (one furigana/TCY tokenizer + `paperInlineTextToHtml` for export + `paperEmphasisMarkToCss`); `renderPaperInlineText` refactored onto it so render==export; export CSS (`textStyle`/`printFrameContentStyle`/`renderPrintTextBox`) now emits writing-mode/orientation/`line-break:strict`/text-emphasis + the vertical bubble/caption flex-axis fix + the ruby inner-wrap. PDF/X vector gate `frameTextIsVectorSafe` now excludes vertical/emphasis/furigana-notation frames so they rasterize (correct) instead of the linear engine drawing horizontal type with literal `《》`. Build + 99 tests green (tokenizer + export-integration + vector-gate); real exported HTML rendered in-browser (bubble 魔法の力だ！ + text 吾輩は猫である。第12巻… both correct). Remaining tradeoff: vertical text exports as raster, not selectable vector — same as other raster-only features.
- [x] **Japanese Phase 2: word-level 圏点 + manga bubble presets + editor ルビ/圏 buttons (docs/notes/857).** 圏点 (bouten) on specific words uses the narou `《《語》》` INLINE notation (a new `emphasis` token in the shared `paperJapaneseText` tokenizer), NOT a per-run model field — so it word-level AND survives the export's rich-text flatten exactly like ルビ (`<span style="text-emphasis: filled sesame">`). Two one-click bubble presets (縦書き ゴシック/明朝 = vertical+JPfont+centered) added to `paperBubblePresets.ts`; the 6 horizontal presets now clear `writingMode` so a bubble toggles back with one click. Rich-editor floating toolbar gained ルビ (wraps selection `語《》`, caret inside for the reading) + 圏 (wraps `《《語》》`) buttons via `wrapSelectionWithNotation` (inserts notation as plain text → round-trips). Inspector Japanese hint documents `《《大事》》`. Build + 105 tests green; faithful render of mixed emphasis+furigana in a vertical bubble verified. Remaining: DOCX round-trip of vertical/ruby/emphasis (Phase 2, low priority for manga); vertical-aware auto-fit (Phase 3).
- [x] **Japanese Phase 2b: ルビ/圏 buttons in the PLAIN editor too (bubbles + uniform frames, docs/notes/857).** The rich-editor buttons only reached rich frames; manga bubbles and uniform text frames use the plain `PaperEditableText` (contentEditable), so a letterer without a Japanese IME had no way to insert the fullwidth `《》`. `PaperEditableText` now takes `vertical?` and, when vertical, mounts a tiny `createPortal` ルビ/圏 toolbar above the editor; `wrapNotation` wraps the selection with the notation via `execCommand('insertText')` + `Selection.modify` caret walk-back (ルビ drops the caret between the brackets). Both bubble and uniform call sites pass `vertical={vertical}`. Verified in a real browser: the exact execCommand round-trip 魔法の力だ → `魔法《まほう》の《《力》》だ`, rendered as vertical `<ruby>` + sesame 圏点 (screenshot); build (tsc -b) + 68 paper JP tests green. Remaining: DOCX round-trip (Phase 2, low priority for manga); vertical-aware auto-fit (Phase 3).
- [x] **Right-to-left binding / reading order (右綴じ, docs/notes/857).** The defining structural feature of a manga/Japanese book was missing — `startOnRight` only offset the cover; within every spread the lower page number was hardcoded LEFT (Western LTR). Added `PaperDocument.view.rtlBinding?: boolean` — **undefined = auto** (right-to-left when the doc has vertical 縦書き text, else left-to-right), resolved everywhere via `effectiveRtlBinding(document)`, so a Japanese/manga doc is right-bound by DEFAULT without any toggle. `buildPaperSpreads` puts the lower page number in the RIGHT slot when RTL (slot order unchanged so geometry is untouched — only which page lands where flips), pages progress right→left, `pageSideLabel` puts recto on the right. Threaded into the workspace spread memo + reader-spread HTML/PDF export + CBZ `ComicInfo.xml` (`<Manga>YesAndRightToLeft</Manga>` / `<Manga>No</Manga>` — the standard field Tachiyomi/Panels/Komga honour). A `右綴じ` top-strip toggle (flips the effective value + pins it via a new generic `setViewOption` store action) + `paper:toggle-binding-direction` command. Verified: `paperSpreads.test.ts` (RTL interior/cover/no-cover/single-page) + `paperDocument.test.ts` (`effectiveRtlBinding` auto/override) + CBZ `<Manga>` (explicit + auto-vertical) + persistence + full sweep **2248 green** + build green. Deferred (honest): PDF `/Direction /R2L` viewer hint + IDML `PageBinding` (don't risk the shipped PDF/X + IDML exports for minor hints).
- [x] **Japanese DOCX round-trip: furigana / 圏点 / 縦書き as native OOXML (docs/notes/857).** `paperDocumentFormats.ts` now carries Japanese through a Word round-trip, reusing the shared `paperJapaneseText` tokenizer so it matches the canvas. **Export** (`buildDocxStoryExport`): each line's inline notation → real Word markup — furigana → `<w:ruby>` (rubyPr/rt/rubyBase), 圏点 → `<w:em w:val="comma"/>`, a wholly-vertical doc → section `<w:textDirection w:val="tbRl"/>` — so a Paper page opens in Word with real ruby/emphasis/vertical, not literal `《》`. **Import**: `<w:ruby>` collapsed to `｜base《reading》` before the run tokenizer (its nested runs would otherwise leak the reading as body text), `<w:em>` runs wrapped in `《《…》》`, a `<w:textDirection w:val="tb…">` section → frames `writingMode:'vertical-rl'`; each disclosed in import `limitations`. Verified: 5 new tests (import ruby/em/vertical + export→native XML + full round-trip) + full `src/lib` sweep **1958 green** + build green + a generated `.docx` validated with Python (well-formed OOXML, contains `<w:ruby>`/`<w:rt>`/readings/`<w:em>`/`<w:textDirection>`, zero raw `《`). Remaining: selectable vertical *vector* type in PDF/X (raster already correct; high-risk, deferred).
- [x] **Japanese Phase 3: CJK line-breaking + kinsoku + vertical text-flow in the threading engine (docs/notes/857).** `paperTextFlow.ts` (which `paperThreadFlow.ts` uses for threaded-frame slicing + the overset ⚠) was Latin-only: (1) it tokenized on whitespace, so space-less Japanese became ONE giant "word" that overflowed — threading/overset was broken for *any* Japanese text; `tokenize` now makes each CJK glyph its own breakable unit (with `sep`/`cjk` fields keeping the line text source-faithful, Latin behaviour byte-identical). (2) The flow now applies 禁則処理 when slicing — never start a line with 。、」 (pulled up, 追い込み) or end it on 「『（ (pushed down). (3) New `PaperTextFlowTypeSpec.vertical` (set when `writingMode === 'vertical-rl'`) swaps the flow axes so a line's length is bounded by frame HEIGHT and lines march right-to-left across the WIDTH → correct capacity/overset for 縦書き frames (browser still renders glyphs via CSS; exclusions skipped in vertical). Verified: new `paperTextFlow.test.ts` cases (CJK per-char break, 行頭/行末 kinsoku, vertical height-bound + RTL + overset) + full `src/lib` sweep **1953 tests green**; build (tsc -b) green. Remaining: selectable vertical *vector* type in PDF/X (raster already correct); DOCX round-trip of vertical/ruby/emphasis.
- [x] **Interface i18n: English / 日本語 language setting (docs/notes/858).** The Japanese authoring surface added JP-only labels to an English UI; the owner asked for a switchable interface language ("one for each language… and where it makes sense, both… but it needs to be a setting"). Built a tiny dependency-free i18n core: `src/lib/i18n.ts` (pure — `AppLocale`, `MESSAGES` catalog `{en, ja, both?}` `as const satisfies`, `translate(key, locale)` locale-following vs `translateBoth(key)` always-both for JP typographic terms 右綴じ/ルビ/圏点, `resolveDefaultLocale` from `navigator.language`, `normalizeLocale`) + `useI18n()` React hook subscribing to `settingsStore.locale`. New persisted `locale` setting (mirrors the `interfaceDensity` enum pattern: initial `resolveDefaultLocale()`, sanitising setter, `normalizeLocale` in the persist `merge`) + a **Language / 言語** picker in SettingsModal (English / 日本語 endonyms). First translated surface = Paper (highest-value, JP-authoring): top-strip toggles (Rulers/Guides/Grid/Frame edges/Spreads/Start R via `t`, **RTL 右綴じ** bilingual via `tBoth`), the ルビ/圏 mini-toolbars (labels + locale-following tooltips), the "Japanese 縦組 · Manga" inspector header, and the binding-direction status toast. Verified: `i18n.test.ts` (catalog completeness, translate/translateBoth/normalizeLocale) + `settingsStore.test.ts` interface-language block (setter sanitise + persist-merge rehydrate) — 17 new + full `src/lib`/`src/store`/`src/features/paper`/`src/components/Settings` sweep green; build (tsc -b + vite) green, dist mtime advanced. Honest scope: this is the i18n **foundation + first surface** — the rest of the app is still English (incremental per-surface migration); JP strings are best-effort DTP terms and want a native proofread (esp. `右起点` for "Start R").
- [x] **i18n rollout — global chrome (batches 1–2, docs/notes/858).** Extended the en/ja i18n system from the foundation to the bars every user sees on every screen, translating each surface **fully or not at all** (no half-translated screens). Added `translateFormat(key, locale, {name})` / `useI18n().tf` — placeholder interpolation, needed because word order differs across languages (`Add {name} node` vs `{name}ノードを追加`). **Batch 1 — `TopNavbar`:** the entire top bar — workspace-switcher tooltips (Flow/Video/Image/Paper NAMES stay as product proper nouns; their descriptions + "Workspace" translate via a per-tab `descKey`), primary actions (Projects, Functions, Export/Copied/Failed, Command palette, Provider settings, Fullscreen, zoom in/out/Fit, Serve to Desktop), app-menu chrome (Application menu / Menu / Switch to classic menu bar / hide-show-expand-collapse interface), and the Video titlebar sub-components (Render/Rendering, Source Bin, Composition, No compositions, Undo/Redo, panel toggles, Help, Source Library summary) — desktop + mobile-drawer variants. **Batch 2 — `BottomToolbar`** (Flow node toolbar): wrapper chrome via `tf` (`Add {name} node`, `Open {name} node category`, `Add {name} image node`, "Image model templates" header); node-catalog NAMES stay as data. Verified: build (tsc -b + vite) green + dist mtime advanced; new `translateFormat` test; **full suite 4044 tests / 550 files green** (default test locale = en, so existing English assertions still hold). Queued next: Settings modal (provider brand names stay English by design there), command palette, common dialogs, per-workspace inspectors, node-catalog data.
- [x] **i18n rollout — Settings modal (batch 3, docs/notes/858).** Fully translated the ~1500-line `SettingsModal` (`settings.*`, the largest single surface): dialog/tab titles, all section headers, every field + option label, all help/prose paragraphs, buttons, the encrypted-backup UI + its JS status/error messages, the keyboard + gamepad editors, image-provider help-card section labels, pricing-table headers/footer, and catalog-status text. Provider/brand NAMES (OpenAI, Atlas, Stability AI, ElevenLabs…) + format-example placeholders (`sk-…`, URLs) stay English by design. Method: a **counted Python codemod** for the 57 unambiguous `label=`/`title=`/option-label replacements (reports per-key hit counts so nothing fails silently) + hand edits for text nodes, ternaries, and interpolated strings; `useI18n()` wired into all ~12 sub-components. Hardened the `t()` core to **fall back to English (never `undefined`)** when the locale is missing — the SettingsModal tests SSR-render against a pre-hydration store snapshot where the selector returns undefined. Verified: build (tsc -b + vite) green + dist mtime advanced; i18n catalog-completeness test covers every new key (en+ja present); **full suite 4045 tests / 550 files green**. Also migrated the **command palette** chrome (`palette.*` — search field + empty state; command names stay registry data). Queued next: common dialogs (content lives at call sites), per-workspace inspectors, and node-catalog/provider-capability DATA (a larger job — those labels live in non-React data modules, so bilingual needs the data resolved through the catalog at render).
- [x] **First-run bilingual language gate + manga-title splash wordmark (docs/notes/858).** On a fresh install the app now shows a one-time **bilingual language chooser** before anything else: it pre-selects from the detected system language (`resolveDefaultLocale`) but the user confirms/overrides with big **English / 日本語** buttons, with all copy shown in both languages (they haven't chosen yet). Gated by a new persisted `settingsStore.localeChosen` (default false; set true by `setLocale` from the gate OR Settings; read back in the persist `merge`) so it appears exactly once. New `FirstRunLanguageGate.tsx` (self-gating overlay, z-200) rendered at the end of `App.tsx`. **Brand wordmark is now bilingual, anime/manga-title style:** reusable `BrandWordmark.tsx` renders the Latin "Sloom Studio" over a faint oversized katakana **ghost** (スルーム・スタジオ) plus a small wide-tracked katakana **subtitle** with hairline flanks; `BRAND_NAME`/`BRAND_NAME_KATAKANA` exported from `i18n.ts`. Used on the in-app `StartupSplash` (overlaid low on a legibility scrim) and the first-run gate; the **Electron main-process splash** (`buildStartupSplashHtml` in `main.mjs`) got the same treatment inline (CSP-safe, system JP font stack). Also moved the **Language picker to the TOP of Settings** under a new bilingual "Interface · インターフェース" section (was buried at the bottom of Providers — the owner couldn't find it). Verified: build (tsc -b + vite) green; i18n + settingsStore tests green (new firstRun.* keys carry en+ja; `localeChosen` in state/merge). JA strings best-effort pending native proofread. Owner preview: `scratchpad/brand-preview.html`.
- [x] **i18n rollout — application MENUS incl. the custom KDE Global-Menu (batch 4, docs/notes/858; owner: "make sure the custom Global-Menu works too").** All FOUR app-menu surfaces are now bilingual: the integrated React menu (`TopNavbar`), the native in-window Electron menu, the KDE **global menu**, and the native-Wayland **panel menu**. Menu translations deliberately do NOT live in `i18n.ts` — the Electron **main process** (`.cjs`) can't import the TS `MESSAGES` catalog — so a sibling **`labelJa`** field was injected beside every English `label` in the shared single-source-of-truth **`shared/workspaceMenus.json`** (245 fields, via a counted formatting-preserving string-replacement codemod with a full-coverage report; brand group **Flow** left English by design). Each consumer gained a `locale` param **defaulting to `'en'`** so every existing English assertion + the native↔integrated parity test stay green (Japanese is purely additive): `appMenuModel.ts buildAppMenuGroups(ws, shortcuts, locale)` (also feeds `buildCommandPaletteEntries({…, locale})` → command-palette entries now localize too), `electron/menu.cjs`, `electron/globalMenu/dbusMenuModel.cjs` (+ `ROLE_ITEMS` Quit/Reload/Full-Screen got `labelJa`), and `panelMenuModel.cjs` (reuses the dbus builder). **Locale reaches main** exactly like keyboard shortcuts: `App.tsx` effect → new `bridge.setLocale` (preload + optional `NativeAppBridge.setLocale`) → IPC `signal-loom:set-locale` → `main.mjs let appLocale` rebuilds the in-window menu + `globalMenuController.refreshShortcuts()` + `panelMenuService.refresh()`; both controllers take an injected `getLocale`. Verified: new JA tests in `appMenuModel.test.ts` (ja labels + Flow stays English + en default unchanged), `workspaceMenus.test.ts` (native↔integrated JA label PARITY — the surfaces can't drift), `electronGlobalMenu.test.ts` (dbus JA top-level + role + en default); a **runtime smoke of the actual `createGlobalMenuController` + `createPanelMenuService` main-process chain** (`プロジェクト…`, `元に戻す`, `Sloom Studio を終了`; English unchanged); 74 menu+i18n tests green; full `npm run build` (tsc -b + vite) green, dist mtime advanced. The DBus transport itself was already on-device-verified in prior sessions (notes 826/827); this batch keeps the menu structure/commands byte-identical and adds translation.
- [x] **i18n rollout — Paper context menu + toolbar, Source Library sidebar, shared dialogs (batch 5, docs/notes/858).** Extended the en/ja rollout to the surfaces the owner explicitly flagged as "so very much" still English. **Paper right-click context menu** (frame + canvas): all 73 `paperUsabilityActions` via a sibling `labelJa` + `paperActionLabel`/`paperActionGroupLabel` helpers + ~40 hand-labeled entries (`paper.ctx.*`). **Paper toolbar + export dropdown**: `paper.strip.*`/`paper.export.*` (format acronyms PDF/KDP/DOCX/CBZ/IDML/… kept English; a hidden test-fallback container preserves the strings `PaperTopStrip.test.tsx` scans). **Source Library / Source Bin sidebar** (`FlowSourceBinSidebar.tsx`): fully — ~105 `sourceBin.*` keys covering header/tabs/search/filters/empty-states, BOTH context menus (item + envelope), every confirm/alert/text-input dialog, the metadata dialog, per-item card tooltips, and the preview placeholders; the 6 sub-components each got their own `useI18n()`; module-level `sourceLibraryPrimaryActionLabel(action, locale)` now takes a locale; `MiniImportChip` keeps its English `label` prop for icon `===` logic but renders a translated display via `MINI_IMPORT_CHIP_KEYS`. **Shared common dialogs** (`ConfirmationDialog`/`AlertDialog`/`TextInputDialog`): default chrome (Cancel/Continue/OK + default titles) via `common.cancel`/`common.continue`/`common.ok` + `dialog.*` — fixes the fallbacks shown on every dialog app-wide. Updated `FlowSourceBinSidebar.test.tsx` (it scanned the component for a literal dialog title now in the catalog → now asserts the `t('key')` call + the en+ja strings in `i18n.ts`). Verified: `npm run build` (tsc -b + vite) green + dist mtime advanced; 15 tests pass (i18n, FlowSourceBinSidebar, AlertDialog, TextInputDialog). Queued next (priority): Paper INSPECTOR (large), Paper `setStatus()` toast/status literals (~50), Flow node catalog + inspectors, Video workspace, Image editor panels, node-catalog/provider DATA.
- [x] **i18n rollout — Paper INSPECTOR + Flow node PALETTE (batches 6–7, docs/notes/858).** **Batch 6 — Paper Inspector:** `PaperInspector` (`PaperWorkspace.tsx`) + its 5 sibling panels (`PaperPreflightPanel`/`PaperLinkedAssetsPanel`/`PaperDtpParityPanel`/`HorizontalRuler`/`VerticalRuler`) fully translated — **247 `paper.insp.*` keys, 268 replacements** via a bounded Python codemod that inserts the catalog block + rewrites only the inspector line range (`label=`/`title=`/`placeholder=`/`aria-label=`→`{t()}`, `>text<`→`>{t()}<`); covers every Document/Frame/Text/Image/Bubble/Print-Production/Style/Preflight control. Kept English by design: axes X/Y/W/H, `DPI`, `https://…`, the `Ag…` type specimen, `Sloom Studio:`. The 5 sibling panels each needed their own `useI18n()`. **Batch 7 — Flow node palette (data-driven):** `nodeCatalog.ts` (the single source for the whole "add node" palette — 10 categories + 62 node entries) got `NODE_CATEGORY_JA`/`NODE_ENTRY_JA` maps + locale resolvers `nodeCategoryLabel/Description` + `nodeCatalogEntryLabel/Description` (default `'en'` → English source + all tests unchanged); `findNodeCatalogEntries` now searches JA too (Japanese palette search works). Both consumers wired to pass `locale`: `BottomToolbar.tsx` (category buttons + node entries) and `App.tsx` (canvas right-click add-node menu). Category names, node names, node descriptions, tooltips, add-node menu all localize at once. Verified: `npm run build` (tsc -b + vite) green + dist mtime advanced; nodeCatalog + BottomToolbar + i18n + 42 paper tests green. **Remaining (documented, priority order):** Video workspace (`VideoWorkspace.tsx`, 307 strings / 70 components — hits multi-line + HTML-entity + many-sub-component-hook edge cases, needs a fresh budget for a safe atomic pass), the 77 Flow node-BODY components (`src/components/Nodes/*.tsx`, ~474 strings), Image editor panels (~10 files), Paper `setStatus()` toasts (~174 mostly-interpolated). The nodeCatalog resolver-map + bounded-codemod patterns are the proven templates for finishing these.
